"""Record GIDEON's improvement loops from the host checkout.

This package imports the standard library, ``yaml``, and ``gideon`` alone.
The trigger registry and its later readers run from the host checkout on the
box's system Python, as ``gideon.evaluation`` does (ADR-0046).
"""
