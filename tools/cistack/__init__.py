"""Run the ``up``, ``down``, and ``status`` commands for the ``gideon-ci`` stack.

The tool owns only the sibling's footprint under ``/data/ci`` and never edits
production's rendered tree or backup staging area.
"""
