"""The clean-VM acceptance harness.

This package is box-side tooling and must remain importable with only the
standard library, PyYAML, and the repository's :mod:`gideon` package.  It is
never a pip dependency and never imports a third-party runtime library.
"""
