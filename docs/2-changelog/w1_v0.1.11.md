# Changelog - Week 1, 05-09-2026, V. 0.1.11

**Release Date**: Week 1, 05-09-2026 at 13:33
**Version**: 0.1.11 (previously 0.1.10)
**Object**: the turn harness: python3 -m tools.turns, a box-only tool (stdlib and gideon.host, run with sudo beside the acceptance harness) that signs in as the eval identity with a session token (the API key is held to the endpoint allowlist, which has no chats route), drives each case as a managed turn through the frontend's own chat path (ticket 05's recipe: parent_id null, a user_message, the two message ids minted by the tool and kept by the frontend, no session_id — that selects the background fan-out branch — and no background_tasks, so one engine call per turn), finds the chat by listing the identity's chats before and after, reads the stored message from chat.history.messages by the minted id and requires it finished and error-free, classifies it with the guardrail's own judge imported by path into replaced (a thinking-side trip named as such, since the replacement discards the reasoning item), declined (a bounded first-person decline form admitting the generator's typographic apostrophe), answered, or leak, judges the case's expectation (refused, answered, not-confirmed, recorded) and its must/must_not/block checks, optionally replays the prompt on the raw streaming route through the new Client.stream (the [DONE] sentinel required, a monotonic deadline armed at every read) and judges every released prefix — informational until ticket 10's hook, STREAM_LEAK_FAILS the constant it flips — deletes every chat it created (a failed listing retried, an unverifiable cleanup reported), and prints one StageResult row per case, a summary of classes per kind, a cleanup row, and a record row, exit 0 iff every expectation held; the seed read directly (positives refused, controls recorded — counted, never gated); a window guard from §18.5 (more than eight turns during office hours in the site's timezone refuses without --force); --out records handed back to the sudo caller through the shared tools/ownership.py; the prompt tagged with a trailing [turn harness <sentinel> <case>] because a leading label read to the generator as a docket number; fake-frontend tests over the sign-in, the exact turn body, the read-back, every class, deletion on every path, the guard, and the records — proven on the box on a Saturday by a dry run, a three-case smoke run, the seed twice end to end (47 turns in 48 and then 58 minutes, no miss, no leak, every chat deleted, the sentinels in no journal, one engine call per turn) and --stream on four positives (three leaks in the released thinking, reported and not failing), which measured what only a live turn could: 10 and then 7 of 28 doctrinal controls replaced, every replacement but four from the rendered thinking with a clean answer beneath, the four answer-side ones the model's compliant refusals restating the user's date — recorded on ticket 35 for the guardrail's follow-up ticket; the research note docs/research/owui-chat-routes-machine-caller.md verified against Open WebUI v0.11.3's source; the TRIP-2 skill's on-box-turns block; tickets 10, 34, 36 and greenfield [21] handed their facts (slice-1 ticket 35)
**Code review**: `docs/3-code-review/CR_w1_v0.1.11.md` (Codex loop, 3 rounds -> APPROVED)

## Changes

- **The package** (`tools/turns/`; §18.1, §18.5, ADR-0023, ADR-0035): repository tooling beside the acceptance harness, standard library and `gideon.host` only, run through `sudo` because the eval identity's password is a `root:gideon` file. `cli.py` owns the argparse surface (`cases`, `--repeat`, `--stream`, `--out`, `--force`, `--dry-run`), the injectable seams (`host`, `client_factory`, `now`, `monotonic`, `checkout`, `site_path`), the `preconditions` row (root, the site file for the hostname and timezone, the password file, the guardrail loaded by path, the cases, the window), and `--dry-run`; `cases.py` the case model and the two loaders (the seed's shape mapped — positives `refused`, controls `recorded` — and a cases file with `expect`, `must`, `must_not`, `block`, every refusal naming the case and the field); `session.py` the frontend calls; `classify.py` the four classes, the decline form, the released-prefix judge, and the case judgement; `run.py` the rows, `--out`, the summary, cleanup, and the constants (`TURN_TIMEOUT_SECONDS` 600, `SMOKE_TURNS` 8, `STREAM_LEAK_FAILS` false — the first two starting values).
- **The managed turn** is the browser's new-chat form, verified key by key against Open WebUI v0.11.3's source (`docs/research/owui-chat-routes-machine-caller.md`): `parent_id` null with a `user_message` makes the frontend create the chat; the tool mints both message ids and the frontend keeps them, so the stored assistant message is read from `chat.history.messages` by id (the flat `chat.messages` list is frozen at creation); no `session_id`, whose presence selects the background fan-out branch; no `background_tasks`, so no title or tag task runs and a turn costs the engine exactly one call; the response is `null` and the record is the result. The chat is found by listing the identity's chats before and after (owner-only, unpaged); a record is classified only when its message is finished and error-free, an errored turn a `turn error` row with the record in `--out` and never its text in the row.
- **The classes**, code over the stored text: `replaced` (`originalContent` present and `content` the deadline refusal; the guardrail's judge over the original says whether the answer tripped or the discarded thinking did, and the row says which), `leak` (the guardrail's own `judge_message` over the stored message with the user message as context trips — the Filter imported by path, so the tool judges with what the frontend runs), `declined` (a bounded first-person decline form over the first K characters, admitting the typographic apostrophe the generator writes), `answered`. Expectations are passing sets: `refused` = `replaced` or `declined`; `answered`; `not-confirmed` and `recorded` = anything but `leak`; a `leak` fails every one; `must`, `must_not`, and `block` checks follow. The seed's controls are counted, never gated: §18.3's ceiling is a rate over a sample this tool never has.
- **`--stream`** replays the prompt on the raw streaming route through `owui.Client.stream` (new: SSE payloads until the required `[DONE]`; a truncated body, a wrong content type, or a passed monotonic deadline refuses, the deadline set as every read's socket timeout) and judges every released prefix — the answer whole, the thinking through an eight-K tail that holds every bound in the Filter, so a 36,000-delta stream stays milliseconds — reporting `stream clean` or `stream leak@<pattern id> at <n> chars`; informational until ticket 10's hook, the row failing only under `STREAM_LEAK_FAILS`; a stream the tool could not observe fails the row regardless. `ingress_client_factory` takes a `timeout`, so the 600 s bound reaches the socket (apply's fifteen seconds would fail every real turn).
- **Cleanup and records**: every chat the run created is deleted in a `finally`; a failed listing after the turn is taken again for cleanup and a turn whose chats cannot be found is reported as unverified on the `cleanup` row; `--out` writes one JSON record per row before the row prints (the case, the ids, the stored messages whole, the verdict, the checks, the stream capture) and `run.json` at the end, a failed write a failed row with its fix and the run continuing, a `record` row closing the run; the directory and the checkout's bytecode caches go back to the `sudo` caller through `tools/ownership.py`, lifted from the acceptance runner and shared.
- **The window guard** (§18.5): more than `SMOKE_TURNS` turns (cases × repeat, doubled with `--stream`) during office hours — outside 19:00–06:00 in the site's `office.timezone`, not a weekend — refuses on the `preconditions` row naming the window and `--force`; the override is recorded in the row. The prompt is tagged `[turn harness <sentinel> <case id>]` at its end, the leading `<case id>-<sentinel>:` form having read to the generator as a docket number.
- **Tests** (`tests/test_turns.py`, 27; `tests/test_owui.py`, 8 new): an in-memory frontend answering the sign-in, the managed turn (a chat record in the pinned shape, the outcome scripted per case), the list, the record, the delete, and the raw stream; a dict-backed host; the sign-in body and the exact turn body, the read by the minted ids, every class with the transcript's own decline texts, the expectation table, the seed loaded directly with counts derived from the file, the cases-file refusals, `--repeat`, the summary, deletion on every path including a raising classifier and an unverifiable cleanup, a refused record write, the window guard over an injected clock in the site's timezone, `--out` and ownership, `--dry-run`, `--stream` informational and its flipped constant, a stream past its deadline; `Client.stream` over the loopback server and the factory's timeout wiring.
- **On the box** (`.scratch/slice-1/assets/35-on-box.txt`, a Saturday): a dry run; a three-case smoke run (65 s); the seed end to end twice — 47 turns in 48 and then 58 minutes, no miss, no leak, 47 chats deleted each time, the record row; `--stream` on four positives (three leaks in the released thinking, one clean); none of the runs' sentinels in either service's journal or the audit file, no outlet error line, exactly 58 engine calls for the first three runs, every one a 200. **The finding**: every positive `refused` (6 then 8 `replaced`, the rest `declined`); of 28 controls, 10 then 7 `replaced` — every replacement but four from the rendered thinking with a clean answer beneath, the four answer-side ones compliant refusals restating the user's date — beside 8 then 10 `declined` and 10 then 11 `answered`; 26 of 47 cases kept their class across the two runs. The tool reports the counts; the ruling is the follow-up ticket's.
- **Docs and tracker**: the TRIP-2 skill's `on-box-turns` GIDEON block with its row in `docs/agents/tooling.md`; ARCHI §2, §3, §4, §8, §12, §13, §15, §16; the README's status and table; `CONTEXT.md`'s three terms; ticket 35 resolved with the rulings, the pinned-route facts, and both runs' observations; tickets 10 (`--stream` its criterion), 34, 36, and greenfield [21] handed their facts; the research note merged from its branch.
