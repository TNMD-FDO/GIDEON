# Changelog - Week 2, 08-09-2026, V. 0.1.29

**Release Date**: Week 2, 08-09-2026 at 17:25
**Version**: 0.1.29 (previously 0.1.28)
**Object**: the cycle's cost, recorded: tools/cycles.py, a stdlib tool run from the dev venv (python3 -m tools.cycles [--write]), reads the harness's session transcripts for the primary checkout and every worktree — the primary resolved through a linked worktree's .git file, the top-level files alone, sidechains and the per-session subdirectories excluded — and computes per session the peak context (the largest prompt sent: one response's input, cache-creation, and cache-read tokens together) with its time, the totals, the turns as distinct message ids (the harness writes one response as several entries with identical usage, which the survey script had counted whole — its turns and totals about 2.7 times too high, its peaks and its one compaction standing), every compaction boundary with its trigger and counts, the skill and the ticket and plan paths recognized by pattern, and the worktree the session entered; the band table one row per effort the cycle sessions' tickets name, a ticketless cycle slice 0's for plans 0.0.x or 0.1.0 and otherwise unattributed, then triage, then every session; --write merging into .scratch/CYCLES.md by short session id — a present transcript rewrites its row, an absent one keeps it, nothing dated by the run, so a second write is byte-identical — the first record's 71 rows committed, v0.1.25's cycle at 697,069 and v0.1.0's implement session's auto-compaction at 1,001,054 among them; ids and figures only, no prompt text, the evidence hygiene walking the file; a transcript without usage or with a malformed usage (a missing field, a differing repeat) counted and never a row of zeros; three refusals with their fix (no store for the family, a short-id collision, an unparseable record — the separator row required, the code review's one finding); the write in the outstanding-items chain (docs/agents/outstanding-items.md), CLAUDE.md's Ticket sizing rule carrying the measured band in place of its anecdote — the window 1M, slice 0's median 509k and maximum 997k with one compaction, slice 1's 575k and 697k with none — the record's path, a compaction the too-large signal and a peak past 800k the early warning, the constant held to the paragraph's figure by a test; the tracker convention, the README row, the tools leaf's row, the glossary's peak context; tests/test_cycles.py over synthetic fixtures alone, 19 tests, the AST check holding the module to the standard library; proven from the cycle's worktree against this machine's store — the slice-0 band identical to the survey's, the second write byte-identical, 5 rows kept over a copy of the family lacking two directories (workflow ticket 06, resolved; released as v0.1.29, v0.1.27 and v0.1.28 having landed from the primary during the cycle)
**Code review**: `docs/3-code-review/CR_w2_v0.1.29.md` (Codex loop, 2 rounds -> APPROVED)

## Changes

- **Feature**: `tools/cycles.py` (new), a single standard-library module: discovery of the checkout family's harness project directories (the primary resolved from a linked worktree's `.git` file; every `--claude-worktrees-` directory; a sibling checkout excluded by the exact prefix; `~/.claude/projects` or `$CLAUDE_CONFIG_DIR/projects`, or `--projects`); the reader over the top-level transcripts alone (sidechains and the per-session `subagents/` and `tool-results/` directories never read), one turn per distinct `message.id` with its usage read once and every repeat validated identical, the four usage fields required as integers, the peak context with its time, the totals, every `compact_boundary`, the skill and the ticket and plan paths by pattern, the worktree a session entered as its *where*, a session present in two directories read once; the band rule per effort with the enumerated slice-0 fallback; the stdout renderer; the record renderer with a fixed preamble and no run date; the record parser (the separator row required, the compactions list keyed by short id), the merge keyed by short id, and `--write` with its default path under the running checkout; three refusals in the `python3 -m tools.cycles: <problem> Fix: <fix>` shape. `.scratch/CYCLES.md` (new), the first record: 71 sessions, 52 stub transcripts counted, one compaction.
- **Rule and workflow**: `CLAUDE.md`'s Ticket sizing paragraph carries the measured band (the window 1M; slice 0's cycle sessions median 509k, maximum 997k, one compaction; slice 1's 575k and 697k, none), the record's path, `/to-tickets` reading it for the previous slice's band, a compaction the too-large signal and a peak past 800k the early warning; `docs/agents/outstanding-items.md`'s commit chain runs the write and stages the record (the `outstanding-items` block unchanged); `docs/agents/issue-tracker.md`'s Conventions name the record as a top-level file that is never a ticket; `docs/agents/tooling.md` §4's rules paragraph names what the sizing rule reads; `README.md`'s "Where things are" gains the row.
- **Corrections found on the way**: the survey's turn counts and totals were about 2.7 times too high (one response, several entries with identical usage: the slice-0 implement session's 1,078 entries are 464 responses, 559.6M tokens in are 244.6M), its peaks and its one compaction standing, so the ticket's figures and the rule's band are unchanged and the survey asset stays as the record of its day; a session's *where* is the first worktree it entered, since a claim starts in the primary and the first working directory alone called every cycle session the primary's.
- **Tests**: `tests/test_cycles.py` (new, 19 tests, 20 subtests) over a synthetic projects tree and a synthetic record — the parse (the split response counted once, the sidechain and the subagent file excluded, the peak with its time, the compaction, the *where* labels, the non-JSON line), the no-usage stub, the two malformed shapes, discovery, the duplicate rule, the bands, the merge (a removed session's row and compaction kept, a present one rewritten, the order, the present line's kept count), byte-stability, the three refusals leaving the file unchanged (the missing separator among them, the code review's finding), the hygiene assertion over the record, the CLI's read-only and `--write` runs, the AST check holding the module to the standard library, and the early-warning constant held to the figure `CLAUDE.md`'s section states. Gate: lint clean, 178 files type-checked, 57 passed over the new suite and the evidence-hygiene, vendored-skills, budget, lock-coupling, and toolchain-pins tripwires.
- **Proven**: from the cycle's worktree against this machine's store — 123 transcripts, 71 rows, the slice-0 band identical to the survey's (18 sessions, median 508,727, maximum 996,798, 4 above 800k, 1 compaction), the second write byte-identical by `cmp`, and over a copy of the family lacking two worktree directories 5 rows kept as last written (the wtprobe session verbatim) and 66 rewritten; no harness transcript touched.
- **Documentation**: the tools leaf (the row, the dev-venv paragraph, the command's bullet, the tests line) and the map's §2 row, §4 tree, and §8 table; `CONTEXT.md` gains *peak context* through `/domain-modeling`; the tutorial `docs/5-tuto/tuto_0.1.29.md`. **The map's count**: ~12,039 tokens by the count script after this release's additions (the tools leaf ~3,316; the glossary ~12,897, reported and never budgeted), past its 12k warning and under its 14k cap, as the `v0.1.27` observation predicted for the next release that added to it. Its compaction is deferred by the maintainer's decision at this release: three cycles were in flight in parallel worktrees, and a compacted map on this branch would collide with any map edit their releases carry, so `/TRIP-compact docs/ARCHI.md` runs from a tracker session in the primary on `main` once they have landed — a `ready-for-human` item at this release's outstanding-items step. **Release note**: a pre-General 0.x tag, skipped per spec §21. **Box ledger**: the §11 fetch found the other repository's copy identical to ours except for our own `v0.1.26` entry, which it lags; nothing shared touched, no entry.
- **Version**: bumped 0.1.28 → 0.1.29 (with `tests/test_version.py`); a patch, nothing under the product's runtime tree; workflow ticket 06 resolved; released as v0.1.29, v0.1.27 (the citation stamp) and v0.1.28 (the vendored-skills hash rule) having landed from the primary during the cycle, the plan file keeping its `F_0.1.27` name.
