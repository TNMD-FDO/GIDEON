# Changelog - Week 3, 18-09-2026, V. 0.2.1

**Release Date**: Week 3, 18-09-2026 at 14:03
**Version**: 0.2.1 (previously 0.2.0)
**Object**: release: v0.2.1 — the box's install home is /opt/gideon (slice-1 84)
**Code review**: `docs/3-code-review/CR_w3_v0.2.1.md` (Codex loop, 1 round -> APPROVED)

## Changes

This release implements slice-1 ticket 84 under the plan `docs/1-plans/opt-gideon-install-home.plan.md`, on the ticket's rulings (a)–(e) of 2026-09-18 and their amendment. Until now, TNMD's box ran from the development primary `~/src/GIDEON`. `upgrade` checked tags out there detached, the upgrade span had to be declared session-free, and a tracker session's uncommitted file refused `upgrade`'s `preconditions`. The box now runs from `/opt/gideon`, the install home every receiving office uses: a clone owned by the dev seat, with the primary as its `origin`. The development checkout becomes an ordinary clone. No product module changes, and no service is recreated.

- **`bin/worktree-remove <slug>`** (new, development repository). A third guard script, beside `bin/worktree-claim`, replaces the bare `git worktree remove` and `git branch -d` in `worktrees.md` §8, `TRIP-3-release` Step 13, and `TRIP-hotfix` Step 8. It checks, in order: the worktree is registered, and its branch is read from git's listing; the branch is on `main`; and no rendered unit's `WorkingDirectory=` names the worktree or a path under it. Only then does it remove the worktree (never `--force`) and delete the branch. A unit naming the worktree is refused with the upgrade from `/opt/gideon` at `main`'s nearest version tag as the fix, read with `--abbrev=0 --match 'v[0-9]*'` so the printed command never carries a `-<n>-g<sha>` suffix. An absent units directory passes. The script keeps the trigger of the miss met at `v0.1.65` and `v0.1.77`, a release that skipped the re-apply and left the units naming a removed worktree, and the workflow note that weighed it is deleted.
- **`tests/test_worktree_remove.py`** (new, 13 tests) holds that contract over a temporary primary and units directory. It joins the export's exclusions in `tools/exportboundary.py` and `.gitattributes`.
- **The rule's documents.** CLAUDE.md's two box paragraphs, `worktrees.md` §1, §2, §6 (retitled "its install home"), and §8 step 10, and `on-box-proofs.md` §7 (retitled) and its opening now say the same thing. A proof's apply still runs from the cycle's worktree. The release upgrades the box with `sudo ./upgrade.sh v<x.y.z>` from `/opt/gideon` after the fast-forward, with the tag still local, then removes the worktree with `bin/worktree-remove`. The session-free upgrade window and the return of the primary to `main` are gone. The separate `engine verify` after the release's apply goes too, because the upgrade gates on its own. `TRIP-1-plan`'s Window paragraph, `TRIP-3-release` Step 13, `TRIP-hotfix` Step 8, and `onboarding.md` §6 (its two anchors) follow. `CONTEXT.md`'s *Dev seat* entry names `/opt/gideon` (via `/domain-modeling`). ADR-0037 stands.
- **The box ledger.** §1's Install-home row is `/opt/gideon`, the rules version moves to 2026-09-18.1, and §12 has this tag's entry. Transcribe's copy was fetched at 2026-09-09.6 with its subsection equal to ours; nothing was merged, and no line addressed to GIDEON is open.
- **Architecture.** The map's §4 states `/opt/gideon` as the install home, TNMD's box's too, in place of the "reserved" sentence, and names `worktree-remove` in the `bin/` comment and in §8's tools row. The map went from ~11,939 to ~11,938 tokens against its 12,000 warning. `docs/archi/tools.md` gains the script's Inventory row, Commands bullet, and Tests line, reaching ~5,952 tokens against 6,000.
- **Proof at `TRIP-2`** (`.scratch/slice-1/assets/84-on-box.txt`, first section, 2026-09-18 13:43 CDT). `/opt/gideon` was created by `sudo install -d` owned by the dev seat, then cloned by the dev seat with `--branch v0.2.0 --no-hardlinks` from the primary's path. It is owned `fpdadmin:fpdadmin`, its `origin` is the primary, its HEAD is detached at `v0.2.0`, and `gideon 0.2.0`. `render --diff` from it with `-B` showed the four units' `WorkingDirectory` hunks alone and `Services apply would recreate: none`. The newest set's link sample spreads 88 `checkout`, 11 `data-registry`, and 1 `data-bulk-openwebui`, so a fresh clone's first set passes with a partial linked count.
- **Proof at `TRIP-3`**: the first upgrade from the new home runs after this commit's fast-forward, in `worktrees.md` §8 step 10's slot. Its transcript (criteria 2 and 3) and the ticket's close-out ride the tracker commit that follows this release commit on `main`.
- **Window**: none. The upgrade's apply moves the four units, which have no container owner, and recreates nothing users see. ADR-0044 keeps users off the box before go-live, so the human items of spec §21 (the supervisor informed, an announced window, the CSAs sending the note) do not apply yet. The upgrade was announced to Transcribe's operator under the ledger's §8.
- **Tutorial**: skipped. This is a patch tag moving the development workflow's install home; the rule is stated in `worktrees.md` §6 and `on-box-proofs.md` §7.
- **Release note**: `docs/release-notes/v0.2.1.md`, with nothing user-visible.
- **The human read** (ADR-0036, ADR-0044): skipped, a patch tag before go-live.
- **Acceptance run**: none, a patch tag whose plan names none.
