# Changelog - Week 2, 08-09-2026, V. 0.1.28

**Release Date**: Week 2, 08-09-2026 at 17:06
**Version**: 0.1.28 (previously 0.1.27)
**Object**: the vendored-skills hash rule holds a block-carrying skill around its blocks: tests/test_skills_vendored.py's rule b hashes every non-slot TRIP skill with each GIDEON block's marker lines and body removed and holds the result to the lock's upstream hash — a Matt Pocock folder hashed whole, the five slot skills skipped as before — through one keyword-only reader on tools/pinwatch/skills.py's folder_hash so the CLI's algorithm keeps its one home: an edit outside a block in a block-carrying skill red, a lock hash rewritten to a customized folder red (the defect class this ticket found, now a subtest), an edit inside a block no hash finding, the four subtests' subjects derived from tooling.md's §3 and §4 tables; the clone checker's fourth status, installed and lock differ from clone, when the installed folder and the lock both differ from the clone — the ticket's claim that the checker reported lock differs from clone was masked by the installed comparison, corrected on the ticket at the claim; skills-lock.json's codex-implement value restored to upstream v2.8.0's from 08554af, the implement-conventions block's stray blank line moved inside its markers so the template's bytes outside them equal upstream's byte for byte; docs/agents/tooling.md §4 (a block in a skill without slots is the whole delta), §5 steps 3 and 4 (the checker's statuses on a customized tree; codex-implement/prompts/implement.tpl in the merge list; the eight pristine skills named), §8 (the hash rule as it now reads, 34 of 39 still checked); TESTING.md's bullet; the tools leaf; proven from the cycle's worktree by two checker runs against a fresh v2.8.0 clone bracketing the lock's fix — codex-implement: installed and lock differ from clone before, installed differs from clone after, no row naming the lock — and the gate through the link, 112 passed over the five affected modules (workflow ticket 15, resolved; released as v0.1.28, v0.1.27 having landed from the primary during the cycle)
**Code review**: `docs/3-code-review/CR_w2_v0.1.28.md` (Codex loop, 1 round -> APPROVED)

## Changes

- **The rule** (`tests/test_skills_vendored.py`, rule `b`): for every entry outside §3's slot table, the folder hash recomputed with the CLI's algorithm equals `computedHash` — a TRIP-source skill's computed over each file's bytes with every GIDEON block's lines removed (the marker lines and the lines they enclose, at the byte level: a line is decoded only to test it against the marker, a line that does not decode is never a marker, a begin without its end drops to the end of the file beside rule `c`'s finding), a Matt Pocock skill's whole, so §4's rule that those take no block stays mechanical. The finding keeps "folder hash differs from skills-lock.json" and names both causes in its fix: the file restored from the vendor commit or the lock's hash from the pristine commit, a lifecycle edit inside a listed block. Consequence: the lock records upstream for every entry and the hosted checks hold it; an edit outside a block in a block-carrying skill is red; an edit inside a block is not a hash finding.
- **The seam** (`tools/pinwatch/skills.py`): `folder_hash` takes one keyword-only reader from a path to the bytes a file contributes, defaulting to reading the file, so the walk, the collation order, and the digest order stay in one function; the checker and the pin watch call it unchanged. The marker grammar stays in the test module beside rule `c`, which owns it.
- **The checker** (`clone_findings`): a fourth status, `installed and lock differ from clone`, when the installed folder differs from the clone and the lock's hash differs from the clone's too; `installed differs from clone`, `lock differs from clone`, `match`, and the absences unchanged, `main`'s exit contract unchanged. Before it, the installed comparison came first and masked the lock's, so a customized skill's row read the same whatever the lock held — the ticket's sentence that the checker reported `lock differs from clone` was wrong for that reason, corrected on the ticket at the claim.
- **The record**: `skills-lock.json`'s `codex-implement` value returns to upstream v2.8.0's (the value at `08554af`, one line; every other entry unchanged, the CLI's byte-stable format kept); the `implement-conventions` block's blank line, the eleventh line of `8bbf696`'s delta and the one byte of the skill outside a marker pair that differed from upstream, moves inside the markers as the body's last line, so the template's bytes outside them equal upstream's byte for byte and the stripped folder hash equals the lock's.
- **Tests**: rule `b`'s test becomes four subtests — a line inside the block-carrying non-slot skill's block body, no hash finding for that directory; a line in that skill's `SKILL.md` outside any block, a finding; a line in a neither-slot-nor-block skill's file, a finding; the lock's hash for the block-carrying skill rewritten to the customized folder's hash computed at the test, a finding — their subjects derived from §3's and §4's tables by two helpers that name no skill, the other mutation tests taking the neither-slot-nor-block helper; the checker's test gains the fourth-status case. No test types a hash. Gate: `lint: clean | typecheck: clean | tests: 112 passed` over `test_skills_vendored`, `test_lock_coupling`, `test_toolchain_pins`, `test_pinwatch`, and `test_evidence_hygiene`.
- **Proven** from the cycle's worktree against a fresh clone of upstream v2.8.0, the checker's status landed before the lock's fix: the first run `codex-implement: installed and lock differ from clone`, the five slot skills `installed differs from clone`, the other eight `match`, exit 1; the second run, after the lock and the template, `codex-implement: installed differs from clone`, the rest unchanged, no row naming the lock, exit 1 as designed for a customized tree; `git diff -- skills-lock.json` one line, `git diff -- .claude/skills/` one hunk inside the block's markers. Nothing on the box: no apply, nothing recreated, no window.
- **Documentation**: `docs/agents/tooling.md` — §4's placement rule (in a skill without slots the block's lines are the whole delta, a blank line the block wants inside its markers; the Matt Pocock sentence's "nothing stripped" now the literal rule) and the `implement-conventions` row's origin; §5 step 3 (the checker's two statuses on a customized tree) and step 4 (`codex-implement/prompts/implement.tpl` in the merge list; the eight skills with neither block nor slot named); §8 (the hash rule as it now reads, 34 of 39 still hash-checked; the removal the tripwire's alone; the checker's statuses). `docs/4-unit-tests/TESTING.md`'s tripwire bullet; the tools leaf's Tests bullet; `README.md`'s status paragraph. The ticket's claim comment carries the discovery rulings (strip over skip; the fourth status; the patch release) and the fact correction. No glossary change (the **Vendored skill** entry stays accurate), no ADR (ADR-0033's decision stands, refined within itself), no release note (a pre-General 0.x tag, spec §21).
- **Box ledger**: the §11 fetch found the other repository's copy behind ours by our own v0.1.26 entry alone, the rules versions equal; this release touches no shared thing, so no entry and nothing to merge.
- **Tutorial**: `docs/5-tuto/tuto_0.1.28.md` — hashing the complement of the delta: why stripping beats skipping, the reader seam, bytes over strings, the placement convention, and what the tripwire cannot see that the checker's fourth status can.
- **Architecture memory counts** (the count script): the map ~11,979 tokens after the rebase onto v0.1.27, this release adding nothing to it, 21 under its 12k warning; the leaves 1,020–5,639, the tools leaf ~2,661; the glossary ~12,777, reported and never budgeted.
- **Version**: bumped 0.1.27 → 0.1.28 (with `tests/test_version.py`); a patch, nothing under `gideon/`, `compose/`, or `config/` changes and no site-file contract moves; workflow ticket 15 resolved; released as v0.1.28, v0.1.27 (the citation stamp, slice-1 ticket 14) having landed from the primary during the cycle, the branch rebased onto it before the tag.
