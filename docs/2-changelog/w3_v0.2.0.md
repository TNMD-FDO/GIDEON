# Changelog - Week 3, 18-09-2026, V. 0.2.0

**Release Date**: Week 3, 18-09-2026 at 08:47
**Version**: 0.2.0 (previously 0.1.82)
**Object**: release: v0.2.0 — the pre-launch release; slice 1 closes (slice-1 18)
**Code review**: `docs/3-code-review/CR_w3_v0.2.0.md` (Codex loop, multi-round -> APPROVED)

## Changes

This release closes slice 1 at its reserved tag (spec §2.1, §22.1) under the plan `docs/1-plans/pre-launch-release.plan.md`, for slice-1 ticket 18. By the maintainer's rulings of 2026-09-17 no user reaches the box before go-live, so `v0.2.0` is the pre-launch release with no user behind it: it ships to the maintainer and Transcribe's operator alone. No product behaviour changes — nothing under `gideon/`, `compose/`, `migrations/`, or the lock files moved.

- **The decision** (`docs/adr/0044-no-user-reaches-the-box-before-go-live.md`, new). Go-live is the `v0.5.0` release that offers the Legal chat; until then no end user reaches the box. From go-live bind the maintenance-window rule for an apply recreating a service users see, the quiet window beside users, the supervisor's working-day notice, the announced window, the CSAs' sent note, and the human read at patch and hotfix tags; the release note for every tag and the human read at minor tags stay at `v0.2.0`. The ADR names the sentences of §21, §22, §22.1, §22.3, and the handoff note it supersedes and what it amends in ADR-0036, ADR-0037, and ADR-0040; the spec is not edited, and dated comments on greenfield [24] and [33] point to it. `CONTEXT.md`'s **Pre-launch release** entry is sharpened (via `/domain-modeling`).
- **The re-key to go-live**, phrase for phrase inside each sentence whose subject is a user: `CLAUDE.md`'s applies paragraph, `docs/agents/on-box-proofs.md` §6, `docs/agents/worktrees.md` §5 and §6, `docs/agents/onboarding.md`'s human-read paragraph, the four TRIP skills (the window paragraph, the on-box proofs paragraph, the user-facing tag check and the human read, the hotfix's note-and-read line), `docs/release-notes/TEMPLATE.md`'s sending comment (`Template: 2` stands), and `docs/runbooks/pin-watch-app-setup.md`'s model-bump bullet.
- **The pin watch's model body** (`tools/pinwatch/pr.py`). A `models.` pull request's "What merging does" reads "a maintenance window from go-live (§21), with `engine verify` gating the new weights", so "go-live" means one thing in the body; `tests/test_pinwatch.py`'s assertion follows. Open pull requests keep their old sentence (ADR-0033).
- **The upgrade runbook** (`docs/runbooks/install-upgrade.md`). §2's paragraph before a user-facing tag states the window rules themselves, keyed to a receiving office's own first users: the quiet window and what is window-bound, the maintenance window and what takes one, and how its cost is read beforehand. §2's and §3's stage tables gain the `engine-verify` row that landed at `v0.1.22`, and §2's timing paragraph the gate's two to eight minutes.
- **The release note** (`docs/release-notes/v0.2.0.md`, the first). Written from template 2 by the staged text at `.scratch/slice-1/assets/18-release-note-v0.2.0.md`, "What was bumped" pasted whole from `python3 -m tools.pinwatch.bumped` after the version bump (no pin moved since `v0.1.82`, the greatest reachable tag). `tests/test_release_notes.py` is green.
- **The announcement** (`.scratch/slice-1/assets/18-announcement.md`, a draft). The CSAs' message to the attorneys, people as placeholders; the CSAs edit and send it after the tag, which does not wait for it.
- **The ticket.** Slice-1 ticket 18 carries this cycle's four discovery rulings of 2026-09-18 and five recorded conflicts. The `v0.1.62` notes line on the shared filesystem's page is acted on by the ledger's **For Transcribe:** line and deleted.
- **The box ledger** (`docs/box-ledger.md` §12). The §11 fetch found Transcribe's copy at rules version 2026-09-09.6 with its subsection identical to ours; sections 1 to 11 are kept from ours and no line addressed to GIDEON is open. The `v0.2.0` entry names the minor's announce-before items and carries two **For Transcribe:** lines; no rule changes, so the rules version does not move.
- **The map and README.** `docs/ARCHI.md` §12's slice-1 row is **Complete** at `v0.2.0` with its ADR-0044 clause and slice 2's row is in progress; the README's **Status:** paragraph says the same. Step 7: no structural change (no command, module, release constant, or test module added, removed, or renamed). The map's count at the slice's close: `docs/ARCHI.md` ~11,939 tokens (warning 12,000); the leaves 1,783–5,634; `CLAUDE.md` ~1,630; the agent documents 329–5,188; `CONTEXT.md` ~16,503 (unbudgeted).
- **The `0.x` must-nots** (§21; slice-1 ticket 17's rulings of 2026-09-04), each against its record:
  - [x] (1) The guardrail and the stamp on the tag — manifest content pushed by apply (tickets 09–11, 14; the stamp's record `.scratch/slice-1/assets/14-on-box.txt`), unchanged by this release.
  - [x] (2) Nobody outside the users group — the LDAP filter and reconcile's `pending` (slice-0 tickets 05 and 09); and until go-live no user at all (ADR-0044).
  - [x] (3) No install at another office — none made. The ruling's parenthesis ("the repository is private until 1.0") predates the public export of every tag (slice-1 tickets 56, 57, 79); ticked on what it forbids, and the close-out records the parenthesis on greenfield [33].
  - [x] (4) No demo-only build — none; the note's title and coverage line state the tag and the roadmap.
  - [x] (5) No matter documents — General's preset toggles (ticket 07) and the base model withheld (ticket 08), `.scratch/slice-1/assets/07-on-box.txt` and `08-on-box.txt`.
  - [x] (6) No matter knowledge bases — `workspace.knowledge` off in the permission artifact until Ingestion GA (`gideon/host/render/owui.py`, `compose/open-webui/permissions.yaml`).
- **The user-facing tag check.** The note is written; the three human items (the supervisor a working day ahead, the announced window, the note sent by the CSAs) bind from go-live and do not apply to this tag (ADR-0044). The supervisor was informed on 2026-09-17.
- **The human read** (ADR-0036, ADR-0044): the maintainer read the diff since `v0.1.0` of the user-text paths — `compose/open-webui/` (the arithmetic guardrail and citation stamp Filters, General's preset, the permission set), `gideon/host/render/owui.py`, the seeds under `eval/seed/` (14 files, +5,987/−50), and the new release note — on 2026-09-18 before the commit, and found nothing.
- **Window**: none — the box has no users (ADR-0044). The `v0.2.0` tree's render differs from the applied record by `manifest.yaml`'s release alone (plus, read from the worktree, the four units' `WorkingDirectory`); the expected recreate row is empty. Riders deferred by ruling (4) of 2026-09-17: pull requests #3, #9, #10, and #11 to the first patch tag after `v0.2.0`, #4 to the first engine window; slice-1 ticket 63 to go-live.
- **The proofs** follow this commit in the evidence commit, in the plan's order: the acceptance run and the clean-VM full restore against the local tag, then the box's `upgrade 0.1.82 → 0.2.0` from the primary before the merge (discovery ruling 1), with `engine-verify` in its sequence.
- **Tutorial**: `docs/5-tuto/tuto_0.2.0.md` — a rule keyed to an event rather than a version, and why the box's upgrade runs before the merge.
