# Changelog - Week 2, 08-09-2026, V. 0.1.26

**Release Date**: Week 2, 08-09-2026 at 15:11
**Version**: 0.1.26 (previously 0.1.25)
**Object**: pin bump names its research notes: every note under docs/research/ opens with YAML front matter naming the pins it was verified against and the version of each — the author's claim, never compared to a lock (nine name the frontend at v0.11.3, two the engine at v0.27.1, one the runner, one SearXNG's pinned tag, the observability note eight image pins at the branch tips it read, the browser note Playwright, the engine-verify note the generator's revision) — parsed by the new tools/pinwatch/notes.py through the Host seam with a duplicate-key-refusing loader; the vocabulary derived from the records (the registry's ids, models.<role> from models.lock's reference profile, dev.<package> from requirements-dev.txt), the last two recorded and not watched; every bump pull request's body ending with the notes verified against its pin — verified and proposed versions beside each, the proposed one from the bump's version-bearing key path or "unchanged (the digest alone moves)", a none line for a pin without notes — a note that cannot be read or names an unknown pin one refusal before any row and before the pull-request listing; python3 -m tools.pinwatch.notes listing the join per pin with no network; tests/test_research_notes.py the tripwire; the researcher agent's convention and the amendment rule (a re-verified note is a docs commit on main, never a commit on a watch branch, which the next run force-pushes — the state table protects the skill-record branches alone); proven from the cycle's worktree by the listing and an ordinary dry run (workflow ticket 05, resolved; notes on slice-1 ticket 41 and slice-0 ticket 14; the delegated-batch block's argument shape corrected)
**Code review**: `docs/3-code-review/CR_w2_v0.1.26.md` (Codex loop, 1 round -> APPROVED)

## Changes

- **The record** (`docs/research/*.md`, thirteen notes): each opens with YAML front matter — `verified_against`, a list of `pin`/`version` items — naming the pins the note was verified against and the version of each, copied from the note's own source-of-truth text and never from a lock: nine notes name `images.open-webui` at `v0.11.3` (the SearXNG note among them), two `images.vllm-openai` at `v0.27.1`, the runner note `host.gh_runner`, the SearXNG note `images.searxng` at its pinned tag, the browser-surface note `dev.playwright` beside the frontend, the engine-verify note `models.generator` at the generator's revision beside the engine, and the observability note eight image pins at what its findings table read — the `main`/`master` tips and the `latest` docs tree on 2026-09-03, never the current releases it reports separately. Two frontend notes that mention the engine's version as context name the frontend alone.
- **The module** (`tools/pinwatch/notes.py`, new): the binding type, a front-matter parser over a `SafeLoader` subclass that refuses a duplicated key (the host loaders' pattern), the reader over `docs/research/` through the `Host` seam at the run's root (the working tree, so a cycle proves its record before its push; the four records stay read from `origin/main`), the vocabulary derived at every run from the loaded records — the registry's ids as `watched`, `models.<role>` over `models.lock`'s reference profile and `dev.<package>` over `requirements-dev.txt`'s exact pins as recorded, not watched — the validation naming the note, the pin, and the three sources on a miss, the join per pin, and `python3 -m tools.pinwatch.notes [--root]`: one row per id in registry order then the models and dev ids, `<id>: <n> — <note> (<version>), …` or `<id>: none`, a recorded id suffixed `(recorded, not watched)`, exit 0 iff every note parsed and named known ids.
- **The body** (`tools/pinwatch/pr.py`): `body_for` takes the bound notes as a required parameter and ends every body with `### Research notes verified against this pin` — one sentence (read them against the proposed version before the merge; an amendment is a `docs:` commit on `main`, never a commit on this branch, which the watch owns and force-pushes) and a table of note, verified version, and proposed version, or one none line. `proposed_version` derives the token from the first version-bearing key path (`.source`/`.base` tags, `registry_image`'s tag, the value itself for a version, floor, branch, ref, commit, argument, or URL) and reads `unchanged (the digest alone moves)` when the token did not change or no change carries one.
- **The run** (`tools/pinwatch/cli.py`): after the registry and before `--only` narrows it, the working-tree `models.lock` and `requirements-dev.txt` are read, the vocabulary built over the full registry, the bindings read and validated; a `NotesError` is one stderr refusal and exit 1 before any row and — the pull-request listing now follows the validation — before any GitHub call. `_process_pin` passes each pin's join to the three `body_for` sites. Rows and `--dry-run` are byte-for-byte unchanged.
- **Tests**: `tests/test_research_notes.py` (new tripwire) — every committed note parses and names ids in the vocabulary derived from the committed records, a count floor, the negatives over fictitious text (no block, never closed, empty list, missing version, unknown item key, wrong top-level key, a duplicated key at either level, an unknown pin whose fix names the three sources); `tests/test_pinwatch.py` — the section with a bound note and with none, `proposed_version` over six key-path kinds, the join's order, the CLI's refusal before any row with no `gh` or `git push` call, the section through `gh pr create`'s stdin and a fake bound note in it, the listing's rows and order over the fake host and its refusal; `PinWatchHost` serves a directory's names and seeds the two working-tree records; no test states a lock value. Gate: `lint: clean | typecheck: clean | tests: 105 passed (14 new)` over the affected modules.
- **The convention** (`.claude/agents/researcher.md`): a new note opens with the front-matter block, the ids being what the listing shows; an amendment after a bump's re-verification reaches `main` by its own `research/<name>` branch or a `docs:` commit, never a `pin-watch/*` branch — because `branch_state` protects a person's commits on the two skill-record branches alone and judges a built-pin branch by its recorded digests, so any other commit on a watch branch is force-pushed over at the next run (ADR-0033's deliberate choice for built pins, never contemplated for a plain image bump). The Monday runbook's §5 (`.scratch/slice-0/assets/10-pin-watch-app-setup.md`) says the same and narrows its "unless a person has committed to its branch" to the three cases the code implements.
- **Proven** from the cycle's worktree on the dev seat (`.scratch/workflow/assets/05-notes-listing.txt`): the listing with nine notes for `images.open-webui`, two for `images.vllm-openai`, one each for `images.searxng`, `host.gh_runner`, and the eight observability pins, `models.generator` and `dev.playwright` recorded and not watched, `none` elsewhere; then `python3 -m tools.pinwatch --dry-run` with its rows unchanged in shape (pull requests #3 and #4 would update, a SearXNG bump would open, #5 would update, the driver proposal declined), exit 0. No on-box step: the pin watch runs on the hosted runner and nothing is applied for this tag.
- **Documentation**: the tools leaf (the Inventory row, the pin-watch bullet with the state table's three cases and the notes section, the listing command), the tests leaf (the tripwire), the map (§2's row, §4's `tools/pinwatch` and `docs/research` lines, §8's row for the listing, §11's pin bullet, §12's tag), `TESTING.md`'s contract list, the glossary's **Pin watch** entry (via `/domain-modeling`), `README.md`'s status paragraph; a dated note on slice-1 ticket 41 (the pointer to the body's list, and the branch fact its ruling must respect) and on slice-0 ticket 14 (`models.generator` as the `ModelPin`'s id shape). A lesson under CLAUDE.md's Lessons rule: the `testing-gate-delegated` block's here-document shape is refused by the harness's worktree guard, so the block and its `docs/agents/tooling.md` §4 row now name the shape that passes — one single-quoted literal argument without apostrophes.
- **Tutorial**: `docs/5-tuto/tuto_0.1.26.md`. **Release note**: a pre-General 0.x tag, skipped per spec §21. **Box ledger**: the §11 fetch found the other repository's copy one entry ahead of ours — Transcribe at v1.36.0 with one request for the shared engine's connection facts — merged; GIDEON's entry changes nothing on the server and answers the request by number as a status, the hand-over being the operator's under open item 1 and put on the tracker at this release's outstanding-items step.
- **Architecture memory counts** (the count script): the map ~11,954 tokens, 46 under its 12k warning; the leaves 1,020–5,639; the glossary ~12,729, reported and never budgeted.
- **Version**: bumped 0.1.25 → 0.1.26 (with `tests/test_version.py`); a patch, nothing under `gideon/`, `compose/`, or `config/` changes and no site-file contract moves; workflow ticket 05 resolved.
