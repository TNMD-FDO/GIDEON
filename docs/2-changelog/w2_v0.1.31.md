# Changelog - Week 2, 08-09-2026, V. 0.1.31

**Release Date**: Week 2, 08-09-2026 at 18:00
**Version**: 0.1.31 (previously 0.1.30)
**Object**: office values out of the tree, and the tripwire that keeps them out: every value that locates or names something of the office is replaced — in the exported tree (the example site file and its spec twin, now byte-equal; twelve site fixtures; the three regenerated render fixture trees; seventeen test modules; the eval seed's example person; eight documents under `docs/`) by documentation values in two reserved families, the example office's RFC 2606 names and RFC 5737 network and the tests' synthetic `.test` family; in the tracker's evidence (43 files: the on-box transcripts, the tickets' comments, the reinstall record, the bootstrap brief and wizard, the greenfield spec, map, and issues, the office-services runbook) by the site-key placeholder vocabulary ticket 55's redaction will emit, `<redacted>` where no key names the value — and `tests/test_office_values.py` holds the whole tree to that rule by an allowlist that names no office value, its public-suffix set derived from the allowlist itself, every finding with path, line, and fix (slice-1 ticket 54, resolved; folded at the claim)
**Code review**: `docs/3-code-review/CR_w2_v0.1.31.md` (Codex loop, 2 rounds -> APPROVED)

## Changes

- **Two vocabularies, one rule** (ticket 54's ruling (c): everything that locates or names a machine, a network, a directory, a mailbox, or a colleague goes; the office's public identity and the CSAs' names stay). The exported tree takes the example office's family — `gideon.example.org`, `example.org` deriving `DC=example,DC=org`, `nas.example.org`, `smtp.example.org`, mailboxes at `example.org`, `192.0.2.0/24` — where a test stands in for the example office, and the `.test` family `tests/test_restore.py` already used (`gideon.test`, `ad.test` deriving `DC=ad,DC=test`, `nas.test`, `smtp.test`) where a test builds its own synthetic site; the second office keeps `exd.example.internal` with `198.51.100.0/24` and `203.0.113.0/24`; every invented range is RFC 5737's. The tracker's evidence reads `<hostname>`, `<auth.ldap.host>`, `<auth.ldap.search_base>`, `<lan_cidrs[0]>`, `<address in lan_cidrs[0]>`, `<backup.target.host>`, `<alerts.smtp.host>`, `<alerts.smtp.from>`, `<alerts.recipients[i]>`, a UPN `account@<auth.ldap.host>`, and `<redacted>` for the iLO address, the colleague's username and workstation, and a CA name; prose that inventoried the values (the two tickets, the spec's two sentences, the greenfield map and issues) describes them instead, and ticket 22's recompute command, whose regex spelled the domains, left the tree — the tripwire is the standing inventory.
- **Classes the inventory's regex never covered**, scrubbed under the same ruling: the mailboxes at the office's bare mail domain (sixteen files), a certificate issuer's CA name embedding a utility server's name, the two domain controllers' names in the greenfield map and issues, and the box's static address; the discovery notes on the ticket record each.
- **`config/site.example.yaml` and its twin** (`.scratch/greenfield-spec/assets/23-site-example.yaml`): the config copy is the source, its scrubbed text written over the twin, and the tripwire holds them byte-equal whenever the twin exists — vacuous in ticket 56's export tree.
- **The tripwire** (`tests/test_office_values.py`, a sibling of the evidence hygiene test): an `rglob` from the repository root skipping `.git`, `.venv`, the caches, other cycles' worktrees, and exactly the skill folders `skills-lock.json` names (upstream's text, refreshed whole), never following a symbolic link. Every dotted token is judged on its final label in three tiers: under a public suffix its registrable domain must be on the allowlist — the egress allowlist's hosts through the product's loader plus 35 curated documentation hosts, the public-suffix set derived from those entries so no IANA list is carried and no file extension ever reads as a domain; under a private-use suffix of the kind directories use it is a finding unless a reserved documentation name covers it; in a hostname position (a URL authority, a mail domain, an `ssh` target, a `DC=` chain, a hostname-kind site key in a YAML file) it is judged whatever its suffix. Every RFC 1918 literal is a finding with three scoped exemptions — libvirt's bridge range (imported from `gideon.host.images`), a Docker pool address on a line that also carries a quoted HTTP request line, and the three range definitions in Markdown prose outside a fenced block and off a `lan_cidrs` line; a `DC=` chain of two or more components must be a documentation domain. Python sources are read through their `tokenize` stream — strings, comments, and the f-string and t-string middle parts (the review's one Major finding) — so attribute chains never match; the placeholder grammar (`<key>`, `<key[i]>`, `<address in lan_cidrs[i]>`, `<redacted>`) is cut out before scanning and nothing else in angle brackets is. A file-count floor of 400 holds the walk; a seeded temporary tree proves every rule and every exemption, the module's own office-shaped seeds and RFC 1918 constants assembled from parts so the module passes its own walk.
- **Reached along the way**: `tests/test_weights.py`'s invented hosts move from a made-up suffix to `.invalid`, the family the module already used, because a made-up suffix fails the hostname-position tier; the searxng research note's quoted docstring example and the observability note's CDI vendor example move under `example.com`, marked as replaced; the allowlist's first run found 208 candidates, 165 after the tokenizer rules, 35 curated entries after the invented names were removed.
- **The record**: ticket 22's inventory command as it stood at `d7587c3`, the bare mail domain added, run from the worktree — at the claim 365 occurrences in 98 files; after Phase 1 only under `.scratch/`; after Phase 2 eight lines in the two inventory tickets alone; after their scrub nothing but documentation values and the two range definitions in Markdown prose. Recorded on the ticket's close-out.
- **The documents**: ARCHI §16's Evidence bullet rewritten within its length to carry the rule and name the tripwire; `CONTRIBUTING.md`'s one bullet; the tests leaf's tripwire line; `TESTING.md`'s contract line; `CONTEXT.md`'s **Office value** entry through `/domain-modeling` (documentation value and site-key placeholder defined inside it); the map's §12 slice row moved to this tag with tickets 54 and 64 named.
- **Architecture memory** (TRIP-3 Step 7): the tests leaf's tripwire line; the map's §16 Evidence bullet rewritten within its length and its §12 slice row moved. The counts after the edits: the map ~12,062 tokens — measured at ~11,993 before the rebase, 7 under its 12k warning; the rebase onto `main` brought `v0.1.30`'s §17 sentence and the §12 row gained two ticket numbers, so the map stands 62 past the warning and under its 14k cap, the budget test green — the render-apply leaf ~6,211 (past its 6k warning since `v0.1.30`), the other leaves under 3.4k, and the glossary ~13,083, reported and never budgeted. The map's compaction, which `docs/ARCHI-rules.md` places in the release that crossed the warning, is deferred by the maintainer's decision to a session of its own beside the leaf's, recorded at the outstanding-items step.
- **The ticket, folded at the claim** (18,305 → 20,756 bytes, every one of its 93 backticked tokens kept), its claim note carrying the three facts the triage lacked and the discovery rulings; dated notes at the release on tickets 55 (the placeholder vocabulary and the hostname-kind key list as landed), 56 (the runbook line a documentation value; the skip list; the twin test vacuous in an export), and 22 (its text scrubbed, its command gone).
- **Tests** (3 new in `tests/test_office_values.py`; 1,210 passing after the rebase): the real tree clean, the file-count floor, and the seeded tree — the private suffix, the unlisted public domain, the URL authority, the autolink, the address, the DN, the ssh target, the bare Docker address, the ported one on an ordinary line, the fenced range, the YAML site value, the plain string, the comment, and the f-string reported; the reserved names, the placeholder, the single label, the libvirt address, the access-log address, the prose definitions, the attribute chain, and the allowlisted host silent; the twins' difference reported.
- **Nothing on the box**: no change under `gideon/`, `tools/`, or `compose/`; nothing applied, no service recreated; the box's own site file is not the example file. A patch; no release note (pre-`v0.2.0`), no acceptance run.
- **Version**: bumped 0.1.30 → 0.1.31 (with `tests/test_version.py`); the plan keeps its `F_0.1.28` name, `v0.1.28` through `v0.1.30` having landed from the primary during the cycle and the branch rebased onto `main` before this version was chosen — the rebase's conflicts were the example manifest (regenerated from the rebased code), `TESTING.md`'s contract list, and `CONTEXT.md`'s two new entries, each kept.
