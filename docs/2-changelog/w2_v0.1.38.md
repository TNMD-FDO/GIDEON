# Changelog - Week 2, 09-09-2026, V. 0.1.38

**Release Date**: Week 2, 09-09-2026 at 10:24
**Version**: 0.1.38 (previously 0.1.37)
**Object**: General's search pages reach the model: General's record renders `file_context: true` beside `web_search: true` (§15, [06] items 7 and 12; slice-1 ticket 65) — on the pinned frontend the files handler, the one step that turns a request's files items into the citation template's context, runs only under the record's `file_context`, and the non-tool search's loaded pages are a files item, so the flag rendered off since `v0.1.9` dropped every fetched page after the "Searched N sites" status and ticket 15's proof, judged on that line, passed; the base record unchanged, `file_upload` and `builtin_tools` off keeping attachments and the file tools out; `ENABLE_RETRIEVAL_QUERY_GENERATION=false` in the connected task-toggle block, since the opened handler asks the task model for retrieval queries the bypass never reads and the same-request cache that would have answered it went off with ticket 16 (the claim's ruling, the frontend's Compose block moving); the sentinel contract's derived environment carrying the key; the two connected sites' fixtures regenerated; the proof driver `65-search-turn.py` proving the outcome on the record it changes — the stored assistant message's sources, the rendered citation elements, and the engine's prompt tokens attributed to the turn through Prometheus (idle before, the request delta held to the turn's own count), the chat deleted in a `finally` whose verdict reaches the exit status — with `65-engine-log.sh` for the engine's throughput lines; proven on the box from the cycle's worktree after a rebase onto `v0.1.37`, a deliberate daytime run with no users: before the apply one searched browser turn as the test account stored 0 sources and 0 citation elements over 1,394 prompt tokens in four requests at tens of tokens a second; `render --diff` naming the frontend alone by its changed Compose block; the apply recreating it with the record synced and read back; after it the same prompt stored one source carrying 92,192 characters of page text over seven hosts and three citation elements, 16,459 prompt tokens over the same four requests at 1,506 tokens a second in the prefill window, the dialog once, the files handler's own two status events beside the search's three; the control with search off storing nothing over three requests; zero loader failure lines in the frontend's journal since the apply; the three research notes corrected in place; `docs/agents/on-box-proofs.md` §9 — a criterion about an outcome is proven on the record the outcome changes, never a status line, the engine's figures attributed by request count — with the preamble's ninth trigger, TRIP-1-plan's `ticket-driven-plan` block's not-traced-further sentence and TRIP-2-implement's `on-box-proofs` block's clause with their tooling rows; the loader's fetch-failure URL lines ruled the second accepted residual beside ticket 16's traceback; spec §15's General paragraph edited in one consolidated pass by the maintainer's ruling at Documentation Sync (File Context on with the mechanism and the retrieval task off, with tickets 29, 28, and 15's deferred wordings) with §4.2's `chat.web_upload` and the Appendix E entry, [06] item 12's line and the spec-bug comment; the leaf, the map, the glossary's General entry, the runbook, the README; the tracker notes on tickets 39, 41, and 62 (slice-1 ticket 65, resolved)
**Code review**: `docs/3-code-review/CR_w2_v0.1.38.md` (Codex loop, 2 rounds -> APPROVED)

## Changes

- **The record** (`gideon/host/render/owui.py`): `GENERAL_CAPABILITIES` gains `file_context: True` beside `web_search: True`, its comment naming why — the pinned middleware runs its files handler only under the record's `file_context`, the search handler's loaded pages are files, and that handler is the only step that applies the citation template to them — with `file_upload: false` keeping attachments out and `builtin_tools: false` the file tools; `BASE_MODEL_CAPABILITIES` unchanged, so the hidden base model reachable by `?model=` gets no pages.
- **The toggle** (`owui_environment`'s connected block): `ENABLE_RETRIEVAL_QUERY_GENERATION=false` after search-query generation. The files handler asks the task model for retrieval queries unless every files item is marked full-context; the search's bypass item marks nothing; the same-request cache (`ENABLE_QUERIES_CACHE`) is off since `v0.1.35` because a hit logs the queries at INFO; under the bypass the queries are never read. Disabled, the task router raises the feature-disabled exception inside the handler's own `try`, which passes, and the pages still go in whole — no extra engine call per searched turn. An upstream default pinned by name (ADR-0017), a connected-host fact, slice 4's Research rendering its own value. The map lives in the frontend's Compose block, so the block's digest moved and the apply recreated the frontend by `changed compose block`.
- **The contract and the fixtures**: `tests/contract/search_sentinel.py`'s `FRONTEND_ENV_KEYS` and `tests/contract/sentinel/compose.yaml` carry the key, so the CI stack's searched turn runs the files handler as the box does; the two connected sites' `compose.yaml`, root `manifest.yaml`, and `open-webui/manifest.yaml` regenerated (six files), the no-GPU site untouched.
- **Tests** (`tests/test_render_owui.py`): the differing-names list reads file context and web search; General's `file_upload` and `builtin_tools` held false; the task-toggle mapping and the connected-minus-disconnected key set carry the toggle; the no-GPU host's environment and the drill's carry none of it. No new module.
- **The proof driver** (`.scratch/slice-1/assets/65-search-turn.py`, `65-engine-log.sh`): the site's hostname from the site loader, a fixed invented today-dependent prompt, `--no-search` for the control, the reminder dialog counted across the flip and the send, every stored status action counted, per stored message the source count, each source's document characters, and its distinct-host count — never a host name or a URL — and the engine attributed through Prometheus on loopback: idle required before the browser launches (no request running or waiting, both counters unchanged across a scrape interval), after the turn a bounded wait until the request delta reaches the turn's own count (four searched, three control), a delta that differs either way repeating the run; the chat deleted in a `finally` and the account's listing read for leftovers, a failed deletion or a leftover making the exit status 5 (the code review's one Major). The script prints the engine's throughput lines from its journal since a timestamp.
- **The proof** (`.scratch/slice-1/assets/65-on-box.txt`): after a rebase onto ticket 19's release, whose worktree apply had set the box's applied record (on-box-proofs §7), a deliberate daytime run with no users before `v0.2.0` — the before-run (0 sources, 0 citation elements, 1,394 prompt tokens over four attributed requests, throughput 30–70 tokens/s), `render --diff` (the compose block's one line, General's record, the four units' working directory; the frontend alone), the apply (recreated open-webui by its changed compose block, the manifest synced and read back, verify green), the after-run (one source of 92,192 characters over seven hosts, three citation elements, 16,459 prompt tokens over four requests, 1,506 tokens/s in the prefill window, the dialog once, `queries_generated` with an empty list and `sources_retrieved` beside the search's three events, the answer 2,226 characters in 168 s), the control (0 sources, three requests, 1,044 tokens, no dialog), and the frontend's journal since the apply (929 lines, no loader failure shape, one frontend startup warning). Two facts recorded, never printed: the on-screen "Searched N sites" line leaves the message's text once sources render, and the citation UI's favicon images call a third-party service with each source's link, three requests the launcher's allowlist aborted.
- **The research notes**, corrected in place and dated: `docs/research/owui-model-record.md` §2.2's `file_context` row (the handler carries the non-tool search's pages, the retrieval query call, the cache) and §2.3; `owui-preset-system-prompt.md` §1.3's hedge replaced by the fact (`RAG_SYSTEM_CONTEXT` off by default, the injection in the user message); `searxng-service-and-owui-search.md` §9's "not traced further" replaced by the traced path from the files list through the gate, the docs branch, and the template to the last user message, no trim, the engine's window the bound.
- **The lessons**: `docs/agents/on-box-proofs.md` §9 (a criterion about an outcome is proven on the record the outcome changes, never a status line; a sync's read-back proves the sync; the engine's figures attributed by request count) and the preamble's ninth trigger, beside ticket 19's §8 met at the rebase; TRIP-1-plan's `ticket-driven-plan` block gains one sentence (a research note's "not traced further" at a boundary the design crosses is a Conflict the plan traces or names with the proof that would expose it) and TRIP-2-implement's `on-box-proofs` block one clause, both under `docs/agents/tooling.md` §4's ceiling with their rows updated.
- **The spec record**: the [06] comment in ticket 29's pattern (item 7's File Context off is the gate of item 12's design; both toggles `exempt: policy toggles`; the loader's URL lines an accepted residual; the ruling), item 12's line gaining the mechanism, and — by the maintainer's ruling at Documentation Sync — §15's General paragraph edited in one consolidated pass (File Context on with the mechanism, the retrieval task off, and the wordings tickets 29, 28, and 15 had deferred: autocomplete generation, `terminal`, `function_calling: legacy`, the evaluation arena, Attach Webpage off by `chat.web_upload`, "≤ 9 pages/turn" an expectation), §4.2's set gaining `chat.web_upload` off, Appendix E its entry.
- **The documents**: the render-apply leaf (the model-records row, the search-rig row's two residuals, the tests bullet), the tests leaf (the sentinel's derived key list), the map (§12's row through `v0.1.38` with 65, §16's two residuals), the glossary's General entry through `/domain-modeling`, the observability runbook's second residual, the README, the tutorial `docs/5-tuto/tuto_0.1.38.md`; the box ledger fetched (`gh api`, the other copy two entries behind ours, no line addressed to GIDEON) and nothing merged or entered, since nothing shared moved.
- **The tracker**: ticket 65 resolved; dated notes on tickets 39 (the searched case attainable, two engine calls on the API path, the stored message the measure), 41 (the searched row's measure, the favicon selector a bump may move), and 62 (the searched turn's baseline figures); the web-sources tightening and the favicon egress for the outstanding-items step.
- **Version**: bumped 0.1.37 → 0.1.38 (with `tests/test_version.py`); a patch. Pre-`v0.2.0`, so no release note.
