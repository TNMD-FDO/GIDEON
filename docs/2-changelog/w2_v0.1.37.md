# Changelog - Week 2, 09-09-2026, V. 0.1.37

**Release Date**: Week 2, 09-09-2026 at 09:44
**Version**: 0.1.37 (previously 0.1.36)
**Object**: every GIDEON service carries a Compose memory limit from the hardware profile's memory table (spec §7.6, slice-1 ticket 19): `models.lock`'s reference profile gains a `memory` mapping, one row per Compose service the stack knows on any host, keyed by the service's name (`SERVICE_NAME`'s hyphenated grammar, so the pin watch's patcher reaches every scalar), each a whole number of decimal gigabytes and, on the generator's row, `role: generator` (the subject of slice-0 ticket 14's size-change rule) — twelve rows summing to 73 GB, §7.6's figures where it names a service (Postgres 20, the frontend 4, the generator 32, Caddy and SearXNG 1 each as the halves of its ~2 GB group) and the smallest whole gigabyte at least four times the 2026-09-07 fourteen-day working-set peak where it does not (cAdvisor 5, Grafana 3, Prometheus and the DCGM exporter 2, the three other exporters 1), E9's starting values with their basis and query in the lock's comment; the loader validates the table collect-all like `requires` (a missing or empty table, a row name outside the grammar, a non-positive `gb`, an unknown row key, a role the profile does not pin, each ending in a fix naming this plan since the lock's first plan deferred the table); render gives every service it emits `mem_limit` as an exact byte count (`memory_limit_bytes`, the row times `GIGABYTE`, a bare integer as the block's last key, applied in one place after the marker and the site have settled which services this host runs — `mem_limit` alone, never a suffix, since Compose reads a `g` as binary), refusing a rendered service without a row naming the row to add and a row naming a service the stack never renders naming the valid names, the drill's Postgres and frontend taking their production namesakes' rows through the same helper; the Overview board's "Container memory" panel draws each container's cAdvisor limit (`container_spec_memory_limit_bytes`, filtered to a positive value because cAdvisor reports 0 for an unlimited container) as a dashed series beside its working set; the three render fixtures regenerated (nine files); proven on the box from the cycle's worktree in daytime with the engine's §8 announcement ruled moot as at `v0.1.34` — `render --diff` naming every service, the apply in 4 m 32 s recreating all twelve by their changed blocks with the engine healthy 187 s after its start, `docker inspect` reading each container's `Memory` equal to its row and `MemorySwap` twice it, Compose's config hashes equal on twelve of twelve, the engine stopped and started once more with its weights' 81 files evicted per file by `posix_fadvise` (30.9 GB resident to 0) and healthy in 60 s against `v0.1.6`'s 133 s with the same container, no restart, and no OOM kill while its cgroup reached the 32 GB limit and reclaimed clean file pages 566 times (4.96 GB anonymous, 25.8 GB file after the load; weights loaded in 7 s), `engine verify` green with its kept event, Prometheus showing every limit equal to its row floored to whole 4 KiB pages (the kernel keeps a cgroup limit in pages, so `memory.max` and cAdvisor read up to 4,095 bytes under a row that is not page-aligned while `docker inspect` reports the request — a fact recorded in the lock's comment and on the ticket), and one `backup drill` whose two containers carried their namesakes' limits in cAdvisor's series; the ledger's §3 host-RAM row written by agreement (GIDEON 73 GB today, about 229 GB from `v0.4.0`; Transcribe 40 GB with its engine's 16 GB on loan while on), the rules version moved, the entry answering Transcribe's line 10; the two open image bump pull requests deferred by comment (#3 to ticket 41's cycle, #4 past `v0.2.0`); the host, render-apply, and stack leaves, the map, the glossary's *Memory table* entry, slice-0 ticket 14's comment (slice-1 ticket 19, resolved).
**Code review**: `docs/3-code-review/CR_w2_v0.1.37.md` (Codex loop, 2 rounds -> APPROVED with observations)

## Changes

- **The lock** (`models.lock`): the `memory` mapping between `requires` and `models`, twelve rows in render's service order, the table's comment carrying the unit, the key, the basis of every row and the query the measure is re-taken with, the page-floor fact, the role's meaning, and the value-only patch rule; the deferral comment now names the chunk guards alone as ticket 58's.
- **The loader** (`gideon/host/models.py`): `MemoryRow(service, gb, role)`, `HardwareProfile.memory` and `memory_row()`, the exported `SERVICE_NAME` grammar, `_MEMORY_ROW_KEYS`, `_MEMORY_FIX`, `_validate_memory` called after the profile's `models` mapping is read so a row's role is held to the pinned roles, the construct; the fix keyword threaded through the loader's error helpers.
- **Render** (`gideon/host/render/compose.py`, `drill.py`): `memory_limit_bytes(profile, service)`; the unknown-row check against the common services plus the engine's and SearXNG's names, before the engine's own refusals so a no-GPU host names a bad table too; the one `mem_limit` loop after the marker's deletion; the drill's two limits.
- **The board** (`compose/grafana/dashboards/overview.json`): the "Container memory" panel's target B and its `byRegexp` override on the ` limit` suffix setting `custom.lineStyle` to `{"fill": "dash", "dash": [10, 10]}` (Grafana's object shape; a bare string draws solid).
- **Fixtures** (`tests/fixtures/render/*`): regenerated by the script; `compose.yaml` (twelve, eleven, and ten `mem_limit` lines), `grafana/dashboards/overview.json`, and `manifest.yaml` (the file digests, every block digest, `models_lock_sha256`; the top-level digest unchanged) per set.
- **Tests** (8 new): the committed lock's twelve rows exactly and the generator's role, the seven refusals with the memory fix; every rendered service's limit derived from the loaded profile on both hosts as the block's last key, the table's names equal to the rendered union, the missing-row and unknown-row refusals; the drill's two limits and its refusal; the panel's two targets and its override; the hand-written lock texts in `test_weights.py` and `test_engine_verify.py` gain a fictitious table.
- **The proof** (`.scratch/slice-1/assets/19-on-box.txt`): as the Object says, nine sections.
- **Documents**: the host leaf (the lock's inventory row and its `models.lock` line), the render-apply leaf (an inventory row, the `render` bullet's limits sentence, the tests line), the stack leaf (the board's limit series), the map (§2's host row, §12's slice-1 row); `CONTEXT.md`'s *Memory table* entry through `/domain-modeling`; the ledger's §3 row, version, and §12 entry; slice-0 ticket 14's dated comment; the ticket's close-out with two criterion corrections (the drill document is written at drill time, never rendered; cAdvisor's series equals the page-floored row).

## Breaking

None. No site-file key or `/etc/gideon` contract moves; a lock without the table refuses, as one without `requires` does, and the only profile is the reference one.

## Window

Before `v0.2.0`, free: the proof's apply recreated every service once (the engine about three minutes, the frontend about two), the announcement ruled moot; the release's apply from the primary recreates nothing. Riders: pin-watch PR #3 deferred to ticket 41's cycle, PR #4 past `v0.2.0` (workflow ticket 07); no open ticket rode.
