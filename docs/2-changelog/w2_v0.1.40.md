# Changelog - Week 2, 09-09-2026, V. 0.1.40

**Release Date**: Week 2, 09-09-2026 at 12:46
**Version**: 0.1.40 (previously 0.1.39)
**Object**: `gideon secrets rotate <name>` rotates one generated secret and recreates exactly its consumers (§1.7, §4.4; slice-1 ticket 24): the registry gains a rotation class per entry — `rewrite` for the engine's key, the session key, and SearXNG's signing key (the file the value's only home; a fresh value written to the same path as `root:gideon` 0440), `remint` for the two minted API keys (the file removed and re-minted by the apply-manifest stage), and `role`, `account`, and `seeded` for the eight whose second home the file does not own, refused before any change with the second home and the by-hand route — and a supplied registry naming each of the four supplied secrets' replacement path; a pure consumer map over the render structure (`render/consumers.py`: each Compose block's `secrets:` list for the mounts, each secret-flagged artifact's `secret_names` declaration, held equal to what its emitter reads, for the carriers), so no second mapping drifts from render; the command on the host path behind the command boundary — pre-run refusals on stderr, then `preconditions` (a verified apply recorded and the recreate judgment strictly empty, a pending change refusing naming `render --diff` then `apply`), `plan` (each mount consumer with its unavailability, the engine's by `ENGINE_READY_SECONDS`; a rewrite secret nothing on the host consumes one row and exit 0), `rotate`, `recreate` (each mount consumer force-recreated once), then apply's own stages from `secrets` through `record`, factored out of `run_apply` into `converge`, so a carrier recreates through the recreate rule, a removed key is re-minted, and the record is written; the value in no row, refusal, argv, environment, or written file but the secret's and the env file that carries it; a failure past the write finished by `apply`, then a fresh rotation where a mount consumer was not reached; 19 new tests over a real fake stack; proven on the box from the cycle's worktree in daytime after waiting for ticket 43's release and rebasing onto it — the engine's key rotated in 3 m 17 s with the engine healthy 123 s after its start and exactly the engine and the frontend recreated, the old key refused 401 and the new answered 200 from the frontend's container, one managed turn answered, no value in any journal line or process command line; the session key with the frontend once and a pre-rotation session token refused after; the admin key re-minted with nothing recreated, the old refused through the ingress and `users reconcile --now` green with the new; SearXNG's key with SearXNG once and its probe green; then `render --diff` empty and one `apply` recreating nothing; the four refusals with their fixes; the install runbook's rotation step and re-run rule, the backup runbook's restore sentence, the map, the render-apply leaf, `docs/agents/on-box-proofs.md` §7, the glossary, the README, the ledger's entry, the §20.2 spec-bug comment (slice-1 ticket 24, resolved)
**Code review**: `docs/3-code-review/CR_w2_v0.1.40.md` (Codex loop, 3 rounds -> APPROVED)

## Changes

- **The registry** (`gideon/host/secrets.py`): `RotationClass` and a `rotation` field on every `GeneratedSecret` — `rewrite` (`engine_api_key`, `webui_secret_key`, `searxng_secret_key`), `remint` (`gideon_admin_api_key`, `gideon_eval_api_key`), `role` (the five Postgres passwords), `account` (the two frontend break-glass and eval passwords), `seeded` (Grafana's admin password); `ROTATABLE_NAMES` derived; `registry_entry`; `SuppliedSecret` and `SUPPLIED_REGISTRY` with each supplied secret's replacement path as a runnable fix (Grafana's by-hand force-recreate named where Grafana mounts the secret and the recreate rule cannot see it); `rotate_generated` writing a fresh `token_urlsafe(32)` value through the seam's same-directory rename with a result carrying `written`, so a `chown` failure after the rename is reported as written but unowned with the `chown`, `apply`, then rotate-again fix.
- **The consumer map** (`gideon/host/render/consumers.py`, `render/__init__.py`, `render/owui.py`, `render/searxng.py`): `Artifact.secret_names(inputs)` (default empty), `owui_secret_names` the one declaration the frontend's env emitter and its artifact share (the engine's key under the marker's absence, the proxy credential only under a configured proxy — the code review's finding), the SearXNG env artifact's declaration likewise; `secret_consumers(inputs)` over `service_blocks` and the secret-flagged applying artifacts, `consumers_of` for one name. The fixtures are byte-identical.
- **Apply's converge** (`gideon/host/apply.py`, `render/command.py`): the stages from `secrets` through `record` factored into `converge`, every argument required, `run_apply` keeping its defaults and calling it; `preconditions` and `force_recreate_services` public; the inputs loader public as `load_render_inputs`. Every existing apply, install, upgrade, restore, and drill test green unchanged.
- **The command** (`gideon/host/rotate.py`, `gideon/host/cli.py`, `gideon/cli.py`, `tests/test_cli_surface.py`): the `secrets` group with `rotate <name>` behind `_guarded`; the pre-run refusals content-free (a role or login from the release's constants, never a value); the four stages and the converge as the leaf records; the positional's help naming the rotatable set; `TOP_LEVEL` gains `secrets`.
- **The tests** (`tests/test_rotate.py`, `tests/test_render_consumers.py`, `tests/test_secrets.py`): 19 new — the rotation cases over a fake stack of `test_apply.py`'s shape after one verified apply, patching nothing (the first draft patched the module's internals and was replaced); the map over the three sites with the declarations held to the emitters' reads and the leftover-credential case; the registry's classes, the supplied names held to render's constants, `rotate_generated`'s contract.
- **The proof** (`.scratch/slice-1/assets/24-on-box.txt`): from the cycle's worktree in daytime with the ledger's §8 announcement ruled moot at discovery; the first preflight found ticket 43's cycle mid-proof with its guardrail at version 4 in the applied manifest, so the proof waited for 43's release and the branch was rebased onto it (the two-cycles rule, now with its manifest clause in `on-box-proofs.md` §7); every step a fixed script writing its own log, every secret by file or stdin, the old values kept root-only and shredded. Outages before `v0.2.0` with no users: the engine about three minutes for its key, the frontend and SearXNG seconds, the admin key none.
- **The runbooks**: the install runbook's step 6 rotation paragraph replaced by the command (what it prints, recreates, refuses, the window rule once General is live, the restore rule for a set older than a rotation) and section 4's re-run rule; the backup runbook's restore step gains the same restore rule.
- **The documents**: the map's §2, §4, §8, §12, §13, and §16; the render-apply leaf's read-when line, inventory row, `secrets rotate` bullet, the converge sentence, the secrets paragraph (the rotation class, the supplied registry, the consumer map), and the tests line; the glossary's *Generated secret* entry sharpened and *Rotation class* added through `/domain-modeling`; the map trimmed back under its 12k warning (~11,986 tokens) and the render-apply leaf left at ~6,926 past its 6k warning under the 8k cap, its compaction owned by workflow ticket 18 (opened from another session; the count recorded there at this release's tracker step); the README's narrative and its root-commands paragraph; the box ledger's entry answering open item 5's rotation half with a line for Transcribe; greenfield ticket 23's spec-bug comment on §20.2's omission of the command, in the shape of ticket 22's `backup drill` line.
- **The tracker**: ticket 24 resolved with the close-out comment; the claim's dated line recording the maintainer's four discovery rulings (the fifth rotatable entry, Grafana's refusal, the daytime proof, the strictly empty precondition); the plan file keeps its claim-time name (`F_0.1.39`) under the release skill's rule while the version is `0.1.40`, ticket 43 having taken `v0.1.39` during the cycle.
- **Version**: bumped 0.1.39 → 0.1.40 (with `tests/test_version.py`); a patch — a new command, no site key, no `/etc/gideon` contract change. Pre-`v0.2.0`, so no release note.
