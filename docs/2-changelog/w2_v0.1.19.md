# Changelog - Week 2, 07-09-2026, V. 0.1.19

**Release Date**: Week 2, 07-09-2026 at 12:21
**Version**: 0.1.19 (previously 0.1.18)
**Object**: TRIP-2 proves staged work on the acceptance VM: the on-box-staged-proof block in the TRIP-2 skill (after on-box-bounded-child, eight lines between its markers, listed in docs/agents/tooling.md §4) — a criterion that is a harness stage is proven over a temporary ref of the staged index, never by committing early: staging, git write-tree, and git commit-tree -p HEAD one && chain so a failed staging never proves the previous index, the ref written to refs/heads/tmp/<ticket>-proof and read back, sudo python3 -m tools.acceptance tmp/<ticket>-proof --until <stage> into a scratchpad --out run only when the ref resolves to the sha just made, the harness's status kept and printed since it prints none of its own, the ref deleted whatever the run's exit and the recipe exiting with the harness's status or 1 when the setup, the deletion, or the no-tmp/-left check failed, the rows into the ticket's <NN>-on-box.txt under a header naming the sha and the deletion, a run to preflight inside the Bash tool's cap and install and beyond detached; docs/archi/tools.md records that the harness resolves any ref the checkout knows (rev-parse --verify as root under safe.directory) and tags its mirror acceptance-<sha12> at it, a tag cloned by name — proven by running the recipe over the staged block itself --until clone three times, the last as the block states it: the clone row's tag the proof commit's first twelve characters, HEAD unchanged, 33 s, nothing left on the box (slice-1 ticket 21, resolved)
**Code review**: `docs/3-code-review/CR_w2_v0.1.19.md` (Codex loop, 3 rounds -> APPROVED)

## Changes

- **The block** (`.claude/skills/TRIP-2-implement/SKILL.md`, `on-box-staged-proof`, after `on-box-bounded-child` and before the `---`; ADR-0033, ADR-0035, `CLAUDE.md`'s Lessons rule): one paragraph in the skill's voice and a fenced recipe of four lines, eight lines between the markers against the ticket's under-ten. The paragraph: a harness-stage criterion is proven over a temporary ref of the staged index, never by committing early; `git write-tree` reads the index, so the work is staged first; the harness resolves any ref the checkout knows and tags its mirror `acceptance-<sha12>` at it; `--until` takes a stage identifier and the VM is torn down whatever the stage; `--out` is a new scratchpad directory, redacted and handed back; the rows go into `<NN>-on-box.txt` under a header naming the sha and the deletion (ticket 02's proof c is the shape); the recipe fails closed; a run to `preflight` fits the Bash tool's ten-minute cap and `install` and beyond go detached with the `on-box-page-cache` shape. The recipe: `git add -A`, the staged stat, `git write-tree`, and `git commit-tree -p HEAD` in one `&&` chain, the sha echoed; `git update-ref refs/heads/tmp/<ticket>-proof` and `rev-parse --verify` of it; the harness behind a guard that the sha is set and the ref resolves to it, its status kept and printed as `harness exit: <code>`; `git update-ref -d`, the check that no `tmp/` ref is left, and `exit` with the harness's status, falling through to 1. No positional placeholder in the block.
- **The tooling document** (`docs/agents/tooling.md` §4): the block's row (anchored after `on-box-bounded-child`, before the `---`; origin `v0.1.19`), and the `on-box-bounded-child` row's anchor amended to drop "before the `---`". `tests/test_skills_vendored.py` holds the (file, id) pair.
- **The code review's three rounds** (`docs/3-code-review/CR_w2_v0.1.19.md`): the recipe as first written was fail-open when pasted whole into one shell call — the deletion masked a red harness, a captured sha was silent, a stale `tmp/` ref left by an interrupted session would have been tested when an earlier line failed, and a failed `git add -A` would have proven the previous index. Each closed in the block; git's two edge behaviours probed in a scratch repository (an empty value for `update-ref` refused with 128, a missing ref deleted silently with 0).
- **Architecture memory** (TRIP-3 Step 7): `docs/archi/tools.md`'s acceptance-harness inventory row cites the block beside `acceptance-run` and slice-1 ticket 21, and its Commands bullet says how the ref is resolved (`rev-parse --verify` over the checkout as root under `safe.directory`; a tag cloned by name, any other commit through the `acceptance-<sha12>` tag the harness puts on the mirror). The map is unchanged: no subsystem appeared, no slice moved, no command landed, no cross-cutting fact moved. Counts: the map ~11.2k, the tools leaf ~2.1k, every leaf under its warning; `tests/test_archi_budget.py` green.
- **Proven on the box** (`.scratch/slice-1/assets/21-on-box.txt`): the recipe run over the staged block itself, `--until clone`, as the block states it — proof commit `07c9a42a…`, the ref read back at the same sha, seven harness rows ok with the `clone` row's tag `acceptance-07c9a42a422f`, `harness exit: 0`, `tmp refs left: none`, the recipe's own exit 0, 33 s from `preconditions` through `teardown`, the branch's HEAD unchanged at `8f036b5`, `--out` (`console.log`, `mail/`) handed back to the caller, no VM, run directory, or `tmp/` ref afterwards. Two earlier runs during the review, before the fail-closed guard and before staging joined the commit chain, passed the same way in 35 s and 36 s.
- **Tests**: no product logic, no new tests; the vendored-skills tripwire, the budget test, the evidence hygiene, and the version test green; lint and typecheck clean.
- **Tutorial** (`docs/5-tuto/tuto_0.1.19.md`): why a proof ref is built with plumbing rather than a commit, and why the harness mirrors the checkout and tags the mirror rather than cloning a sha.
- **Documentation sync**: the plan's Documentation Impact named `docs/agents/tooling.md` and `docs/archi/tools.md`, both changed in the cycle; the diff touches nothing else outside the TRIP docs (README's "Where things are", `CONTEXT.md`, the ADRs, the site example, `CLAUDE.md`, the runbooks unaffected). Slice-1 ticket 21 resolved with both criteria ticked.
- **The box ledger**: no shared thing touched (the acceptance VM and its run directory are GIDEON's own rows); the fetch of the other repository's copy returned a copy identical to ours; no entry, nothing to merge.
- **User-facing tag check**: a pre-General 0.x tag; skipped.
