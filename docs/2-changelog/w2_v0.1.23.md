# Changelog - Week 2, 07-09-2026, V. 0.1.23

**Release Date**: Week 2, 07-09-2026 at 22:21
**Version**: 0.1.23 (previously 0.1.22)
**Object**: the dev toolchain pinned once, and the local gate equal to CI's: requirements-dev.txt at the root the one list of the five pins (ruff, mypy, pytest, PyYAML as the stand-in for the box's python3-yaml, Playwright the package alone for the turn harness's browser mode — never the product's, spec §1.5), ci.yml's checks job and TESTING.md's onboarding command installing from it and naming no version; every gate that stated `mypy gideon tests` now `mypy gideon tests tools` as CI runs it (TESTING.md's pre-commit line, the TRIP-2, TRIP-3, and TRIP-review slot commands) and the false "CI installs ruff/mypy/pytest only" line gone from TESTING.md and TRIP-test; tests/test_toolchain_pins.py the tripwire — exact pins only, the install step reading the file with no inline pin, the gate's tools derived from the workflow and pinned, every name==version token in any workflow or TESTING.md equal to the file's line (pin-watch.yml's PyYAML held), the file's playwright pin equal to tools/turns/chromium.py's PLAYWRIGHT_VERSION, the onboarding line and the gate line built from the workflow's three steps present in TESTING.md, every ruff or mypy invocation in the four gate documents carrying the workflow's arguments and prose never an invocation — stating no version, each finding ending in its edit, each rule proven over a synthetic tree; a fresh venv from the documented command proven green (ruff, mypy over 170 files, 1118 tests); the archi-map-update block's count command also reporting CONTEXT.md's count, reported and never compared (slice-1 ticket 48's ruling and the budget test's glossary case unchanged; 1,150 bytes between the markers, under workflow ticket 11's ceiling), ARCHI-rules.md's Step 7 clause and the §4 row; the map's §3 cells, §4 tree, §6 command, and §15 pointing at the file, the tests leaf's tripwire line, the README row (workflow ticket 10, resolved)
**Code review**: `docs/3-code-review/CR_w2_v0.1.23.md` (Codex loop, 1 round -> APPROVED)

## Changes

- **One pin list.** `requirements-dev.txt` at the repository root holds the five exact pins the hosted `checks` job and a fresh dev `.venv` install — `ruff`, `mypy`, `pytest`, `PyYAML` (the stand-in for the box's `python3-yaml`), and `playwright` (the package alone, so `mypy` sees the turn harness's browser mode) — with the reasons `ci.yml` used to carry and the standing rule that the product never installs from PyPI (spec §1.5's boundary untouched). `ci.yml`'s `Install dev tools` step and `TESTING.md`'s onboarding command install from it and name no version.
- **The local gate equal to CI's.** Every place that stated `mypy gideon tests` now reads `mypy gideon tests tools`, as the workflow runs it: `TESTING.md`'s pre-commit line and the slot-filled commands of `TRIP-2-implement`, `TRIP-3-release`, and `TRIP-review`'s checklist (slot text, not drift: the pristine files carry no such line). The false "CI installs ruff/mypy/pytest only" sentence in `TESTING.md` and `TRIP-test` names the file instead.
- **The tripwire** (`tests/test_toolchain_pins.py`): exact `name==version` lines only; the `checks` job's install step read from the file with no inline pin; the gate's tools (the first token of the `Lint`, `Typecheck`, and `Unit tests` steps) pinned in the file; every whole-token `name==version` in any workflow under `.github/workflows/` or in `TESTING.md` equal to the file's line — which holds `pin-watch.yml`'s one-package `PyYAML` install, kept light on purpose; the file's Playwright pin equal to `tools/turns/chromium.py`'s `PLAYWRIGHT_VERSION`, the box-side check's home; the onboarding line and the gate line built from the workflow's three steps present in `TESTING.md`; every `ruff` or `mypy` invocation in the four gate documents carrying the workflow's arguments, a prose mention never an invocation. Names compare PEP 503-normalized; each finding ends in the edit that fixes it; each rule is proven over a synthetic tree with names no index publishes and versions outside any real series; the module states no version and the committed tree is asserted clean.
- **Proven from a fresh venv.** `python3 -m venv` plus the documented install, `pip freeze` showing the five at the file's versions, then `ruff check .` clean, `mypy gideon tests tools` clean over 170 files, `pytest` 1118 passed (1024 subtests) in 20 s.
- **The glossary's count reported.** The `archi-map-update` block's count command also names `CONTEXT.md`, its count reported beside the map's and the leaves' and never compared or budgeted — slice-1 ticket 48's ruling and `tests/test_archi_budget.py`'s glossary case unchanged; the block is 1,150 bytes between its markers, under workflow ticket 11's 1,500-byte ceiling; `docs/agents/tooling.md` §4's row and `docs/ARCHI-rules.md`'s Step 7 clause say the same.
- **Documentation.** The map's §3 cells for the five tools point at the file (Playwright's row keeps the launcher's constant as the code home), §4's tree gains the file, §6's venv command installs from it, §15 names pytest without a version; the tests leaf gains the tripwire's line; `TESTING.md`'s contract list names it; the README's "Where things are" gains the toolchain's row.
- **Version**: bumped 0.1.22 → 0.1.23 (with `tests/test_version.py`); a patch, no site-file change; workflow ticket 10 resolved.
