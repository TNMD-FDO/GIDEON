# Changelog - Week 2, 11-09-2026, V. 0.1.59

**Release Date**: Week 2, 11-09-2026 at 16:17
**Version**: 0.1.59 (previously 0.1.58)
**Object**: release: v0.1.59 — one home per workflow fact (workflow ticket 31)
**Code review**: `docs/3-code-review/CR_w2_v0.1.59.md` (Codex loop, 1 round -> APPROVED; no findings)

## Changes

`v0.1.56`'s release commit touched 31 files and its Documents bullet named eight documents for one ticket, several of them mirrors of one another: the Purpose column of `docs/agents/tooling.md` §4's block table restated each block beside the block itself, and the paragraph after the table restated every `CLAUDE.md` rule the blocks override; the tools leaf's inventory rows described behaviour the module docstrings, the tests, and the changelogs carry, and its Commands bullets had taken the mechanics back since compaction 10; the onboarding page and the workflow-commands memo paraphrased `CLAUDE.md`'s rules, so one rule change edited three documents; the release skill's Step 7 block named the documents it counts by a glob, and no document listed once which documents a release touches. The budgets fired on the mirrors — `tooling.md` at 5,995 and the tools leaf at 5,998 against a 6,000 warning — and nothing stated that the cycles record tool is complete, so each observation about it became a feature (second audit, findings 2 and 4; workflow ticket 31, rulings a–e). This release applies the architecture memory's one-home-per-fact rule (`docs/ARCHI-rules.md`) to the workflow's own documents: four moves and one line, no code, every cut a move or a pointer.

- **A block describes itself** (`docs/agents/tooling.md` §4): the block table keeps id, file, anchor, and origin — the Purpose column's 3,574 characters of cells leave, each block's own text being what a session reads; the sentence before the table says a block's own text says what it asks and a pointer block names its document in that text; the rules paragraph becomes one sentence, the rules living in `CLAUDE.md`, which names beside each the skill text it overrides; the ceiling paragraph keeps the rule and the ADR-0017 clause and loses its three figures to workflow ticket 11, which it names. The tripwire's `_block_rows` reads the first two cells, unchanged. 5,995 → 4,986 by the count script.
- **A tool's behaviour lives in its module and its test** (`docs/archi/tools.md`): each Inventory row is the tool, its command line, its home, and its spec; each Commands bullet opens with the same three and keeps its stages and invariants, one line each, the mechanics leaving to the module docstrings, the test modules, and the changelogs of the releases that landed them — the classifier's cases and the frame observer's states, the pin watch's state table and tree-listing walk, the cycles record's store discovery and subagent fold, the renumber's file set, the tracker's section contents and blocker grammar; the Tests part one line per module. The cycles row states that `tools/cycles.py` is frozen — no new column, table, or selector; a wish for one is an observation — because its three decisions (a ticket's band, the seam, the pairing per phase) are served now that phases are sessions. The leaf template and the standing rules stand unchanged (the claim's discovery decision). 5,998 → 4,868.
- **A rule's home is `CLAUDE.md`, a command's the memo's card** (`docs/agents/onboarding.md`, `docs/6-memo/workflow-commands.md`): onboarding's §2 steps 4 and 5, its §3 cycle paragraph, and its §6 worktree, one-box, version, ledger, and seam paragraphs each become one pointer sentence naming the rule and linking its `CLAUDE.md` section, the worktree, on-box, and ledger references linked beside it, the memo named by backticked path in three places and linked nowhere; the memo's phases-as-sessions, pairing, and worktree paragraphs point at `CLAUDE.md` "Ticket sizing", `tooling.md` §3, and `CLAUDE.md` "Two sessions, one checkout", keeping the launcher's commands, the handoff, the phone path, and the frontier rule as their home. Every link and anchor of the onboarding page resolves (the resolving script: 0 findings). 4,918 → 4,612 and 1,859 → 1,767.
- **The release's document list has one home** (`docs/ARCHI-rules.md`, `TRIP-3-release`): the Step 7 checklist lists once, each the home of one kind of fact, the documents a release touches — the map, a leaf, a tool's module docstring and test, `CLAUDE.md`, a document under `docs/agents/`, the glossary, the workflow-commands memo, a GIDEON block — with the count command and the rule that a release edits a fact at its home and adds no restatement, a mirror the diff shows cut in the same release with its ledger; "One home per fact" says the workflow's own documents follow it; the History gains the event's line. The `archi-map-update` block is a pointer at that checklist, 1,182 → 771 bytes, its row's origin gaining ticket 31; Step 10's blocks are untouched.
- **The move ledger** (`.scratch/workflow/assets/31-move-ledger.md`): the committed text at `b58f7cb` against the new text, per document — 738 backticked and bold spans, 94 absent from the new whole files, every one with a home, true losses none, four corrections (the ceiling paragraph's three figures, the Purpose cell's handoff spellings, the cycles row's command line, the ledger's own count of the leaf corrected at this release to the count script's 4,868).
- **Documents**: `docs/agents/tooling.md`, `docs/archi/tools.md`, `docs/agents/onboarding.md`, `docs/6-memo/workflow-commands.md`, `docs/ARCHI-rules.md`, and the `archi-map-update` block of `.claude/skills/TRIP-3-release/SKILL.md` — six; the ledger and the ticket are tracker files.
- **Tests**: none new — the change is prose. `ruff`, `mypy`, and the suite green; `tests/test_archi_budget.py` counts the leaf and the agent documents, `tests/test_skills_vendored.py` reads the four-column table and holds the block under its ceiling, `tests/test_export_boundary.py` finds no link into the excluded memo, `tests/test_evidence_hygiene.py` and `tests/test_office_values.py` walk the new asset; no test changed.
- **Ticket 31 close-out**: the ticket's five criteria and the brief's seven ticked; the deferred criterion — the following release's changelog Documents bullet naming six documents or fewer — cannot be met before that release and stays unticked with the completion comment saying so; the outstanding-items step writes it as one Observe line in `.scratch/workflow/99-observations.md` (the mechanism the Documents bullet, the measure the number of distinct documents the following release's bullet names, the threshold six), struck in the `observed at` form by that release's outstanding-items step, which ticks the criterion with a dated comment in the same commit. The cycle writes no observation about `tools/cycles.py`.
- **Tutorial**: skipped — a patch tag whose one design decision, one home per fact, is already the architecture memory's rule (`docs/ARCHI-rules.md`) and the subject of `v0.1.58`'s changelog.

**Step 7's counts**: `docs/agents/tooling.md` 4,986 by the count script (from 5,995), `docs/archi/tools.md` 4,868 (from 5,998), `docs/agents/onboarding.md` 4,612 (from 4,918), each at least 1,000 under its 6,000 warning; `docs/6-memo/workflow-commands.md` 1,767 (from 1,859) and `docs/ARCHI-rules.md` 4,639 (from 4,010), neither budgeted; the map 11,615, `CLAUDE.md` 3,306, the other leaves and agent documents unchanged from `v0.1.58`; the glossary 15,799, reported and never budgeted. No map row moved — no subsystem appeared and no command landed — and the tools leaf's Cited-by line stands. The budget test green.

**The box ledger**: no shared thing touched, so no GIDEON entry; the §11 fetch found one new line in Gideon Transcribe's §12 subsection (its `v1.38.0` entry: `v1.37.0` built and started on the server before the window at its operator's decision, every request from `v1.38.0` carrying `priority` 1, and the Moments feature's `video_url` requests at about 3,000 prompt tokens each under §3's interactive rule, nothing shared changing), merged into our copy; sections 1 to 11 and the GIDEON subsection identical. The line is a record and asks nothing of GIDEON, so no answer.

**Window**: none — nothing runs on the box; no service recreated, restarted, or rebooted. No release note and no human read: a pre-General 0.x tag (the release note and ADR-0036's read start at `v0.2.0`). No acceptance run: a patch tag.
