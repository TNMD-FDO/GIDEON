# Changelog - Week 2, 11-09-2026, V. 0.1.58

**Release Date**: Week 2, 11-09-2026 at 14:48
**Version**: 0.1.58 (previously 0.1.57)
**Object**: release: v0.1.58 — the brief is the one contract (workflow ticket 30)
**Code review**: `docs/3-code-review/CR_w2_v0.1.58.md` (Codex loop, 1 round -> APPROVED; no findings)

## Changes

The tracker rule's fold (workflow ticket 02, `v0.1.22`) was written to stop tickets reaching 19–24 KB and in practice tripled one at triage: the body rewritten around the ruling, a ruling paragraph restating it with every verification clause and lettered ruling, a `## Agent Brief` carrying a second criteria list beside the ticket's, and the commit body a fourth copy — at workflow ticket 30's triage the 28 open `ready-for-agent` tickets had a median of 13,093 bytes, tickets 25 and 29 had gone 2,881 → 10,335 and 3,036 → 10,596, and each ticket is read whole at the claim, the plan, the implement session's Step 0, and the close-out. This release rewrites the rule so a triage writes each fact once, in the one place that owns it — the architecture memory's one-home-per-fact rule applied to a ticket — and the documents that cite the fold follow in the same change (second audit, finding 1; workflow ticket 30, rulings a–e). No code changes; the section keeps its name and its anchor.

- **The fold section** (`docs/agents/issue-tracker.md`, "Folded at `ready-for-agent`"): the opening paragraph keeps the history and the transition rule whole and gains the second half of the history — the fold as first written tripled a ticket, with the figures above — and ends with the rule applying forward: a ticket is never refolded at its claim, and one that reached `ready-for-agent` before the rule is read as it stands (ruling c); the sentence that had the claiming `TRIP-1` session fold a pre-rule ticket is gone. The shape list is seven items: the body as opened, a fact the ruling changed carrying a superseded clause in the section's existing shape, a dated note that landed before the ruling folded once as one attributed sentence copied nowhere else, the body never rewritten around the ruling (ruling a); the ticket's-own-criteria item removed (ruling b); the ruling paragraph the three verification clauses, then the rulings (a)…(n) and the sizing sentence, nothing restated from the body or the brief, a gap the triage found a ruling and not a separate clause; the `## Agent Brief` the one contract with the one criteria list, the opened checkboxes moved into it, each kept as written or sharpened by the ruling, a criterion the ruling met ticked there with the ruling's letter, the release's `ticket-closeout` block unchanged; the comments item as before. The superseded-fact paragraph is unchanged. The check paragraph is replaced whole: the loss ledger, the token scan, and the worked example of slice-1 ticket 24 leave, since nothing is rewritten (the ticket-24 fold stays recorded on workflow ticket 02 and in `F_0.1.22`'s plan); the check is the size — the commit body's byte counts, **before** being the file at the triage's read (the opened text plus every dated note since) and **after** the committed file — and a triage that more than doubles that size is restating something and rereads the ticket against the shape before its commit, the measure workflow ticket 30's from tickets 25 and 29's triples.
- **The commit section** ("One commit at the ruling"): the commit body is the three verification clauses, the lettered rulings, and the byte counts before and after — nothing of the ticket's text restated, the body being git's record of the triage and never the ticket's (ruling e). The subject shape, the 72-character rule, the one `AskUserQuestion`, the standing-move exception, and the *Defer* paragraph are unchanged.
- **The Conventions bullets**: the `Status:` bullet says the brief is the folded ticket's one contract; the Comments bullet says the pre-ruling comments fold into the body once, each fact one attributed sentence. Both pointers unchanged.
- **The Observations section**: a strike form for a measurement, `observed at <event>: <figure>` — a line naming a measure and the event that supplies it is struck by the session that sees the event only when the figure satisfies the line's threshold, the figure in the clause, the event never a date; when the line stands for a ticket's criterion the same commit ticks that criterion with a dated comment, resolved or not; a figure that misses the threshold goes in that commit's body and the line and the criterion stay open for the next event. The citation is the commit's body, not a ruling paragraph, and the four-readers bullet's triage clause carries that exception: a line whose measure the triage's own commit supplies is no fact of the ticket being ruled. Written for this release's own deferred criterion, below.
- **The citing documents** (ruling d): `docs/agents/triage-labels.md`'s folded sentence states the shape in one sentence at the same pointer; `docs/agents/onboarding.md`'s cycle paragraph gains the shape in a clause at the same link, its worked example keeps slice-1 ticket 55 and its commit and says the file carries the shape the rule had at `v0.1.52` — the ticket's criteria beside the brief's — with a triage after workflow ticket 30 writing the brief's list as the one list, and its result-of-a-ruling paragraph says the body stays as opened, the ruling paragraph carries the triage's verdicts, the brief holds the one list, and later facts stay dated. Every link and anchor resolves; the page's line about the architecture compaction's loss ledger is `TRIP-compact`'s and stays. `CLAUDE.md`'s "written folded" sentence stands; `docs/agents/tooling.md` is untouched, the `triage` skill and its `AGENT-BRIEF.md` being Matt Pocock skills that take no block and already saying the brief is the contract.
- **Backward**: the rule applies forward. The open `ready-for-agent` tickets carrying no ruling paragraph and those folded under the old rule are read as they stand; a claiming session folds nothing; a closing session ticks whichever criteria list the ticket carries, the `ticket-closeout` block naming no list. This release's own ticket, folded under the old shape with two lists, closes that way.
- **Tests**: none new — the change is prose. `ruff`, `mypy`, and the suite green; `tests/test_archi_budget.py` counts the three documents, `tests/test_tracker.py` and `python3 -m tools.tracker --strict` unchanged (the tool reads the `Status:` line alone; the strict run's one finding before the commit is the uncommitted claim), `tests/test_skills_vendored.py` sees no drift.
- **Ticket 30 close-out**: the ticket's first three criteria and the brief's first five ticked; the deferred criterion — the next triage after the rule lands writes a ticket at most twice its pre-triage size, its byte counts in the commit body — cannot be met before this release lands and stays unticked with the completion comment saying so; the outstanding-items step writes it as one Observe line in `.scratch/workflow/99-observations.md` (the mechanism the fold, the measure the next `ready-for-agent` ticket's two sizes from its triage commit's body, the threshold at most double), struck in the `observed at` form by the next triage that writes a `ready-for-agent` ticket, which ticks the criterion with a dated comment in the same commit.
- **Tutorial**: skipped — a patch tag with no teachable design decision beyond one-home-per-fact, already the architecture memory's rule (`docs/ARCHI-rules.md`).

**Step 7's counts**: `docs/agents/issue-tracker.md` 5,285 by the count script (from 4,966; the plan foresaw a shrink, but the opening paragraph's figures and the `observed at` strike form outweighed the check paragraph's leaving), `docs/agents/onboarding.md` 4,918 (from 4,827), `docs/agents/triage-labels.md` 439 (from 415), each under its 6,000 warning; the map 11,615, `docs/archi/tools.md` 5,998, and `docs/agents/tooling.md` 5,995 unchanged from `v0.1.57`, the last two still two to five tokens under their warning; the other leaves and agent documents unchanged; `CLAUDE.md` 3,306. No leaf touched and no map row moved — the tool is unchanged. The budget test green.

**The box ledger**: no shared thing touched, so no GIDEON entry; the §11 fetch found Gideon Transcribe's copy identical to ours whole — sections 1 to 11 and both subsections — so nothing merged.

**Window**: none — nothing runs on the box; no service recreated, restarted, or rebooted. No release note and no human read: a pre-General 0.x tag (the release note and ADR-0036's read start at `v0.2.0`). No acceptance run: a patch tag.
