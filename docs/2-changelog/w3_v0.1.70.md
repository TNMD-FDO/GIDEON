# Changelog - Week 3, 16-09-2026, V. 0.1.70

**Release Date**: Week 3, 16-09-2026 at 16:42
**Version**: 0.1.70 (previously 0.1.69)
**Object**: release: v0.1.70 — the public repository and its export (ticket 57)
**Code review**: `docs/3-code-review/CR_w3_v0.1.70.md` (Codex loop, 2 rounds -> APPROVED with observations)

## Changes

Spec §2.6's home, `TNMD-FDO/GIDEON`, is now a public repository. From this tag on, it receives a filtered `git archive` of every release tag as one commit carrying the same tag. The development repository is renamed `TNMD-FDO/GIDEON-dev` and stays private (slice-1 ticket 57; slice-0 ticket 22; spec §2.6 read under the ticket's ruling (a)). The plan is `docs/1-plans/public-repository-and-export-block.plan.md`.

- **The export step** (`bin/release-export`):
  - It is one fixed script in the shape of `bin/release-git` and `bin/worktree-claim`, and it runs from the primary.
  - Its guards run before any write: a worktree copy is refused, `origin` must name `GIDEON-dev`, the tag must be on `main` and on `origin` at one commit, a git identity must be set, and `gh` must read `TNMD-FDO/GIDEON` as itself and public, never a redirect.
  - It clones the public repository, empties it, extracts the tag's archive into it, and runs `ruff`, `mypy`, and `pytest` in the export tree.
  - It commits with the tag as the message, tags, pushes `main` and the tag atomically, and waits for the public `ci` run to go green.
  - Before any commit, it enforces the lineage invariant: the public tip's message must be the name of the `v*` tag on it.
  - A tag that is already public goes straight to the wait, and a red check or run stops the export.
- **The tests**:
  - `tests/test_release_export.py` (new) has 21 cases over bare repositories and stub tools.
  - `tests/test_release_git.py`, `tests/test_worktree_claim.py`, `tests/test_trip.py`, and `tests/test_toolchain_pins.py` gain the export-tree skip.
  - The archive of the staged tree passes `ruff`, `mypy`, and `pytest` with 60 skipped.
- **The skills**: `TRIP-3-release` Step 14 gains the public-export paragraph, and `TRIP-hotfix` Step 8 gains its sentence. Workflow ticket 43 carries the dated line.
- **The repositories** (the outward steps, each recorded on ticket 57):
  - The rename ran, and `origin` was re-pointed once in the shared configuration.
  - The public repository was created empty, and its settings were set and read back: issues on, wiki and discussions off, secret scanning and push protection on, GitHub-owned actions only, fork approval for all external contributors, and private vulnerability reporting on.
  - The `v*` ruleset (id 23565960) was proven by a tag push refused while its bypass list was empty.
  - The organisation's only runner group reads `allows_public_repositories: false`.
  - The history-wide scan found 0 secret shapes over 467 commits.
- **The documents**:
  - The README gains a `## Contributing` heading.
  - `CONTRIBUTING.md` names the development repository and the public pull request's route.
  - The three runbooks say where the ruleset lives and name `GIDEON-dev`.
  - ARCHI §4, §12, and §16, the tools and tests leaves, and `docs/agents/tooling.md` §1 are updated.
  - At Documentation Sync, the run links in `docs/6-memo/lock-coupled-tests.md` and the branch links in `docs/teach/RESOURCES.md` were retargeted to `GIDEON-dev`.
- **The box ledger**:
  - The rows agreed with Transcribe's operator are written: the Copies line, §1's two rows, §9's two rows, and §11's fetch. The rules version is 2026-09-16.10, since v0.1.68's §2 rows took .9 on the same day. The rebase kept both changes.
  - The §11 fetch found Transcribe's copy at 2026-09-09.6 with a subsection identical to ours.
  - The §12 entry carries **For Transcribe:** line 23: fetch from `GIDEON-dev`.
- **The tickets**:
  - Ticket 57 is resolved with four of its criteria ticked. Criterion 4, the first export's green public run, is ticked at the outstanding-items commit.
  - Slice-0 ticket 22 carries the dated comment.
- **Tutorial**: skipped. This is a patch tag, and the plan's Overview names no teachable design decision; the script follows the guard scripts' shape.

**Step 7**: `bin/release-export` and `tests/test_release_export.py` are new. The tools leaf's row and bullet, the tests leaf's line, and map §4 were updated in the cycle. Counts: the map is 11,777 (warning 12,000), `tools.md` is 6,061 (warning 6,000, past it, so it goes on Step 15's list), and `tests.md` is 2,064. The budget test is green.

**Window**: none. Nothing on the box changed, no apply runs, and no service is recreated. There is no release note and no human read, since this is a pre-General 0.x tag. There is no acceptance run, since this is a patch tag.
