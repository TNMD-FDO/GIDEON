# Changelog - Week 2, 09-09-2026, V. 0.1.42

**Release Date**: Week 2, 09-09-2026 at 16:43
**Version**: 0.1.42 (previously 0.1.41)
**Object**: a prompt in the chat page's URL leaves no log the box keeps (§19.4, [22] items 14–15; slice-1 ticket 69): the rendered Caddyfile's `log` block takes Caddy's `filter` encoder over `json` with a `query` filter on the request URI, the Referer, and a redirect's Location, each replacing the six parameters that carry a user's text or choice — `q`, `shared`, `redirect`, `load-url`, `youtube`, `v` — with `redacted`, the flags and ids left readable; the frontend's Compose block restates the image's `bash start.sh` with uvicorn's defaults and `--no-access-log`, `AUDIT_UVICORN_LOGGER_NAMES=uvicorn.error` making the flag hold and `ENABLE_AUDIT_GET_REQUESTS=false` pinned by name, the drill's frontend the same; the search-sentinel CI stack runs the command and the two names; proven on the box from the worktree by five anonymous marker requests before and after the apply
**Code review**: `docs/3-code-review/CR_w2_v0.1.42.md` (Codex loop, 1 round -> APPROVED)

## Changes

- **The ingress** (`compose/caddy/Caddyfile.tmpl`; `render/caddy.py` unchanged, the hostname its one substitution): the `log` block keeps `output stdout` and its `format` becomes the `filter` encoder wrapping `json`, a `fields` block naming `request>uri`, `request>headers>Referer`, and `resp_headers>Location`, each under a `query` filter with six `replace … redacted` actions. The four names are the pinned frontend's chat-page parameters that carry a user's text or choice (the prompt, the sign-in page's nest, the two attach targets — slice-1 ticket 69's ruling), and the two more are the backend's ASGI middleware's: `GET /?shared=<text>` (the web-app manifest's share target) is redirected to `/?q=<text>` or to `youtube=`/`load-url=` when the text is a link, and the legacy `/watch?v=<id>` to `/?youtube=<id>`, the redirect's `Location` a response header Caddy logs — the plan's Codex review found it, the tags confirmed it, and the ticket's four parameters and two fields became six and three under the ruling's own principle. `replace` over `delete` so the event stays readable; `hash` unused (at the pin it hashes a constant); the flags and ids (`model`, `submit`, `web-search`, …) readable on purpose; every logged query string re-encoded with its keys sorted, accepted; the comment above the block says all of it for the CSA who reads the rendered file. A `caddy adapt` pre-flight through the pinned image parsed the block before the apply; Caddy's pre-existing "not formatted" warning at the template's line 2 is unchanged.
- **The frontend** (`render/compose.py`, `render/drill.py`, `render/owui.py`): `OWUI_COMMAND`, the image's `bash start.sh` followed by uvicorn's shipped defaults (`--workers 1 --ws-per-message-deflate true`) and `--no-access-log`, rendered as the `open-webui` block's `command` on every host and in the drill's document — the image declares no entrypoint and the script hands any container argument to uvicorn in place of its defaults, so the defaults are restated. The environment gains `AUDIT_UVICORN_LOGGER_NAMES=uvicorn.error` — the frontend's startup empties uvicorn's two loggers and attaches its own handler to every logger named there, default `uvicorn.access`, the route by which the access line reached the journal and what would re-arm it after the flag, since each connection asks that logger for handlers once when it opens; pointed at the error logger, the flag holds and uvicorn's two startup lines reach the journal instead — and `ENABLE_AUDIT_GET_REQUESTS=false`, the one switch that would put a page load's URI into the audit file, pinned by name in ticket 16's pattern. `GLOBAL_LOG_LEVEL` stays `INFO`.
- **The CI stack** (`tests/contract/search_sentinel.py`, `sentinel/compose.yaml`): the derived key list gains the two names and the stack's `.env` gains `OPEN_WEBUI_COMMAND`, the constant joined as shell words, so the sentinel frontend runs the box's whole posture; the five runs are unchanged in outcome (the discovery's one addition to the ticket).
- **Tests** (2 new; `tests/test_render.py`, `tests/test_render_owui.py`): the Caddyfile encoder case (the three paths, eighteen `replace` lines with the names asserted individually, `delete`/`hash`/`log_credentials` absent, no `;`, no `$`) and the frontend command case over the production and drill documents; the two names in both environment tests; the nine fixtures regenerated, the diff the log block, the command list, the two environment lines, and the digests.
- **The proof** (`.scratch/slice-1/assets/69-probe.sh`, `69-on-box.txt`; from the cycle's worktree, 16:21–16:25 CDT, no users before `v0.2.0`): `render --diff` named the Caddyfile, the Compose file, and the four units and the row `caddy, open-webui`, the plan's Window bullet; the probe's five anonymous requests through the ingress (the site CA, the hostname resolved to loopback, no session, no redirect followed) with a fresh marker — the chat page with `?q=` beside `web-search=true`, `/api/config` with a Referer carrying that page, the sign-in page with it nested in `redirect`, `/?shared=`, `/watch?v=` — put the marker, before the apply, in the ingress's URI four times, its Referer once, its Location twice, and in four of the frontend's five access lines, nowhere else; the apply recreated the two by their reasons and verified; after it the same requests left the marker in no journal and not in the audit file, `q=redacted` three times and `shared`, `redirect`, `youtube`, and `v` once each standing in the ingress's lines, the frontend writing nothing for the five (its healthcheck and probe lines gone with the access line, two lifecycle lines arrived); the second `render --diff` empty. Counts and statuses only, ticket 16's enumeration reused by path. **Window**: Caddy and the frontend, recreated by the proof's apply from the worktree, free with no users on the box; the release's apply from the primary moves the units' `WorkingDirectory` alone.
- **The research note** (`docs/research/ingress-log-filter-and-frontend-access-line.md`): Caddy v2.11.4's `filter` encoder grammar (the `fields` block optional), the `query` filter's actions, its per-element pass over a header array, the absent field skipped, the unparseable value logged unchanged, the `hash` action's constant; Open WebUI v0.11.3's two environment names, its logger startup's two asymmetric loops, the audit middleware's methods and entry, `start.sh`'s argument substitution, the image's command with no entrypoint; uvicorn 0.51.0's `--no-access-log` and the per-connection `hasHandlers()` snapshot; its B7 the redirect middleware, the share target, the OpenSearch descriptor's `?q=` template, and Caddy's `resp_headers`. Written by a `researcher` spawned from the primary before the claim's worktree whose working directory followed the session into the worktree, every later command refused, so the note was copied onto the branch by hand; CLAUDE.md's Subagents paragraph carries the clause (wait for the note's commit before `EnterWorktree`).
- **Records**: the stack leaf's inventory row (and the search-probe row rejoined to the table it had fallen out of), the render-apply leaf's constants clause, the map's §16 network clause, the glossary's *Ingress log*, the [22] comment under item 15 (the second shape in which user text travels in a URL through the ingress; spec §15's last sentence and §19.4 edited at their next touch), dated lines on tickets 16 (the `[?&]q=` shape on the ingress now counts redacted lines) and 41 (the parameter set, the redirects, the entrypoint's defaults, and the two names re-read at a bump); the box ledger merged with Transcribe's v1.36.1 copy — its answers to GIDEON's lines 12–16 and the shared-engine client live since 14:38 — and GIDEON's `v0.1.42` entry closing Transcribe's line 1 and GIDEON's line 12 by number with the rules change ticket 66 proposed (slice-1 ticket 66's remaining criteria); ticket 69 resolved, its widened set recorded.
