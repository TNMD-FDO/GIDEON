# Changelog - Week 2, 07-09-2026, V. 0.1.21

**Release Date**: Week 2, 07-09-2026 at 16:16
**Version**: 0.1.21 (previously 0.1.20)
**Object**: the window guard counts a browser turn as three engine calls: the turn harness's office-hours guard compares engine calls with SMOKE_TURNS — under --browser each case turn counts BROWSER_ENGINE_CALLS_PER_TURN (3: the answer, then the page's title and tag tasks, follow-ups rendered off), the probe's chat-id call one, the API mode's count and rows byte-identical — the constant a starting value beside SMOKE_TURNS in tools/turns/run.py whose comment cites ticket 36's measurement (65 engine calls for 24 completions, 62 over 21 browser turns with the three probes at one) and names the engine access log's POST /v1/chat/completions lines over a run as its correction; one fragment, '(3 per browser turn[, the probe 1])', follows the engine-call count in the refusal (3 turns, 9 engine calls (3 per browser turn) during office hours), the ok row, and the dry run's new engine-calls line, the summary row still counting rows; the browser guard test rewritten to the ticket's numbers (three refused before sign-in, two admitted, two plus the probe at seven, three under --force) and the API mode held to no 'engine calls' phrase; docs/archi/tools.md's invariants and browser-mode sentence, the TRIP-2 skill's on-box-turns block (two browser cases the daytime handful) and its docs/agents/tooling.md row — proven on the box at office hours on a Monday: the two-case dry run printing six engine calls, the three-case set refused at nine with the fix, the forced dry run, and a two-case live run admitted with the engine's access log growing by exactly six lines in two groups of three, every one a 200, the frontend's completions route serving the two turns alone (slice-1 ticket 40, resolved)
**Code review**: `docs/3-code-review/CR_w2_v0.1.21.md` (Codex loop, 1 round -> APPROVED)

## Changes

- **The constant** (`tools/turns/run.py`; spec §18.5, ADR-0017, ADR-0028): `BROWSER_ENGINE_CALLS_PER_TURN` (3) joins the harness's starting values beside `SMOKE_TURNS`. Its comment states the mechanism — a turn from the frontend's own page sends the answer, then the page's title and tag tasks, follow-ups being rendered off in `render/owui.py`, where the API mode's managed turn and the probe's chat-id completion send no background tasks and cost one — cites the measurement (ticket 36's proof: 65 engine calls for 24 completions, 62 over 21 browser turns once the three probes are counted at one), and names the correction: the engine access log's `POST /v1/chat/completions` lines over a run's span, divided by that run's browser turns. `SMOKE_TURNS`'s comment now says the handful is engine calls; the summary's comment says a turn is a row.
- **The guard** (`tools/turns/cli.py`): the count splits into the turns (cases × `--repeat`, × 2 under `--stream`, + 1 with `--probe-inlet` — the rows, unchanged) and the engine calls (the case turns × the factor under `--browser`, × 1 otherwise, + 1 for the probe's call in either mode); the office-hours refusal fires on the engine calls. One fragment — `(3 per browser turn)`, `(3 per browser turn, the probe 1)` when flagged — is built once and follows the engine-call count in the refusal's detail (`3 turns, 9 engine calls (3 per browser turn) during office hours (16:03 America/Chicago)`), the ok row's detail, and the dry run's new `engine calls:` line; the fix is unchanged; the API mode's rows and dry run are byte-identical to `v0.1.20`'s; the `summary` row keeps counting rows.
- **Tests** (`tests/test_turns_browser.py`, `tests/test_turns.py`): the browser guard test rewritten and renamed to the ticket's numbers at an injected office-hours clock — three browser cases refused before sign-in with the factor in the row and `--force` in the fix, no page launched and no frontend call; two admitted at six; two plus the probe admitted at seven with the summary's three turns; three admitted under `--force` with the override phrase — and the two dry-run tests asserting the new line; the API-mode guard test holds its rows and dry run to no "engine calls" phrase. Lint, typecheck, and 84 tests with 60 subtests green across the touched areas and the vendored-skills, budget, and evidence-hygiene tripwires.
- **Documents**: `docs/archi/tools.md`'s invariants clause reads engine calls and its browser-mode sentence carries the factor and the rows that show it, the inventory row citing ticket 40; the TRIP-2 skill's `on-box-turns` block gains one clause (a browser turn counts three engine calls against the guard, so two browser cases are the daytime handful) and its `docs/agents/tooling.md` §4 row the clause and the version; the map's §12 slice-1 phrase moves to `v0.1.21` with ticket 40 in its list — the ticket's "§8 browser-mode sentence" lives in the leaf since ticket 47's split, and the map's §8 table row already points there.
- **On the box** (`.scratch/slice-1/assets/40-on-box.txt`, a Monday at 16:03 office time): the two-case `--browser --dry-run` printing `2 turns; 6 engine calls (3 per browser turn)` and the dry run's own line, no launch; the three-case set refused before sign-in at nine engine calls with the fix, exit 1, no `--out` directory made; the same set under `--force --dry-run` admitted with the override; then a two-case live run admitted at office hours — both turns answered with a clean screen in 7.66 s and 14.73 s, two chats deleted, 796 requests on the one host, exit 0 — with the engine's access log, idle for the thirty minutes before, growing by exactly six `POST /v1/chat/completions` lines in two groups of three, every one a 200, the frontend's completions route serving the two turns alone: the starting value's first correction, unchanged at 3.
- **Version**: bumped 0.1.20 → 0.1.21 (with `tests/test_version.py`); a patch, no site-file change, no acceptance run; slice-1 ticket 40 resolved, a pointer comment on ticket 36.
