# Changelog - Week 2, 10-09-2026, V. 0.1.51

**Release Date**: Week 2, 10-09-2026 at 17:51
**Version**: 0.1.51 (previously 0.1.50)
**Object**: the README is the export's first page (§2.6, §2.2, §1.9, §1.5; ADR-0004, ADR-0031; slice-1 ticket 60): the 205-line release-by-release narrative that restated `docs/2-changelog/` at every tag leaves for a two-line `**Status:**` paragraph — the slice state, the tree's tag in the fixed clause "this tree is `v0.1.51`", and the link to `CHANGELOG.md` — under a CI badge for `ci.yml` at spec §2.6's home; the `Running` prose is re-cut by reader into `## Install` (the install runbook's receiving-office sequence at command level, the owned `/opt/gideon` before the clone and the `cd` into it made explicit in the runbook too) and `## Quickstart` (the CLI from a checkout, the bare-host boundary, the venv and the three checks); the `Where things are` table moves whole under `## Documentation`, its nine inline links into `.scratch/` now backticked references under the one sentence ticket 56 asked for; `tests/test_readme.py` holds the badge, the four headings in order, the status paragraph's shape and its tag equal to `gideon.__version__`, every relative link resolving, and none into `.scratch/`; and the release skill's Step 9 gains the `readme-status` block: the tag is the one token a release moves, and what a release landed is the changelog's, never a sentence in the README
**Code review**: `docs/3-code-review/CR_w2_v0.1.51.md` (Codex loop, 2 rounds -> APPROVED)

## Changes

- **`README.md`** (the plan's Implementation Details 1): 283 lines to 128. The title, then GitHub's own workflow badge for `.github/workflows/ci.yml` at `https://github.com/TNMD-FDO/GIDEON` on `main`, linked to the workflow's page — spec §2.6's home, which ticket 57's rename ruling keeps for the public export, so the badge shows this repository's checks to signed-in members today and the public repository's after the first export; the opening paragraph verbatim; the two-line status; `## Install` as the runbook's §1 sequence, each step's commands a fenced block a reader can paste — the install home created owned by the checkout's account (`sudo install -d`, the acceptance harness's shape, since `/opt` is root's), the clone and the `cd`, provision with the print-once identity stored and the backup key authorized on the target and the `reboot-required` rule, the site file, the supplied secrets, `registry mirror`, provision again, `preflight.sh`, `install.sh` with its phases and the two break-glass passwords printed once, `alerts test` — then the re-runnable rule scoped from provision on, the corpus step named as slice 3's, the thin-entrypoints sentence with upgrade and rollback, and the runbook and office-services documents as backticked references; `## Quickstart` for the developer — `python3 -m gideon --help` from a checkout, the boundary and root-and-Docker sentences from the old `Running`, the venv from `requirements-dev.txt`, the three checks as the venv's executables with ARCHI §6's arguments; `## Documentation` with the sentence that paths under `.scratch/` are the development repository's work tracker, not carried by the public export, and the table's 30 rows in their order and wording; `## License` unchanged. The loss ledger: 62 tags in the removed narrative, each with its changelog file except `v0.2.0`, the future tag in the release-notes row; the old `Running` section's every sentence in `Install` or `Quickstart`; the table 30 rows before and after; exactly the nine `.scratch/` link targets dropped and the two badge URLs added. No table of contents (five headings; GitHub renders the outline), no length ceiling: the rule is that the file carries no release history.
- **`tests/test_readme.py`** (Implementation Details 2): the changelog-index test's shape — a frozen finding, pure `rule_*` functions over an injectable root, `findings()` in rule order, the sentence `<path>:<line>: <what>. Fix: <how>` — with `rule_badge` (the first non-blank line after the title is the linked `ci.yml` badge for `main`), `rule_headings` (`Install`, `Quickstart`, `Documentation`, `License` as an ordered subsequence of the `##` headings, so a heading may join without editing the test), `rule_status` (exactly one `**Status:**` paragraph before the first heading, at most three lines, the clause "this tree is `v<version>`" exactly once for the version passed in — the milestone tags in the same paragraph can never stand in for the tree's — and an inline link to `CHANGELOG.md`), `rule_links` (every relative inline link resolving from the root, fenced and inline code skipped, fragments and schemed targets ignored), and `rule_boundary` (no inline link target under `.scratch/`, the one prefix the ticket's criterion named; ticket 56's boundary test takes the whole list when it lands and rules on this one then). The committed tree clean against `gideon.__version__` imported, never restated; eight firing cases on a seeded tree (the badge, a heading, a stale tag, a status over three lines, a status without the link, a second status paragraph, a broken link, a link into `.scratch/`). The inline-link finder is the budget test's shape written again, `tests/` not being a package.
- **The release's Step 9** (Implementation Details 3–4): the `readme-status` GIDEON block after Step 9's upstream text and its row in `docs/agents/tooling.md` §4 — the README carries no release history; Step 9 is one token, the tag in the status paragraph moved with `tests/test_version.py`'s pin and held equal by the tripwire before the commit question; a cycle's implementation leaves the tag at the tree's version so its testing gate stays green (this cycle wrote `v0.1.50` and Step 9 moved it); a slice's close or a heading's change is a Documentation Sync correction with the test's heading rule edited in the same change. `tests/test_skills_vendored.py` holds the block under the ceiling and the count per file to the table.
- **Documents** (Implementation Details 5, 6, 8, 9): `docs/agents/worktrees.md` §8's rebase parenthesis names the README's status tag in place of the clause; the install runbook's §1 step 1 gains the owned-directory line before the clone and the `cd` into the checkout, the harness's proven shape, so the README and the runbook state one sequence; dated notes on tickets 56 (the README's `.scratch/` links already backticked and the sentence written, so its README work is the runbook rows' retarget; the boundary rule's hand-over) and 57 (the reporting line's home under the test's subsequence rule — a `## Contributing` heading or a sentence in `## License`; the badge already names the public home); the tests leaf's tripwire line; the map's §11 version bullet names the README's tag as the version's one other appearance, held equal by the tripwire. `CONTEXT.md` unchanged (no new term); no ADR; no release note before `v0.2.0`; `CONTRIBUTING.md` untouched (already backticked references and the `CHANGELOG.md` pointer); ticket 60 resolved. The counts at Step 7: the map ~11,420 tokens against the 12,000 warning, the tests leaf ~1,910, the tools leaf ~5,960 under its 6,000, `docs/agents/tooling.md` ~5,820; `docs/agents/on-box-proofs.md` stands at 6,056, past its 6,000 warning since `v0.1.49` named it for `TRIP-compact`, the person's command, untouched by this release; the glossary at 15,211, reported and never budgeted. The budget test holds every cap.
- **This release**: no product code, no on-box step; Codex's plan review three rounds (round one: the tag written at implement must be the tree's current one, the install list complete at the command level, a fixed clause for the tag; round two, after one reply cut off mid-draft and re-issued: the owned directory before a non-root clone, the `cd`, the backup key's authorization, the re-runnable promise scoped past the clone; round three: the two added commands absent from the runbook, absorbed as its correction); the code review two rounds (the three checks as the venv's executables; the install re-run sentence naming the re-run's effects as the runbook does). Lint, types, and the full suite green on the tree: 1383 tests, 9 new. The tutorial `docs/5-tuto/tuto_0.1.51.md`: a document held by a tripwire on its shape rather than a ceiling on its size, and the version's second home held by import.

**Window**: none — nothing applies on the box; no service is recreated, restarted, or rebooted, and no rider is ruled on. No release note and no human read: a pre-General 0.x tag. The box ledger is untouched: no shared thing moved.
