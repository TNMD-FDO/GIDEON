# Changelog - Week 2, 09-09-2026, V. 0.1.39

**Release Date**: Week 2, 09-09-2026 at 12:02
**Version**: 0.1.39 (previously 0.1.38)
**Object**: the hidden base model withheld by the guardrail's inlet: the arithmetic guardrail's `inlet` declares the pinned frontend's `__model__` parameter and refuses a `user`-role turn whose model entry's record carries no base-model id — the generator addressed by its own id through the chat page's `?model=` parameter or a hand-made request, a discovered model with no record, or no entry at all — with the fixed branch refusal before the engine is called (§16, ADR-0006's inlet still content-blind; slice-1 ticket 43), the test structural so presets pass by their record and the Filter holds no copy of the generator's served name, admins and the eval identity passing as at the session gate, which runs first so its cases are unchanged; the Function's frontmatter at version 4 and the six fixture files regenerated; the turn harness's browser mode gains the third probe row `inlet-base-model` from the users' seat — the generator's served name read from `models.lock` through the site's profile under `--probe-inlet` alone, the row posting to the base model with the owned chat id and expecting the refusal on a 400, a turn only when it answers — the fake frontend and the real lock's text in its tests; proven on the box from the cycle's worktree in one daytime sitting: `render --diff` naming the manifest and the four units' `WorkingDirectory` with an empty recreate row, the apply pushing the Function and recreating nothing, the maintainer's `?model=gideon-generator` turn as the test account refused in the bubble with one ERROR line and no engine request for its completion (the page's title task after it the recorded residual — a task call no Filter governs), the same URL as an admins-group account answered, the browser mode's three probe rows green, the API mode as the eval identity unchanged, a second apply a no-op; the map's §16 clause, the render-apply and tools leaves, `docs/agents/on-box-proofs.md` §3, the glossary's *Generator* entry, spec §16's enforcement sentence with its Appendix E line, ticket 08's pointer line (slice-1 ticket 43, resolved)
**Code review**: `docs/3-code-review/CR_w2_v0.1.39.md` (Codex loop, 2 rounds -> APPROVED with observations)

## Changes

- **The rule** (`compose/open-webui/functions/arithmetic_guardrail.py`): `BRANCH_REFUSAL` beside `SESSION_REFUSAL` — *"GIDEON answers only through one of its branches. Start a new chat and ask General."* — with its `BranchRefusal` exception; a pure helper `_is_preset` that reads a model entry's `info.base_model_id` and nothing else (never `id` or `name`, so the Filter holds no copy of the generator's served name and slice 4's Research preset passes unchanged); `Filter.inlet` declares `__model__` as its fourth parameter, the pinned frontend's process-cache entry for the id the request named, handed by name; the branch gate runs after the session gate on the `user`-role path — a sessionless request on any model is still the session refusal — and admins and the eval identity return before both. The base entry's record carries `base_model_id: None`; a discovered model without a record carries no `info` under GIDEON's environment (the frontend's default-metadata merge is dormant, `docs/research/owui-model-record.md` §2.6) and `info` with `meta` alone if it were enabled; both shapes, a non-mapping, and no entry fail the same test. The frontmatter moves to version 4; the module comment names the two transport-only gates.
- **The Filter's tests** (`tests/test_arithmetic_guardrail.py`): fictitious entries (`a-preset` over `a-base`, the base, `meta` alone, no `info`, an empty base id, a string, `None`) with a comment citing the merge's shape; the existing `user`-role calls hand the inlet a preset entry; the branch-rule cases, the gate order, admins and the eval identity on the base entry and on no entry; the hygiene test pins the refusal's text and the inlet's parameter names beside the other two hooks' signatures.
- **The fixtures**: the three sites' `open-webui/manifest.yaml` (the Function's content) and `manifest.yaml` (its digest), regenerated by `tests/regenerate_render_fixtures.py` twice — once after Codex's batch and once after the comment rewrap.
- **The harness** (`tools/turns/cli.py`, `tools/turns/run.py`): under `--probe-inlet` alone the CLI reads `models.lock` from the checkout through the lock loader and `select_profile` with the site's `hardware_profile` (a loader error, an unknown profile, or a generator-less profile a `preconditions` refusal with the loader's fix), `RunSpec.base_model` carrying the served name (`None` outside the probe, so the API mode reads no lock), the dry run's probe line naming it; `_run_probe` posts the third completion through the existing chat-id call with the base model's id and the owned chat id before that chat is deleted and before the stray sweep, ok iff 400 with the Filter's `BRANCH_REFUSAL` as the detail, the row's detail a status and never response text, the three failure branches naming three rows, the completion counted as a turn only when it answered (the gate failed open — the code review's Minor).
- **The harness's tests** (`tests/test_turns_browser.py`, `tests/test_turns.py`): the fake frontend answers a completion naming the base id (400 with the loaded refusal by default; a 200 answer and a 400 with another detail for the failure shapes); the fake host carries the checkout's real `models.lock` text at the root path and the expected base id is derived through the loader and the site's profile (the testing guide's derive rule, never a restated served name); three rows green with the third's JSON record, the summary at two turns, the chat-id and base-model completions both on the owned chat, the chat deleted once; the failure shapes printing the status and never the detail, three turns when the base answered; the transport and internal-error branches naming three rows; the dry run's engine-call line unchanged and its probe line naming the base id; the malformed-lock and absent-profile refusals; the API mode's negative (no lock read, no probe row). Seven new tests; 238 pass across the affected modules and the tree-wide tripwires.
- **The proof** (`.scratch/slice-1/assets/43-on-box.txt`, the one-case set `43-probe-cases.yaml`): from the cycle's worktree in one daytime sitting — `render --diff` naming the manifest's hunk and the four units' `WorkingDirectory` hunks (Codex's plan-review P1) with `Services apply would recreate: none`; the apply in 69 s pushing the Function and recreating nothing; the maintainer's `?model=gideon-generator` turn as `gideon-test-user` refused in the bubble, "New Chat" on General, the chat deleted, one ERROR line carrying the sentence, no completion between the page's request and the refusal; the same URL as an admins-group account answered (three engine requests); the browser mode with `--probe-inlet` over the one-case set — the case answered clean from the seat, `inlet-bare`, `inlet-chat-id`, and `inlet-base-model` green, two turns, two chats deleted, 591 requests to the one host, four engine completions, the two refusal lines in the journal; the API mode as the eval identity answered on General; a second apply a no-op with an empty diff. Nine engine completions in the sitting, under the guard's handful with no `--force`; no root-owned entry left in the worktree.
- **The residual**, recorded on the ticket and in the map: two seconds after the refused turn the page's title task ran on the task model (one completion of 307 prompt tokens, then the sidebar's refresh) — a task call, which the ticket's finding (2) recorded as untouched by any inlet rule; the turn's completion never reached the engine, and closing the title task would take a frontend patch (ADR-0001) or title generation off product-wide. The code review's Major was read against the ticket's criterion ("no engine request for that turn") and closed as accepted with the residual documented.
- **The documents**: `docs/ARCHI.md` §16's bullet rewritten from "a convenience, not a boundary" to the gate and §12's row through `v0.1.39` with 43; the render-apply leaf's Filters bullet (the two gates and their order, the refusal's two surfaces, the residual), its constants line (three refusals), and its tests line; the tools leaf's browser-mode row and sentence (three probe rows, the lock read); `docs/agents/on-box-proofs.md` §3's probe clause; spec §16's enforcement sentence with the branch clause and Appendix E's line (ADR-0006's rejected content block stays rejected); the glossary's *Generator* entry through `/domain-modeling`; the README's status sentence; the tutorial `docs/5-tuto/tuto_0.1.39.md`.
- **The tracker**: ticket 43 resolved with the close-out comment, folded at the claim under the tracker's rule (18,833 to 19,026 bytes, the ledger at the plan's end); ticket 08's pointer line. The box ledger fetched and compared: Transcribe's copy carries the older rules version and no new line, nothing merged, no entry (no shared thing touched).
- **Version**: bumped 0.1.38 → 0.1.39 (with `tests/test_version.py`); a patch — no site key, no `/etc/gideon` contract change. Pre-`v0.2.0`, so no release note.
