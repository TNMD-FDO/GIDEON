# Changelog - Week 2, 10-09-2026, V. 0.1.45

**Release Date**: Week 2, 10-09-2026 at 09:15
**Version**: 0.1.45 (previously 0.1.44)
**Object**: the arithmetic guardrail's Guidelines family (§16, §18.2, ADR-0006; slice-1 ticket 42): a second family judged after the deadline family — an offense level and a criminal history category resolved to a months range, a range placed on the client or the matter, or the user's range or pair affirmed — with its own refusal and seed, the Filter's plumbing made per family (the tripped family's refusal, supplied figures, confirmation context, set version), the tools reading every family's refusal, and the seed on the box at 0 of 54 controls replaced with one live resolution caught in the stream
**Code review**: `docs/3-code-review/CR_w2_v0.1.45.md` (Codex loop, 2 rounds -> APPROVED)

## Changes

- **The family** (`compose/open-webui/functions/arithmetic_guardrail.py`, version 6; the plan's Solution Architecture §§1–2): `GUIDELINES_FAMILY` after `DEADLINE_FAMILY` in `FAMILIES`, three bounded patterns under the ruling's ids — `guidelines/level-and-category-to-range@1` (a level-and-category pair in either order or the `20/III` shorthand, a resolving link — an arrow, a colon, `=`, "is", "yields", "gives", "would give", "produces", "results in", "comes to", "calls for", "would be", "equals", "of" — and a months range whose forms include HARV-011's non-breaking hyphen and narrow no-break space and the table's top row "360 months to life"; the reverse alternative a range then "for" or "at" and the pair), `guidelines/range-asserted@1` (a possessive subject's range, the applicable or advisory range with a present or conditional link, an exposure verb, or the range for the client — "was" and "were" never links, so a court's finding passes by tense), and `guidelines/range-confirmed@1` (the deadline family's two confirmation shapes over the targets "range", "guideline range", "offense level", "category", the in-text shape gated on the family's confirmation context — a figure plus an ask — by `Pattern.needs_context`, the maintainer's ruling at discovery refined at the plan review: Guidelines doctrine opens with "Yes" beside "category"). The Guidelines grammar joins its words with one whitespace character and admits at most one optional space, so the longest pair, the gaps, the longest link, and the longest range fit `MAX_MATCH_CHARS` (141 forward, 130 reverse; the code review's Major, the two maximal texts held by the bounds test). `GUIDELINES_REFUSAL` is the ruling's text, pinned verbatim.
- **The plumbing per family** (§§3–6): `Family` carries `pattern_set_version`, `figure_form`, `reverse_form` (the figure that, leading a match, marks the assertion shape), `figures` (the normaliser — a date to `YYYY-MM-DD`, a range to `low-high` or `low-life`, a pair to `level/ROMAN`), `figure_nouns`, `constructions` (the four restatement forms built by one factory, `_build_constructions`, over the family's figure form), and `confirmation` (its context and pattern id); the stash and `message_context` carry `supplied` (family → keys) and `confirmation` (the families whose context matched); `judge_text` and `judge_rendered` take the pair with empty defaults (`NO_SUPPLIED`); the bare opening affirmation is the first family in order whose context matched; `REFUSALS` and `REFUSAL_BY_FAMILY` derive from `FAMILIES`, the outlet's `replace_message` and the stream's `_trip_chunk` take the tripped family's refusal, the error path and a stateless stream keep the deadline's; `PATTERN_SET_VERSION` retired — the set version is per family, each seed header naming its family's.
- **The seed** (`eval/seed/guardrails/guidelines-range.yaml`, 21 positives and 27 controls, every text invented; the README rewritten for two files): the direct, memo (HARV-011's shape with its characters), asserted, buried, reverse, top, confirm, and bypass positives; controls over construction, base offense levels, adjustments, zones, the career offender's category VI opening with "Yes", points, statutory ranges, a guideline's term range, the one-book rule, the court's range in a case description (`case-01`, the one tripping control under the ruling's "of" link — the maintainer's ruling to hold the class here and leave the attribution construction to ticket 45), a second passing by tense, the user's figures restated under each construction, a bare illustrative range, the Sentencing Table described, the safety valve, variance and departure, relevant conduct, acceptance, the mixed pair-for-context case, the asks answered without an affirmation, and a thinking-only computation as the withholding's control.
- **The tests** (`tests/test_arithmetic_guardrail.py`, `tests/test_turns.py`, `tests/test_engine_verify.py`; 15 new): the seed gate over every file under `eval/seed/guardrails/` with the trip's family the file's and each family's ceiling; the fake stream asserting the tripped family's refusal in-stream, at the outlet, and in the trip row; the `Guidelines` class — the canonical figures, the constructions over family matches exempt and tripping, the confirmation gate, the two in-text shapes by target and the bare opening's tie-break, the prefix-mode opening, the bypass cases read from the seed, the maximal texts under the bound; the harness's stream and outlet attribution over the Guidelines refusal and its seed reader over the new file; `engine verify`'s classification over either refusal and its loader refusing a Filter without `REFUSALS`.
- **The tools** (`tools/turns/classify.py`, `gideon/host/engine.py`): the refusal tests iterate `REFUSALS`; `_load_filter` requires it. `render/owui.py`'s Function description names a filing deadline or a Sentencing Guidelines range; the six manifest fixtures regenerated.
- **The proof** (`.scratch/slice-1/assets/42-on-box.txt`, `42-browser-cases.yaml`): the cycle waited from 00:54 to 07:03 CDT for ticket 71's release (its uncommitted worktree apply held the box's record, the §7 rule), rebased onto `v0.1.44` — which took this plan's number — and applied from its worktree with the manifest alone changed, the recreate row `none`. The seed in the API mode twice, 96 turns, daytime by the maintainer's ruling with `--force`: controls replaced 0 of 54 (the deadline family's 2 of 72 at `v0.1.43` the comparison), positives 38 of 42 refused — one replaced in the stream, the appositive "of" form produced live, `guardrail_trips` row 32 as the family's first kept event — and 4 answered with no leak and no range in the stored answer (ticket 38's decline-form shape; one a 6,976-character variance memo with no figure at all); the thinking-side count 0 by construction. Three users-seat turns as the test account: the variance-memo request ended in a memo with no figure and every frame clean; the construction and base-offense-level questions answered with the timer present ("Thought for 2 minutes", "Thought for 4 minutes") — the latter recollecting § 2K2.1(a)(4)(A)'s level as 26 where the guideline says 20, a level with no range, ticket 45's shape. No construction the rule lacks was met; the Filter is unchanged after the run.
- **Documents**: spec §16 (the family paragraph, the tier-2 list, the enforcement sentence's refusal, the eval line), §18.2's `guardrails` row, §18.3's gate, Appendix E; ADR-0006's amendment paragraph and the glossary's *Guidelines guardrail*, *Guidelines-range computation*, *Guidelines-range restatement*, and *Guidelines refusal* entries via `/domain-modeling`; the render-apply leaf (the families' row, the Filters bullet, the constants and tests lines) and the tools leaf; the README's sentence; the notes on greenfield [07], tickets 39 and 44, the `Blocked by` lines of 45 and 70 with a note on 45; ticket 42 resolved. The tutorial `docs/5-tuto/tuto_0.1.45.md`.

**Window**: none — the apply pushed the manifest alone and recreated nothing users see; the recreate row read `none` before the apply. No release note and no human read: a pre-General 0.x tag (the release note and ADR-0036's read start at `v0.2.0`).
