# Changelog - Week 1, 06-09-2026, V. 0.1.15

**Release Date**: Week 1, 06-09-2026 at 15:39
**Version**: 0.1.15 (previously 0.1.14)
**Object**: glossary entries name their leaf: 66 of CONTEXT.md's 206 entries end with one _Leaf_: line linking the leaf under docs/archi/ whose landed code owns the term (render-apply 23, host 13, tools 12, backup-restore 10, stack 8, tests none), judged per entry from each leaf's Read-this-when, Inventory, and Commands text with the map's §8 table the tiebreak and every borderline decided in the plan (Quiet window to tools for the turn harness's office-hours guard, Reference build to host for the models lock's reference, Backup identity to backup-restore for the sealing at backup run, Kept event to stack for the audit writer, Mirrored group to render-apply for the manifest's group, Runtime measure to stack for observability; Site, Record, Branch, Release, Changelog, the design rules, and every slice-2-to-6 term unlinked), the twenty groups, every definition, and the 143 _Avoid_ lines byte for byte as they were — the diff 66 added lines and the preamble's one sentence stating the shape; tests/test_archi_budget.py's link rule scans CONTEXT.md after the map and the leaves (a glossary link to a missing file a finding with the same one-line fix, no budget and no index for the glossary, one new case); the shape recorded in CLAUDE.md's Domain docs paragraph and the glossary's own preamble, never in the domain-modeling skill, whose folder still matches skills-lock.json; docs/ARCHI-rules.md's slice-start sentence names the glossary link as a step and docs/archi/tests.md's tripwire line names the glossary in the scan set; the count script reads the glossary at ~12.5k from ~11.4k; slice-1 ticket 48 resolved with the count recorded
**Code review**: `docs/3-code-review/CR_w1_v0.1.15.md` (Codex loop, 1 round -> APPROVED)

## Changes

- **The glossary** (`CONTEXT.md`; slice-1 ticket 48, `docs/6-memo/archi-map-and-leaves.md` §4): 66 of the 206 entries end with one `_Leaf_:` line in the `_Avoid_:` idiom — the leaf's path from the repo root as an inline link whose text is that path in code font, the map's §2 idiom — after the `_Avoid_:` line where there is one and after the definition where there is none. Ownership was judged per entry in the plan (`docs/1-plans/F_0.1.15_glossary-leaf-links.plan.md`, Implementation Details §1) from each leaf's Read-this-when, Inventory, and Commands text, the map's §8 table the tiebreak, a shared or partly landed mechanism linking the leaf whose landed code produces or enforces the term today: `render-apply.md` 23 (the model records and presets, the secrets, the rendered tree, the guardrail's family, the release note's template row), `host.md` 13 (the host layer, provision, preflight, the site model, the locks, the office clock), `tools.md` 12 (the harnesses, the pin watch, the vendored skills, the built image, the quiet window's guard), `backup-restore.md` 10, `stack.md` 8 (the directory identity, the release registry, the audit writer's rows, alerting, observability's runtime measures), `tests.md` none. Left unlinked with the reason recorded in the plan: a term that names a path or a policy no code reads (Site, Record, No-model-arithmetic rule), a concept spanning unbuilt branches (Branch), the release process's documents (Release, Changelog), site keys no code reads yet (Prompt logging, Chat retention), and every organisation, legal, and slice-2-to-6 term. Two entries the plan's first draft had left unlinked were added at Codex's plan review: Mirrored group (each `mirror_groups` entry is rendered as a manifest group today) and Runtime measure (its definition says observability owns it). Against the pre-cycle file the diff is the 66 added lines and the preamble's one sentence; the twenty `###` groups in their order, every definition, and the 143 `_Avoid_:` lines are unchanged; ~11,418 → ~12,477 tokens, no budget. Edited through `/domain-modeling`, whose files are untouched (`tests/test_skills_vendored.py` green).
- **The link rule** (`tests/test_archi_budget.py`): `GLOSSARY_PATH` beside the map and leaf paths; the link rule's scan set is the map, the leaves, then `CONTEXT.md` when it exists, so a synthetic root without a glossary scans nothing new and the existing case's finding order holds; the glossary's links resolve from the repo root, where the file sits. One new case: a glossary linking one present leaf and one missing leaf yields exactly one finding, at `CONTEXT.md`, rendered as the same problem-and-fix line, and a glossary five times the map cap yields no budget finding. The budget rule and the index rule are untouched: the glossary has no cap and is never indexed. `CommittedTree` now resolves the 66 committed links.
- **The convention's homes**: the glossary's preamble gains its third sentence (entries grouped by domain area; an entry whose mechanism a landed leaf owns ends with a `_Leaf_:` line linking it; a new entry joins its group), the place the `domain-modeling` skill reads before it edits; `CLAUDE.md`'s Domain docs paragraph gains one sentence naming the shape and that it lives in the preamble, never in the skill (a Matt Pocock skill takes no block: `docs/agents/tooling.md` §6 reinstalls upstream's files whole and the drift test holds the folder hash with nothing stripped); `docs/ARCHI-rules.md`'s Step 7 checklist says a slice start also links the glossary entries the new leaf owns through `/domain-modeling`; `docs/archi/tests.md`'s tripwire line names the glossary in the link rule's scan set, the one home of that fact. `docs/ARCHI.md` unchanged (§1 already names the glossary as an authority; §15 points at the tests leaf); the map at ~11.1k and every leaf under 3k.
- **Documentation sync**: the memo's status line records that §4's glossary link landed here and that its grouping half was found already in place.
- **Tracker**: ticket 48's four criteria ticked, the count by leaf and the two plan-review corrections recorded in its comments, resolved.
- **Version**: bumped 0.1.14 → 0.1.15 (with `tests/test_version.py`); a patch — documentation and one test module, no product code, no rendered artifact, no site-file or `/etc/gideon` contract change (§2.1); no acceptance run (patch tags never run it). Pre-General 0.x tag: no release note (§21's check begins at `v0.2.0`). No tutorial: a documentation convention, its design in the plan and the memo.
