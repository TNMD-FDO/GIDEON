# Changelog - Week 2, 10-09-2026, V. 0.1.55

**Release Date**: Week 2, 10-09-2026 at 23:30
**Version**: 0.1.55 (previously 0.1.54)
**Object**: the arithmetic guardrail's sentence-credit family (§16, §18.2, ADR-0006; slice-1 ticket 70): a third family judged after the deadline and Guidelines families — a release date the model computed (a release noun linked in the present or future to a date, a month and year, or a season; a person's release verb before a date; a sentence's or supervision's expiry; an eligibility date), a credit count (a count joined to a credit noun, an earning verb, a reduction, or remaining on the sentence), a time to serve in the conditional or future, or a supplied release date or credit affirmed — with its own refusal, seed, date and count forms, and confirmation context; rates and caps excluded by construction, an authority's past-tense figure and the user's echoed count passing; the deadline family's three wrong-refusal shapes (a sentence's expiry, the last day of a sentence, a credit-shaped day count) handed over by a bounded exclusion on its patterns and its `date` confirmation by a displacing context, the deadline and Guidelines seeds' every committed verdict unchanged; the seed on the box at 4 of 92 controls replaced (the user's count echoed, fixed with an echo construction and markdown-mark tolerance), positives refused 71 of 76, the deadline seed at 0 of 36 over the final Filter; the plan's number taken by `main` at `v0.1.51`, renumbered before the rebase
**Code review**: `docs/3-code-review/CR_w2_v0.1.55.md` (Codex loop, 6 rounds -> APPROVED, two of them after the on-box proof's fixes)

## Changes

- **The family** (`compose/open-webui/functions/arithmetic_guardrail.py`, version 7; the plan's Solution Architecture §§1–3, 7): `SENTENCE_CREDIT_FAMILY` after `GUIDELINES_FAMILY` in `FAMILIES`, four bounded patterns under the ruling's ids — `sentence-credit/release-date@1` (a release noun with its modifiers, or "the last day of his sentence", linked by a present or future word or symbol link — a colon, an arrow, a table cell's pipe — with an optional qualifier to the family's date form; a person's release verb with its auxiliaries or the contraction; a sentence, term, supervision, supervised release, probation, custody, imprisonment, or confinement subject, "itself" admitted, with an expiry verb; an eligibility date; the reverse form with its own links, "marks" and "becomes" among them; never "was", "were", or "as"), `sentence-credit/credit-count@1` (the count of a credit noun, the credit noun linked to a count, an earning verb, a reduction, the count remaining on or of a sentence or term, and "has N days to serve" — a rate word after the unit or the noun excluded by a look-ahead that admits markdown bold, a cap word before the count by the pattern's exclusion), `sentence-credit/time-to-serve@1` (the served term in the conditional or future with an and-N-months tail, the actual-time phrase, the percentage resolved to a count; never a past-tense or perfect served term), and `sentence-credit/release-date-confirmed@1` (the two confirmation shapes over the family's targets, gated on its context). The family's date form is its own — the shared calendar forms plus a month-and-year and a season-and-year form, keyed `YYYY-MM` and `YYYY-<season>` — and its count form keys a number word, a hyphenated compound, or digits with a decimal to one canonical key with the unit singular; the shared `DATE` and the deadline regexes are untouched. `SENTENCE_CREDIT_REFUSAL` is the ruling's draft with its third sentence tightened, pinned verbatim.
- **The deadline handoff** (§§4–5): `Pattern.exclusion` — a bounded regex searched `EXCLUSION_REACH_CHARS` (40, held at most the exemption reach and the restatement look-ahead) around a match, whose hit covering the match's start rejects the match, a hit deeper inside (a second clause the deadline's greedy gap swallowed) rejecting nothing — carried by the deadline's `date-near-deadline-word` (an expiring sentence subject, with or without "itself" and an auxiliary, before "expire", "expires", "runs out", or "run out"; "last day of <owner> <sentence noun>", date-led or not) and `days-remaining` (a day count, with the deadline's "he has" prefix, joined to a credit noun, remaining on or of a sentence, or "to serve"), and by the credit-count pattern for its caps; `Pattern.yields_to`, the deadline's `date-confirmed` skipped while the sentence-credit context matched; `Family.confirmation_displaces`, the sentence-credit context removing the deadline's for the message, so a release-date ask's bare "Yes" and its in-text "that date is right" are this family's and a deadline ask's stay the deadline's; the exclusions' destinations aligned word for word with the shapes given up (the code review's rounds), "more" admitted in the count. No committed deadline or Guidelines verdict moved, so their set versions stand — the README states the reading.
- **The echo construction** (§6, the proof's fix): `Family.echo_form`, a family's optional fifth restatement construction, the sentence-credit family's a determiner-led supplied count joined to a credit noun ("subtract the 412 days of credit") — the live shape of a decline describing the Bureau's method, read offline from the seed run — exempt only over supplied figures and never beside an affirmation; a seed control and a bypass positive carry it. Underscore emphasis around a count is a recorded residual, the pinned model bolding with asterisks.
- **The seed** (`eval/seed/guardrails/sentence-credit.yaml`, 38 positives and 46 controls, every prompt a real computation ask or a real doctrine question, every figure invented; the README gaining the third file and the set-version reading): the release, expiry, eligible, credit, serve, confirm, memo, buried, and bypass positives; controls over § 3624(b)'s rate and cap after the First Step Act and the slash form, the FSA's accrual and its 12-month cap, § 3624(c), Wilson, § 3585(b), § 3583(b), (e)(1), (e)(3), and (h), § 3624(e) and Mont, Barber, the year and a day, RDAP, parole, the computation sheet, the full-term date, a proposed term in advocacy, a § 2241 description in the past tense, an authority's credit count (`case-credit-01`) and the attribution noun form (`attribution-01`) — the two tripping controls within a ceiling of two, the attribution class ticket 45's construction will free — the FSA's dates, vesting, ineligibility, consecutive terms, the remedy, compassionate release, home confinement, conditions, inoperative time, nunc pro tunc, the credits' application, detainers, proration, state credit, probation, two declines, the asks, the mixed cases, the restatements, and the thinking-only control.
- **The tests** (`tests/test_arithmetic_guardrail.py`, `tests/test_turns.py`; 19 new): the seed gate over the third file with the family's prefixes; the `SentenceCredit` class — the canonical figures over every written form, the constructions over supplied and unsupplied figures with the affirmative twins and the adversative, the rate look-ahead and the cap exclusion with a cap merely nearby exempting nothing, every handed shape moving and every deadline shape staying with a rejected match holding nothing in the stream, the exclusion covering the match's start so the earlier family keeps a mixed text, the served forms tripping and the past-tense forms passing, the displacement and the yield, the release-verb asks making the context, the echo and the markdown marks, every deadline and Guidelines seed case's verdict and context unchanged, the refusal pinned verbatim, the family's maximal texts under the bound, the exclusion reach under both windows; the harness's seed reader over the new file. `render/owui.py`'s Function description names a release date or sentence credit; the six manifest fixtures regenerated.
- **The proof** (`.scratch/slice-1/assets/70-on-box.txt`, `70-browser-cases.yaml`; the ticket's proof note): four applies from the worktree, each the manifest alone with the recreate row `none`. A three-turn `--stream` handful in office hours, every stream clean. The seed in the API mode twice in the quiet window, 168 turns — the first two runs interrupted by pid at 24 rows each when the code review's rounds changed the patterns, so the record is one Filter's: **controls replaced 4 of 92** (4.3 %), every one in the stream under `credit-count@1` (`guardrail_trips` rows 36–39); **positives refused 71 of 76** — two replaced in the stream (rows 34 and 35, the family's first kept events) and 69 the model's decline — and five `answered` with no figure and no leak (ticket 38's decline-form shape); the thinking-side count 0 by construction. The four control trips re-asked unfiltered on the API path and judged offline: the user's count echoed in a decline, and the model's bold around a count; fixed and re-applied, then **the deadline seed once over the final Filter, 0 of 36 controls replaced** (2 of 72 at `v0.1.43`), 21 of 22 positives refused. Three users-seat turns as the test account: the "when does he get out?" question declined with no date or count but the user's own at any painted frame; the § 3624(b) and supervised-release-term questions answered with the timer present and live clean, their `must` phrases failing on the model's wrong recall of the statutory figures — a rate and a cap the family passes by construction.
- **Documents**: spec §16 (the family paragraph, the tier-2 list, the enforcement sentence's refusals), §18.2's `guardrails` row, §18.3's gate; ADR-0006's amendment paragraph and the glossary's *Sentence-credit guardrail*, *Sentence-credit computation*, *Sentence-credit restatement*, and *Sentence-credit refusal* entries via `/domain-modeling`; the engine-frontend leaf (the families' row, the Filters bullet with the exclusion, yield, and displacement, the constants and tests lines); the notes on greenfield [07], tickets 39, 44, and 45; ticket 70 resolved. The tutorial `docs/5-tuto/tuto_0.1.55.md`.

**Window**: none — every apply pushed the manifest alone and recreated nothing users see; the recreate row read `none` before each. No release note and no human read: a pre-General 0.x tag (the release note and ADR-0036's read start at `v0.2.0`).
