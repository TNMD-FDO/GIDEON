# Changelog - Week 1, 06-09-2026, V. 0.1.14

**Release Date**: Week 1, 06-09-2026 at 10:57
**Version**: 0.1.14 (previously 0.1.13)
**Object**: ARCHI as a map with leaves: docs/ARCHI.md is the map (sections 1–19 with their numbers kept, §2 one row per leaf, §8 the dispatch contract, the three registries, the rows rule, and a 21-row command-to-leaf table, §7 and §15 their models, §12 a leaf column, every link file-relative) at ~11.1k tokens from ~17.7k, and docs/archi/ holds six leaves made by moving text — host.md, render-apply.md, backup-restore.md, stack.md, tools.md, tests.md, 0.8k–2.8k each — with a diff-derived ledger of the pre-split file's 807 backticked and bold spans finding none absent (.scratch/slice-1/assets/47-move-ledger.md); tests/test_archi_budget.py holds the map to 14k and each leaf to 8k with the count script's own character-category estimate reproduced to the token, every leaf indexed from §2 and every indexed leaf existing, and every relative link resolving from its file's directory (nine root-relative links corrected); docs/ARCHI-rules.md rewritten around the model (budgets and warnings, routing, the leaf template, one home per fact, compact then split by mechanism, Step 7's checklist, the standing rules, a dated History of the four compactions and the split); three GIDEON blocks — archi-leaves-read in TRIP-1-plan and TRIP-2-implement, archi-map-update in TRIP-3-release Step 7 — with their rows in docs/agents/tooling.md §4, the plan template's Documentation Impact line naming the leaves a ticket touches, TRIP-test's contract line pointing at the tests leaf, CLAUDE.md's architecture-memory, lessons, and compaction paragraphs naming the map, the leaves, the routing rule, and the per-file compaction; README, TESTING.md, and the memo synced; slice-1 ticket 47 resolved with the counts recorded
**Code review**: `docs/3-code-review/CR_w1_v0.1.14.md` (Codex loop, 2 rounds -> APPROVED)

## Changes

- **The map** (`docs/ARCHI.md`; slice-1 ticket 47, `docs/6-memo/archi-map-and-leaves.md`): sections 1–19 keep their numbers and headings, since plans, changelogs, code-review records, the review checklist, the plan template, and test docstrings cite them; §1 carries the reading rule (the map first and whole, then the leaves a ticket touches) and names the budget test; §2's inventory is one row per leaf with what it covers and the commands it owns; §3's tooling paragraph, §4's lock-file bullets and tool footprints, §7's six model bullets, §8's twenty-one command bullets, and §15's tripwire list moved out, each replaced by a pointer or a table — §7 one bullet per model naming the module and the leaf, §8 the dispatch contract, the three registries, the rows rule, a 21-row command-to-leaf table, and the "when a command gains behaviour" rule; §12 gains a Leaf column (slice 0 the six, slice 1 `host.md`, `render-apply.md`, `tools.md`, slices 2–4 their future names in code font); §13's two labels and every self-reference to moved content cite the leaf; nine root-relative links retargeted to resolve from `docs/`. ~17,657 → ~11,126 tokens.
- **The six leaves** (`docs/archi/`): `host.md` (~1.6k: provision, preflight, the site model, the three locks, host state, no-GPU mode, the office clock, the runner step), `render-apply.md` (~2.8k: render, apply, `models pull`, the frontend client, secrets, the rendered tree, the release constants by module — the whole §7 bullet, the permission set included — the model records, the guardrail), `backup-restore.md` (~1.4k), `stack.md` (~0.9k: TLS, the registry mirror, alerting, reconcile and the audit writer), `tools.md` (~2.0k: the pin watch, the build tool, the acceptance and turn harnesses, their interpreters and footprints), `tests.md` (~0.8k: the repo-wide tripwires, the per-command pattern, the contract tier). Every leaf follows one template: a "Read this when" paragraph, the inventory table, the commands, the leaf-specific parts, the tests, and cross-references. Made by moving lines verbatim: the ledger (`.scratch/slice-1/assets/47-move-ledger.md`, from `git show fca53ac:docs/ARCHI.md`) classifies the pre-split file's 807 backticked and bold spans as map 238, map+leaf 313, leaf 234, shared 22 (cross-cutting names, each with its leaves), absent 0, and lists every correction.
- **The budget test** (`tests/test_archi_budget.py`): the count script's estimate reproduced in Python — five character categories with the linefeed as the record separator, records counted as awk reads them, the same rounding; 17,657 on the pre-split map, the script's own figure — holding the map under 14,000 and every `docs/archi/*.md` under 8,000; the set of leaves §2 links equal to the files under `docs/archi/` (a stray non-Markdown file red); every inline link in the map and the leaves without a scheme resolving from its file's directory, fenced blocks and code spans excluded from the scan. Pure rule functions returning findings with a fix, proven on synthetic trees, run once on the committed tree; no lock value in the module (`tests/test_lock_coupling.py` green).
- **The rules** (`docs/ARCHI-rules.md`, rewritten): the model with the stable-numbering invariant; the budgets and warnings (14k/12k, 8k/6k, `exempt: tooling`); routing (the plan names the leaves, implement reads the map plus those, release updates the leaves and the map wherever its own content changed — §2 and §12 only when a subsystem appears or a slice moves, §8's table when a command lands, a cross-cutting section when its fact moves; more than three leaves is a ticket to split); the leaf template; one home per fact; compact with `TRIP-compact <file>` in the release that crossed the warning, then split by mechanism; Step 7's checklist; the standing rules without their anecdotes; a dated History of the four compactions (2026-09-02 to 06, each with its commit's figures and the rule it produced) and this split.
- **The routing in the skills** (`docs/agents/tooling.md` §4, three rows): `archi-leaves-read` after the reading list of `TRIP-1-plan` (read the map, then the leaves the ticket's commands belong to by §8's table; name them in Documentation Impact) and of `TRIP-2-implement` (read the named leaves before the first batch; §8's table the fallback); `archi-map-update` in `TRIP-3-release` Step 7 (the named leaves and any the diff touched, the map wherever its own content changed, the count script on the map and every leaf, a file past its warning compacted per file in this release, the budget test before the commit question); the plan template's Documentation Impact line asks for the leaves and every map section whose content changes; `TRIP-test`'s contract line points at `docs/archi/tests.md`. `TRIP-compact`, `TRIP-research`, `TRIP-hotfix`, and the Codex templates untouched (no slot; a block there moves a lock hash); `tests/test_skills_vendored.py` green.
- **The rules in `CLAUDE.md`**: the architecture-memory paragraph names the map, the leaves, the routing rule, the budget test, and the rules doc; the lessons paragraph names the map or its leaf; the compaction paragraph says `TRIP-compact` runs on one file — the map or the leaf past its warning, passed as the invocation's argument, which overrides the skill's own target line.
- **Documentation sync**: `README.md`'s "Where things are" row names the map and the leaves; `docs/4-unit-tests/TESTING.md` points at the tests leaf for the contract list; the memo's status line says it is implemented here.
- **Tracker**: ticket 47's first five criteria ticked with the counts and the ledger summary in the close-out; the sixth's second half (ticket 06's plan naming its leaves) is checked in that cycle, which the `archi-leaves-read` block now requires; resolved.
- **Version**: bumped 0.1.13 → 0.1.14 (with `tests/test_version.py`); a patch — documentation and repository tooling only, no product code, no rendered artifact, no site-file or `/etc/gideon` contract change (§2.1); no acceptance run (patch tags never run it). Pre-General 0.x tag: no release note (§21's check begins at `v0.2.0`). No tutorial: a documentation and tooling release, its design already in the memo.
