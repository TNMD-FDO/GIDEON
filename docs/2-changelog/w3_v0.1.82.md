# Changelog - Week 3, 18-09-2026, V. 0.1.82

**Release Date**: Week 3, 18-09-2026 at 01:23
**Version**: 0.1.82 (previously 0.1.81)
**Object**: release: v0.1.82 — General's texts refer a user nowhere (slice-1 83)
**Code review**: `docs/3-code-review/CR_w3_v0.1.82.md` (Codex loop, 2 rounds -> APPROVED)

## Changes

This release implements slice-1 ticket 83 under the plan `docs/1-plans/general-texts-refer-nowhere.plan.md`, on the maintainer's ruling at slice-1 ticket 18's discovery (2026-09-17). Four release texts sent a user to "Research", a name ADR-0038 had already replaced with Legal. The ruling is that a chat's fixed text refers a user nowhere: General states what it does not do and declines, and "Legal" supersedes "Research" in every text a user reads, Research remaining the internal name of the service, the route, the suites, and the glossary's term.

- **General's preset** (`compose/open-webui/general.yaml`). The description ends at "and it verifies nothing."; the system prompt's Citations paragraph loses its two Research clauses and reads "…Whenever you mention one, say that you have not verified it. If asked to verify a citation, say that you cannot." The header names ADR-0043 beside §15 and [06] items 4–5.
- **The search-confirmation dialog** (`gideon/host/render/owui.py`). `WEB_SEARCH_CONFIRMATION_TEXT` loses its last sentence and ends at "Do not paste client or case material here."
- **The citation stamp** (`compose/open-webui/functions/citation_stamp.py`). `CITATION_STAMP` is the one-sentence label "General does not verify citations."; the Function's frontmatter moves to `version: 2`. No pattern, bound, or hook changed; the shorter label still matches no citation family.
- **The smoke seed** (`eval/seed/general/smoke.yaml`, append only). `identity-02`, `citation-02`, and `verify-02` supersede `identity-01`, `citation-01`, and `verify-01`: General names itself and no other chat, the citation answer carries the new label and not the old second sentence, and a verification request is declined with no destination named. The three predecessors stay byte for byte and the loader retires them; the README's list follows, and `tests/test_turns.py`'s smoke contract reads the successors (eleven live cases).
- **The release-note template** (`docs/release-notes/TEMPLATE.md`). The Coverage comment's fixed line is "General only; the Legal chat arrives at go-live on SCOTUS and the Sixth Circuit.", and from go-live it copies the Legal chat's description; the Rules of use sentence ends "…and until go-live there is no Legal chat to check it in." `Template: 2` stands, since no heading moved. `tests/test_release_notes.py`'s `PRACTICE_RULE` and its good-note fixtures follow.
- **The record.** ADR-0043 (`docs/adr/0043-a-chats-fixed-text-refers-a-user-nowhere.md`) supersedes §15's two fixed texts and the stamp's sentence and §21's rule wording; the spec is not edited. Dated comments on greenfield [06] and [33] point to it. `CONTEXT.md`'s *Privilege reminder*, *Pre-launch release*, and *Legal* entries move with it (via `/domain-modeling`). The engine-frontend leaf's citation-stamp row and the render-apply leaf's `render/owui.py` bullet cite ADR-0043.
- **Tests and fixtures.** `tests/test_citation_stamp.py` asserts the version and the label; the `example`, `second-office`, and `no-gpu` render fixtures were regenerated by `tests/regenerate_render_fixtures.py`, their diff exactly the plan's matrix. The gate is green: `ruff`, `mypy` (208 files), and the suite.
- **Proof** (`.scratch/slice-1/assets/83-on-box.txt`, 2026-09-18 00:31–01:05 CDT, from the worktree). `render --diff` named `open-webui` alone in its recreate row; `apply` recreated the frontend once and `apply-manifest` converged. The running container's `WEB_SEARCH_CONFIRMATION_CONTENT` is the two-sentence text, and the stamp's stored Function row carries `version: 2` and the new label, not the old second sentence. The smoke set ran under the turn harness with `--repeat 2 --stream`: 44 turns, the three successors green on both repeats, the predecessors retired, leak 0, misses 0, the stream clean 22 of 22.
- **Window**: the cycle's apply recreated `open-webui` once, with no user on the box (users arrive at go-live), and the ledger's §8 names no frontend announcement. The release's apply from the primary returns the four units' `WorkingDirectory` and recreates nothing. No rider: `pin-watch/images.open-webui` (#3) waits for the first patch tag after `v0.2.0`, as ruled at ticket 18's discovery. The box ledger is untouched.
- **Tutorial**: skipped. This is a patch tag and moves release text only; the rule is stated in ADR-0043.
- **Release note**: none. The §21 user-facing note is required from `v0.2.0`, whose note is written from this template.
- **The human read** (ADR-0036): skipped, a pre-General 0.x tag. The maintainer fixed the four texts' wording at the plan's discovery.
