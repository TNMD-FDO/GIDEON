# Changelog - Week 3, 16-09-2026, V. 0.1.73

**Release Date**: Week 3, 16-09-2026 at 19:05
**Version**: 0.1.73 (previously 0.1.72)
**Object**: release: v0.1.73 — the build box's crossing, procedure and proof (21)
**Code review**: `docs/3-code-review/CR_w3_v0.1.73.md` (Codex loop, multi-round -> APPROVED)

## Changes

This is the second cycle of slice-0 ticket 21, and it changes no code. The plan is `docs/1-plans/build-box-roles-on-the-box.plan.md`. The first cycle landed the build-box declaration at `v0.1.68`. What was left was proving on TNMD's box the crossing that ruling (f) describes, and recording it.

- **The runbook** (`docs/runbooks/install-upgrade.md` §1, the **Build box** paragraph): the old sentence said how TNMD's box would cross `v0.1.68`, but the box never followed that plan. The sentence is now two:
  - The procedure, for any host that already runs the three units when it crosses `v0.1.68`: `sudo ./upgrade.sh <tag>` (the three steps skipped, their units still running), then `sudo python3 -m gideon host provision --build-box` (the three `ok`), then `sudo python3 -m gideon apply`.
  - The past fact: TNMD's box was declared on 2026-09-16, at `v0.1.71`'s release, before any upgrade. The paragraph also says where the crossing's transcripts are kept.

  The paragraph states no outcome of the window, which runs after this tag.
- **The proof** runs after the merge, the push, the CI run, and the export, from the primary. The box is already declared, so the crossing is re-enacted:
  1. The marker is removed to leave the mode.
  2. The primary is detached at `v0.1.72`, so the upgrade crosses the checkout and takes the `pre-v0.1.73` set.
  3. `./upgrade.sh v0.1.73` runs, then the declaration (dry run first), then `apply`.
  4. A stop before the upgrade's checkout re-declares the box and returns the primary to `main`.
- **The record** is this release's outstanding-items `tracker:` commit: the redacted transcripts in the second section of `.scratch/slice-0/assets/21-on-box.txt`, the ticket's ticks and resolution, the box ledger's §12 entry after the §11 fetch, and the board. Until then this release commit leaves the ticket `claimed`.
- **Applies on the box** (prospective):
  - `render --diff` is read before each apply and compared with the plan's §4.
  - The upgrade's apply renders without the marker. The declaration's apply renders the host-unit rule and the runner's unit pattern back.
  - Each apply is expected to recreate `node-exporter` and `grafana` and nothing else. Any other row stops the window.
  - Both sets are in the observability tier and this is a pre-`v0.2.0` tag, so no user window is needed.
  - The tracker commit records the rows actually read and the outcome.
- **Release note**: none before `v0.2.0` (`tests/test_release_notes.py`). As ruled at `v0.1.68`, the runbook paragraph and this entry name the on-box commands.
- **Tutorial**: skipped. This is a patch tag, and it teaches no design decision; the modes are ADR-0035's, amended at `v0.1.68`.

**Step 7**: no structural change. The host, backup-restore, and render-apply leaves were read, and none of them changes.

**Window**: none needed (see the Applies bullet). There is no human read and no release note, since this is a pre-General 0.x tag. There is no acceptance run, since this is a patch tag. The box ledger's entry comes with the tracker commit, after the window.
