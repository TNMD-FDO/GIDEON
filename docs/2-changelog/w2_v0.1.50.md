# Changelog - Week 2, 10-09-2026, V. 0.1.50

**Release Date**: Week 2, 10-09-2026 at 14:40
**Version**: 0.1.50 (previously 0.1.49)
**Object**: the citation favicons held to the box (§15, §2.3, §19.4; ADR-0001, ADR-0017, ADR-0028; slice-1 ticket 72): the pinned frontend (Open WebUI v0.11.3) draws each stored source's icon as an image whose address is a third-party favicon service's with the source's full link as the query — the citations component's sources toggle and the two rows of the web-search results component — so every user's browser sent the fetched pages' addresses, with the workstation's address and browser identity, to a party neither SearXNG's engines nor the page's host, unseen by the box; the frontend's own `CONTENT_SECURITY_POLICY` setting is now rendered as release content in `owui_environment`'s frontend-wide switches with the value `img-src 'self' data: blob:`, which the pinned backend's security-headers middleware sets on every response, so the browser issues no image load to another origin and the sources render with their titles and links, the toggle showing the frontend's own logo; no other directive; the render test pins it on the GPU host, the no-GPU host, and the drill, the three fixtures move on it, the search-sentinel contract's key list and Compose file carry the name and one test asserts the header on the frontend's page against the rendered value, and the on-box proof drove ticket 65's searched turn before and after the apply — three off-host requests to the one service aborted by the launcher, then none — with the sources stored both times and a non-searched control unchanged
**Code review**: `docs/3-code-review/CR_w2_v0.1.50.md` (Codex loop, 1 round -> APPROVED)

## Changes

- **The rendered policy** (`gideon/host/render/owui.py`; the plan's Implementation Details 1): a release constant `CONTENT_SECURITY_POLICY` beside the loader's agent and the confirmation text, its comment naming the three call sites on the pinned frontend (the sources toggle, whose failed load falls back to the frontend's own logo — the LICENSE-covered element ADR-0003 leaves alone — and the results component's two rows, with no fallback), the backend's security-headers module reading the name from the environment and its pure-ASGI middleware setting the header on every response, computed once at start, what each source keyword admits (`'self'` the frontend's own files and uploads, `data:` the stored profile images and the forms plugin's inline backgrounds, `blob:` the composer's previews), that no `default-src` is set so every other directive is a later ruling, ADR-0017 `exempt: policy`, and the ingress header as the recorded fallback should a bump drop the setting; rendered as one entry in the frontend-wide switches block of `owui_environment` beside `ENABLE_VERSION_UPDATE_CHECK`, the block every frontend shape shares — the GPU host's Compose block, the no-GPU host's, and the drill's document built at drill time. No site key (ADR-0028), no other directive.
- **The render test and the fixtures** (`tests/test_render_owui.py`, `tests/fixtures/render/`; Implementation Details 2–3): the identity-and-policy test asserts the literal value beside the update-check assertion — the pin, so a drift of the constant is a deliberate edit of the test — and the every-host posture mapping gains the name and value, holding the GPU, no-GPU, and drill shapes; the three fixtures regenerated by the repo's script, the diff one environment line in each `compose.yaml` and two moved digests in each `manifest.yaml` (the frontend block's and the file's sha256; the top-level digest is hashed over the top-level sections alone and did not move — the plan review's first finding).
- **The contract's guard** (`tests/contract/search_sentinel.py`, `tests/contract/sentinel/compose.yaml`; Implementation Details 4): `FRONTEND_ENV_KEYS` gains `CONTENT_SECURITY_POLICY`, so a render that drops the value fails the stack's environment write before any container starts; the sentinel Compose file passes it through as `${CONTENT_SECURITY_POLICY}`; and a new test ordered before the five log runs, on the stack `setUpClass` brings up, makes one GET of `/` on the stack's loopback port with the standard library's HTTP client and asserts the `Content-Security-Policy` header equals the rendered value — derived from the render, never restated — so the job every frontend pin bump runs is red when the render or the image drops the setting. The product client's `Response` stays without headers by design. Run on the box from the worktree before the release: 6 tests, 200.7 s, OK — the value crossed Compose's `.env` interpolation intact (the plan named the equality as that risk's detector).
- **The proof** (`.scratch/slice-1/assets/72-on-box.txt`; on-box-proofs §1, §3, §6, §7, §9; Implementation Details 5): a deliberate daytime run with no users (discovery's ruling), the journal read first; ticket 65's driver as the test account through the browser-mode launcher — the before-run storing seven pages (49,747 characters) with the engine's four requests attributed and the launcher aborting three off-host requests to the one favicon service, the ticket's premise on the live stack; `render --diff` naming the frontend's block alone (`open-webui`); the apply from the worktree recreating the frontend by its changed block and verifying, the engine untouched; one HEAD request through the ingress reading `Content-Security-Policy: img-src 'self' data: blob:`; the after-run storing seven pages (70,870 characters) with the four requests attributed and **no off-host request at all** (`off_host=0`, `off_hostnames=()`; the driver's request log 399 lines, none aborted, none naming the service, the last `favicon` line the frontend's own logo loading from the box at 39.5 s as the sources toggle's fallback) — the policy-refused loads never issued by the renderer, so the launcher's route never met them and the plan's contingency (a console listener) was not needed; the non-searched control unchanged at 397 on-host requests; `render --diff` then `none`. Two facts recorded: the driver's inline-citation selector reads 0 on both runs while the screenshots show the answers' domain-name citation pills and the collapsed "7 Sources" toggle with three frontend-logo icons — the selector predates the pill markup (65 read 3 on 2026-09-09), a fact for the driver's next touch; and the icon loads fire from the sources toggle and the results rows whatever the inline count, so the plan's repeat rule for a zero-citation run was not triggered and `off_host` with the stored sources is the measure. The driver gained one write beside its `requests:` line — the launcher's request log to `requests.log` in the out directory, never committed — the cycle's one extension of it outside the contingency. The hostname and the service's name are withheld from the transcript under the ticket's ruling.
- **Documents** (Implementation Details 6): the map's §16 Network bullet gains the clause that the page's image loads are held to the box by the frontend's rendered policy, the citation UI's favicons the case; the render-apply leaf's `render/owui.py` constants bullet gains the image policy among the frontend-wide switches and its search-rig row's ticket list gains 72; the tests leaf's `search_sentinel.py` sentence gains the header assertion; `[06]` item 13's comment in ticket 29's pattern — item 13's "only runtime path" stands for the box, the page itself had a second egress from the workstation, closed by the rendered policy, §15 gaining the posture at its next touch; ticket 41's dated note for the frontend bump's read-back was written at the triage; `CONTEXT.md` unchanged (the Search egress entry describes the box's path; no new term); no ADR (§15: every General decision is a toggle or a config line); no release note before `v0.2.0`; ticket 72 resolved. The render-apply leaf was split at `v0.1.48`, so the compaction the ticket's ruling (e) named is not owed: the leaf stands at ~3,650 tokens after this release's clause, the tests leaf at ~1,790, the map at ~11,380; `docs/agents/on-box-proofs.md` stands at 6,056, past its 6,000 warning since `v0.1.49` named it for `TRIP-compact`, the person's command, untouched by this release; the glossary at 15,211, reported and never budgeted. The budget test holds every cap.
- **This release**: `main` took the plan's number at `v0.1.49` while the cycle ran, so Step 2 ran `python3 -m tools.renumber 0.1.49 0.1.50` — one line rewritten in one file, none renamed — before the rebase, which met no conflict; the version and its documents were written once against the tip. Codex's plan review two rounds (the top-level digest does not move; the citation count recorded, not compared); its code review one round, no findings. Lint, types, and the affected tests green on the rebased tree.

**Window**: the frontend alone, recreated by the proof's apply from the worktree by its changed Compose block, free with no users on the box before `v0.2.0`; Caddy, SearXNG, Postgres, and the engine untouched; the release's apply from the primary moves the units' `WorkingDirectory` alone. Riders: pull request #3 (`images.open-webui`, the digest rebuilt) recreates the frontend and stays **deferred** to ticket 41's cycle with the browser-mode re-proof, by 41's 2026-09-08 ruling — a comment on the pull request after the push says so; #4 is the engine's and not this apply's service; #5 touches nothing on the box; no open ticket carries a change this apply could have taken. No release note and no human read: a pre-General 0.x tag. The box ledger is untouched: the frontend is GIDEON's own service and no shared thing moved.
