# Changelog - Week 2, 10-09-2026, V. 0.1.48

**Release Date**: Week 2, 10-09-2026 at 13:33
**Version**: 0.1.48 (previously 0.1.47)
**Object**: the render-apply leaf split by what grew (workflow ticket 25; `docs/ARCHI-rules.md`'s compact-or-split rule): `docs/archi/render-apply.md`, past its 6,000 warning a third time at 6,316 tokens, keeps the host tree — render, apply, `secrets rotate`, `models pull`, the rendered tree, secrets and their rotation, the search rig's rendered files, the memory limits, the render modules' constants — at 3,590, and the new leaf `docs/archi/engine-frontend.md` takes the engine and the frontend's runtime — the engine service and the frontend's connection, the two model records, the frontend client and the managed turn, the two Filters and the trip's row, `engine verify` and its sample — at 2,976, each made from the leaf template by moving lines whole, the 276 backticked and bold spans of the pre-split file ledgered with none absent; the map indexes the new leaf in §2, routes its commands in §8, names it in §12's landed rows, and repoints §13, §14, and §16; the five other leaves' Cites bullets and two inline links, the glossary's 23 `_Leaf_:` lines, the rules' compact-or-split example — the seam named at the crossing by what grew, in place of the `render.md`/`apply.md` seam drawn when the leaf stood at 2.8k — with its History line, and the memo's §1 row follow
**Code review**: `docs/3-code-review/CR_w2_v0.1.48.md` (Codex loop, 2 rounds -> APPROVED)

## Changes

- **The two leaves** (`docs/archi/render-apply.md` modified, `docs/archi/engine-frontend.md` new; the plan's Implementation Details 1 and 2): the pre-split file's inventory rows for the engine service, the frontend's connection, the two model records, the arithmetic guardrail, the citation stamp, and `engine verify`; its Commands bullets for the frontend client and the managed turn, the two Filters and the trip's row, and `engine verify`; its constants lines for `engine.py`, `enginesample.py`, and the two Filter modules; and its Tests lines for `test_arithmetic_guardrail.py`, `test_citation_stamp.py`, `test_engine_verify.py`, `test_owuiturn.py`, and `test_enginesample.py` — sixteen lines, each carried byte for byte and in order into the new leaf under the template's headings, verified against `main`'s copy by a scratchpad check. The only new prose is the template's: the new leaf's H1 and Read-this-when paragraph, its constants part's one-line citation of the old leaf's rule, its Cross-references (Cites `host.md`, `render-apply.md`, `backup-restore.md` — the review's one plan finding, since `engine verify` gates install, upgrade, and rollback — `stack.md`, `tests.md`; Cited by §§2, 8, 12, 13, 14, 16), and the old leaf's trimmed Read-this-when with its pointer, its Cites gaining the new leaf, its Cited by without §14. The render modules' constants lines (`render/owui.py`'s included) and the two contract modules' line stay with the host tree; the search rig's row keeps `host/owuiturn.py` in its code column.
- **The pointers** (`docs/ARCHI.md`; `docs/archi/{backup-restore,stack,tools,tests}.md`; `CONTEXT.md`): the map's §2 gains the new leaf's row after `render-apply.md`'s, whose Covers column now names the search rig's rendered files and the memory limits and whose Commands column drops `engine verify` and the frontend client; §8's table splits the row, `engine verify`, the frontend client, and the two Filters routed to the new leaf; §12's two landed rows name both halves; §13's `engine verify` label, §14's Filters clause, and §16's hidden-base-model bullet point at the new leaf, and §16's key-material bullet names each half for what it owns; the map 11,196 → 11,321, under its 12,000 warning. `stack.md`'s trip-writer clause and `tools.md`'s managed-turn clause link the new leaf; the Cites bullets of `backup-restore.md` (the gate), `stack.md`, `tools.md` (keeping `render-apply.md` for the render fixtures its bump regenerates), and `tests.md` gain it; `host.md` is unchanged, depending on the host tree alone. The glossary's 23 `_Leaf_:` lines — General, Generator, Engine, Task model, Model record, Preset, Built-in tools, Privilege reminder, Machine identity, Engine verification, Citation stamp, the eleven Guardrails entries, Managed turn — retarget to the new leaf through `/domain-modeling`, the diff exactly those 23 line pairs; the twelve entries of the host tree stay. `docs/agents/on-box-proofs.md` §6 and the search sentinel's comment name the old leaf for the recreate rule and the apply-manifest stage, both of which stay, so neither moves.
- **The rules and the memo** (`docs/ARCHI-rules.md`, `docs/6-memo/archi-map-and-leaves.md` §1): the compact-or-split sentence's first example is this split — `render-apply.md` split by what grew into the host tree and the engine and the frontend's runtime, the `tools.md` example kept — and gains the rule's clause: the seam is named at the crossing by what grew, since a seam named in advance is drawn against the leaf as it stood then (the `render.md`/`apply.md` seam was drawn at slice-1 ticket 47 when the leaf stood at 2.8k and the guardrail was one row, and would have landed the runtime's ~1,750 tokens of Commands text whole in the half that crossed first). A dated History line records the split with its counts, the ledger's figures, the corrections, and the lesson; the memo's §1 table gains the split's row, the map's count beside it. The `Budgets:` line and every other rule are untouched; `tests/test_archi_budget.py` needs no change and holds the seventh leaf's index and every link.
- **The proof** (`.scratch/workflow/assets/25-move-ledger.md`, slice-1 ticket 47's shape): every backticked and bold span of `main`'s `9c502e2` copy of the leaf, 276 deduplicated in document order, classified by whole-string presence in the two halves — kept 160, moved 78, shared 38 (each with its reason: a template line, a module both halves name, a short span inside a longer one), absent none; the corrections section names the two Read-this-when paragraphs, the constants citation, the Cross-references, and every link retargeted outside the halves. The script is the scratchpad's, reproducible from the header.
- **The counts at this release**, by the count script: `render-apply.md` 3,590 and `engine-frontend.md` 2,976 (from 6,316), the other leaves 1,555 to 5,667 (`tools.md`), the map 11,321, `CLAUDE.md` 2,919, the agent documents 329 to 5,893 (`on-box-proofs.md`), the glossary 15,188 reported and never budgeted; every budgeted file under its warning, the first release since `v0.1.45` with none past one.
- **This release**: the plan's number stood; the one cycle in flight (workflow ticket 20's, also at plan number `0.1.48`) touches neither the leaf nor the map, and `main` did not move between the claim and the tag. No code, no test change beyond the version pin; lint, types, and 1,366 tests green.

**Window**: none — nothing on the box changes; no apply. No release note and no human read: a pre-General 0.x tag. The box ledger's fetched copy lags ours — rules version 2026-09-09.5 against our .6, our §12 entries through `v0.1.45` absent there, nothing newer of Transcribe's — so nothing merges; no shared thing is touched and no entry is added.
