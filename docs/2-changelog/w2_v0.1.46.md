# Changelog - Week 2, 10-09-2026, V. 0.1.46

**Release Date**: Week 2, 10-09-2026 at 10:59
**Version**: 0.1.46 (previously 0.1.45)
**Object**: a budget for `CLAUDE.md` and the agent documents (workflow ticket 24; ADR-0017): `tests/test_archi_budget.py` holds `CLAUDE.md` to a 5,000-token cap with a 4,000 warning and every document under `docs/agents/` to a leaf's 8,000 and 6,000, the values corrected against the compactions' returns on workflow tickets 22 and 23 before they were written, the four classes' figures stated once on a machine-readable `Budgets:` line in `docs/ARCHI-rules.md` that the test reads and the release block and `CLAUDE.md` point at, and Step 7's count covering the two classes
**Code review**: `docs/3-code-review/CR_w2_v0.1.46.md` (Codex loop, 2 rounds -> APPROVED)

## Changes

- **The values, from the returns** (the ticket's claim line; ADR-0017): `CLAUDE.md` gets a 4,000-token warning and a 5,000-token cap, corrected down from the leaf's 6,000 and 8,000 — ticket 22's return is ~2,811 tokens after a 36 % compaction from ~4,417, the size workflow ticket 19's triage flagged as paid on every turn of every session, so the warning sits just under that size and the 1,000 gap absorbs the file's 23-token locale variance and about ten releases at the ~100 tokens a release it grew before the compaction. An agent document gets the leaf's 6,000 warning and 8,000 cap, confirmed rather than corrected: ticket 23's returns are 5,656, 5,942, and 4,966 tokens, each worked to under 6,000 with no split, the 2,000 gap absorbing the 26–49 token locale variance and a dense document's ~5 % return; `on-box-proofs.md` at 58 under weighs its named split at its next crossing.
- **One home for the figures** (`docs/ARCHI-rules.md`, Budgets and warnings): one `Budgets:` line under the section's heading, after a one-line HTML comment in `tooling.md` §4's ceiling-line shape, names the four classes — the map, a leaf, `CLAUDE.md`, an agent document — with a cap and a warning each; the prose around it says why `CLAUDE.md`'s class is the tightest and names the correction's source, restating no figure. The compact-or-split rule covers the four classes: a leaf splits by mechanism, `CLAUDE.md` moves a section's detail whole to a document under `docs/agents/` keeping the rule and a pointer (ticket 22's shape), an agent document splits by mechanism as a leaf does, `on-box-proofs.md`'s named split the standing example. The Step 7 checklist counts the four classes with the glossary's count beside them; one History line.
- **The test** (`tests/test_archi_budget.py`; 4 new tests): the `MAP_CAP` and `LEAF_CAP` constants gone; a reader over the rules document's Budgets section, in `test_skills_vendored.py`'s ceiling-reader shape — exactly one `Budgets:` line under the heading, four clauses in the fixed order, an integer pair per clause with thousands separators, a finding naming the fix otherwise, and a finding for a warning at or above its cap. `rule_budget` then holds the map and the leaves from the parsed values, `CLAUDE.md` when present at the root, and every `docs/agents/*.md` when the directory exists, each over its cap a finding whose fix names the compaction and the move or the split. The module states no figure: the temporary-root cases seed a rules document of visibly fictitious values through one helper and size their oversized fixtures from the parsed values; the new cases are `CLAUDE.md` over its cap, an agent document over its cap, a missing, doubled, malformed, or out-of-section line as one finding (the last the code review's one finding, a Minor, fixed in round two), and a warning at its cap.
- **Step 7's count** (the `archi-map-update` block in `TRIP-3-release/SKILL.md`, 1,149 → 1,182 of its 1,500 bytes; `docs/agents/tooling.md`'s row for the block): the count command gains `CLAUDE.md docs/agents/*.md`, the two parentheticals of figures become a pointer to the `Budgets:` line, and a file that cannot come under its warning moves or splits per the rules. `CLAUDE.md`'s Lessons section gains the one sentence — the two classes have a budget, so a lesson that would carry its document past the warning is a compaction or a split in the same change — and its Compaction clause drops its two figures for the same pointer. The tests leaf's tripwire line names the four classes and the line.
- **The counts at this release**, by the count script: `CLAUDE.md` 2,877; the agent documents 329 to 5,893 (`on-box-proofs.md`), `tooling.md` 5,667; the map 11,160; the leaves 1,544 to 6,316 — `docs/archi/render-apply.md` past its 6,000 warning, grown from 5,834 at `v0.1.40`'s compaction 7 by six releases' rows, a crossing that predates this cycle (the outstanding-items step's record says how it is handled).

**Window**: none — nothing applies on the box. No release note and no human read: a pre-General 0.x tag (the release note and ADR-0036's read start at `v0.2.0`).
