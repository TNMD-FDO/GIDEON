# Changelog - Week 2, 10-09-2026, V. 0.1.49

**Release Date**: Week 2, 10-09-2026 at 14:25
**Version**: 0.1.49 (previously 0.1.48)
**Object**: the cycles record gains turns beside the peak and the rule that follows (ADR-0017, §19.4; workflow ticket 20): `tools/cycles.py` carries `SEAM_TURNS = 150` beside `EARLY_WARNING`, a starting value from the record corrected there and never by argument, and the band table carries median turns, maximum turns, and the count above it; `CLAUDE.md`'s Ticket sizing states the mechanism — every turn re-sends the whole conversation, so a session's cost is its size times its turns and grew as `turns^1.62` over the record — and the rule that a session past 150 turns ends at its next seam (a batch checkpoint, the testing gate's summary, a review round's verdict, an on-box proof's asset written, a phase boundary) and a fresh session enters the worktree by path, the count read with `python3 -m tools.cycles --latest`, one line; the tool reads every harness store on the machine in one run — the default projects directory and the configuration directory's, a session present in two stores one row — and each session's row carries `subagent tokens in`, read from the nested `subagents/*.jsonl` transcripts for usage alone, folded by message id on the three context fields and included in no other column; the parser accepts the previous record shape, a kept row carrying a blank figure; the record regenerated over both stores — 191 rows, 52 nested files — byte-stable on a second write; the tools leaf's three rows, the glossary's Peak context entry, the outstanding-items chain's one write, the map's §8 row; the cycle itself ran as three sessions split at seams — plan, implement, release — the record's rows showing it; the plan's number `0.1.48` taken by `main` while the cycle ran, renumbered by `python3 -m tools.renumber` before the rebase (workflow ticket 20, resolved)
**Code review**: `docs/3-code-review/CR_w2_v0.1.49.md` (Codex loop, 1 round -> APPROVED)

## Changes

- **The constant and the band columns** (`tools/cycles.py`; the plan's Implementation Details 1): `SEAM_TURNS = 150` beside `EARLY_WARNING`, each with the comment naming `CLAUDE.md`'s Ticket sizing paragraph as the figure's home; `BAND_COLUMNS` gains `median turns`, `max turns`, and `above 150 turns` between the peak columns and `compactions`, the header stated once and rendered in both outputs; `Band` and `_band` compute the three over the sessions' turns as the peak figures are computed, the count strictly greater. Over the regenerated record the cycle bands read: slice 0 a median of 126 turns and 7 sessions past 150, slice 1 a median of 110 and 10 past, the workflow effort a median of 69 and none past.
- **Every store, one run** (Implementation Details 2): `store_directories` returns the default projects directory and the configuration directory's when `CLAUDE_CONFIG_DIR` names another, deduplicated after resolution, or the one store `--projects` names; `_family_directories_by_store` walks each, the primary's directory before the worktrees' within a store; `read_family` reads them all, the candidate fold by session id making a session present in two stores one row (the later last timestamp winning, the duplicate counted); the no-store refusal fires only when no store holds the family and names every store looked in; the report prints one `stores read:` line per store. The regenerated record read 326 transcripts across the two stores, 191 with usage, 0 duplicates — the second store's sessions, absent from every earlier write, now in it.
- **The subagent figure** (Implementation Details 3): `read_subagents` walks `<dir>/<id>/subagents/*.jsonl` for the top-level transcript, reads assistant usage alone through the existing `_usage` guard, and folds by message id across the set on the three context fields alone — the harness writes a subagent's response as several entries whose `output_tokens` grows while the context fields stay equal — a repeat whose context differs being the malformed case; a malformed or unreadable nested file is skipped and listed, the parent's row never dropped; a symbolic link (the harness links one subagent's transcript into a second session's directory) is passed over, the session holding the regular file owning the figure. `Session.subagent_tokens_in` is the sum, rendered as `subagent tokens in` after `tokens out` and included in no other column, so `tokens in` and `turns` stay the session's own; the record's preamble and measure paragraph say so; the report counts the nested files read and skipped (52 read, 0 skipped at this write). Workflow ticket 06's ruling (g), which excluded the nested transcripts, is superseded by the ticket's own: the top-level file's own `isSidechain` lines stay excluded.
- **Both record shapes, and `--latest`** (Implementation Details 3 and 4): `_parse_sessions` accepts `SESSION_COLUMNS` and `PREVIOUS_SESSION_COLUMNS` (the tuple without the new column, kept with a comment naming the ticket), reads cells by column name from the header the table was parsed under, and gives a kept row from the older shape a blank figure; `merge_rows` is unchanged. `--latest`, mutually exclusive with `--write`, prints one line — the newest present session's short id, where, skill, turns against `SEAM_TURNS`, and peak against `EARLY_WARNING` — and refuses with its fix when no transcript carries usage.
- **The tests** (`tests/test_cycles.py`; Implementation Details 5): the fixture's nested transcript becomes a counted one with the real repeat shape, a second nested file, and a malformed one beside them; a second store holding a copy of one session and one of its own, for discovery over both, the one-row rule, the stores line, `--projects` alone, and the refusal naming both; the bands' turn columns and the strictly-greater count; both header shapes through a write, the kept rows surviving, byte-stability over the new shape; `--latest` and its refusal, `--latest --write` an argparse error; `EarlyWarning` reading `150 turns` from the Ticket sizing section as it reads the peak's figure. Eight new tests; the module and its test held to the standard library by the existing AST check.
- **The rule and the documents** (`CLAUDE.md`, `docs/archi/tools.md`, `CONTEXT.md`, `docs/agents/outstanding-items.md`, `docs/ARCHI.md`; Implementation Details 6 and 7): the Ticket sizing paragraph keeps its first two sentences and the peak rule — 800k still its first number, so the peak test keeps reading it — and gains the mechanism in one sentence, the rule, the five seams, how a fresh session continues (the worktree entered by path as `TRIP-2-implement`'s Step 0 provides, the plan's ticked checkboxes and the staged tree, the Codex thread resumed by the plan's path), the count command, and `/to-tickets` reading the turn columns beside the peak; `TRIP-2-implement` takes no block. The tools leaf's inventory row, Commands bullet, and Tests line carry the stores, the nested reads, the two constants, the four refusals, and `--latest`; the glossary's **Peak context** entry, through `/domain-modeling`, says turns are the cost's measure beside the peak — the peak predicts compaction, the turns predict spend — its `_Leaf_:` line kept; the outstanding-items chain's per-store sentence (`v0.1.47`'s) becomes one write reading every store; the map's §8 tools row gains `[--latest]`, nothing else in the map moving. `docs/agents/on-box-proofs.md` §1 carries a lesson the implement session met: the detached shape serves every `codex-implement` batch too, and the exit line is printed after a newline of its own, since Codex's report ends without one.
- **The demonstration**: the cycle ran as three sessions split at phase boundaries — the plan session (`beef9584`, 23 turns) ended at the plan review's approval, a fresh implement session (`0b389a8c`, 52 turns) ended at the code review's approval, and a fresh release session (`9e1485df`) entered the worktree by path — the record's three rows for `worktree a-session-past-150-turns-ends-at-a-seam`. None reached 150 turns, so the mid-phase seam was not exercised. `--latest` from the release session named a concurrent `TRIP-1-plan` session's row, whose last timestamp was later — the flag reads the machine's newest session, not the caller's — recorded at the outstanding-items step.
- **The counts at this release**, by the count script: the map 11,324; the leaves 1,555 to 5,960 (`tools.md`, from 5,667, under its 6,000 warning by 40); `CLAUDE.md` 3,100 (from 2,919); the agent documents 329 to 6,056 — `docs/agents/on-box-proofs.md`, at 5,989 on `main` after `v0.1.48`'s tracker commit, crosses its 6,000 warning with this release's 67-token lesson and is named for `TRIP-compact`, the person's command, at the commit question; the glossary 15,211 reported and never budgeted. The budget test holds every cap.
- **This release**: `main` took the plan's number at `v0.1.48` while the cycle ran, so Step 2 ran `python3 -m tools.renumber 0.1.48 0.1.49` — four lines rewritten in two files, none renamed — before the rebase, whose one conflict was the cycles record, regenerated whole after the code review was written so its plans column reads `0.1.49`; the version and its documents were written once against the tip. Lint, types, and 1,374 tests green on the rebased tree.

**Window**: none — nothing on the box changes; no apply. No release note and no human read: a pre-General 0.x tag. The box ledger's fetched copy is the one `v0.1.48` found — rules version 2026-09-09.5 against our .6, our §12 entries through `v0.1.45` absent there, nothing newer of Transcribe's — so nothing merges; no shared thing is touched and no entry is added.
