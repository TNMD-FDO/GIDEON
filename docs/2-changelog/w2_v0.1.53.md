# Changelog - Week 2, 10-09-2026, V. 0.1.53

**Release Date**: Week 2, 10-09-2026 at 22:06
**Version**: 0.1.53 (previously 0.1.52)
**Object**: the export boundary — what the public repository receives at a tag, and the tests that hold it (§2.6, §2.1–2.2, §0.1 rule 2; ADR-0004, ADR-0031, ADR-0033, ADR-0035; slice-1 ticket 56): `tools/exportboundary.py` states the twelve excluded prefixes once — the tracker, the handoffs, the teaching workspace, the four TRIP working directories, the vendored skills, the eval harvest, the two box workflows, Dependabot's file — mirrored as `export-ignore` lines in `.gitattributes` and held equal by the new `tests/test_export_boundary.py`, which also archives `HEAD` under the working tree's attributes and finds no excluded path, forbids every product, compose, config, workflow, and tool string naming an excluded prefix (the tracker's own tools exempt by name) and every kept document's inline link into one; the six operator runbooks move from the tracker to `docs/runbooks/` with every reference retargeted (the README as links, the map, `CONTRIBUTING.md`, sixteen Grafana annotations and their fixtures, the pin watch's texts) and the office's OU name dropped; the four lock refusals and the package docstring name the host leaf and the map; twelve tests skip in an export tree by `in_export_tree`; `ci.yml`'s three box jobs run only where the repository is private; the office-values tripwire learns that a workflow's Actions event expression is not a hostname; the export of the staged tree — 542 files — passes `ruff`, `mypy`, and `pytest`
**Code review**: `docs/3-code-review/CR_w2_v0.1.53.md` (Codex loop, 1 round -> APPROVED)

## Changes

- **The boundary** (`tools/exportboundary.py`, `.gitattributes`; the plan's Implementation Details 1–2): `EXCLUDED_PREFIXES`, twelve repository-relative prefixes with the reason each is excluded — `.scratch`, `docs/handoffs`, `docs/teach`, `docs/1-plans`, `docs/3-code-review`, `docs/5-tuto`, `docs/6-memo`, `.claude/skills`, `eval/seed/prototype-qa`, the two box workflows, `.github/dependabot.yml` — with `is_excluded(path)` (the prefix or under it, by segment) and `in_export_tree(root)` (none of the prefixes present); `.gitattributes` carries one `export-ignore` line per prefix under one comment, directories with a trailing slash, ticket 61's Dependabot line moved into the group. The module lives under `tools/` because `tests/` is not a package and four test modules import the helper (ruled at the claim).
- **The boundary test** (`tests/test_export_boundary.py`; Implementation Details 3): four rules in the evidence-hygiene and budget tests' shape, findings as `<path>:<line>: <what>. Fix: <how>` — every prefix has its attribute line and every attribute line names a prefix; an archive of `HEAD` under the working tree's attributes (`--worktree-attributes`, read with `tarfile`) carries no excluded member, the committed case skipping in an export tree and the seeded case a real temporary repository; no text under `gideon/`, `compose/`, `config/`, `ci.yml`, or `tools/` names an excluded prefix as a token, `tools/tracker.py`, `tools/cycles.py`, `tools/renumber.py`, `tools/pinwatch/skills.py`, and the list itself exempt by name with the reason; no inline link in a kept markdown file resolves into one, the budget test's link finder lifted with newlines kept inside multi-line code spans so the line numbers hold. One committed-tree test per rule, a seeded tree per rule, and the helper proven both ways and on `ROOT`; 9 tests.
- **The skips** (`tests/test_skills_vendored.py`, `tests/test_toolchain_pins.py`, `tests/test_tracker.py`; Implementation Details 4): the vendored-skills tripwire's committed-checkout check and its nine rule tests that copy `.claude/skills/` skip through one guard; the toolchain-pins gate-documents rule reads its list through `gate_documents(root)`, the three skill documents dropped in an export tree and `TESTING.md` kept; the tracker's real-tree test skips. Every skip is `in_export_tree`, never the absence of `.git`; on this checkout nothing skips.
- **The product strings** (`gideon/host/{lock,models,images,egress}.py`, `gideon/__init__.py`; Implementation Details 5): the four lock refusals end `consult docs/archi/host.md.` (the memory table's fix loses its plan and keeps the leaf), the docstring names the development repository and the map with no tracker path; `tests/test_preflight.py`'s two assertions and `tests/test_models_lock.py`'s one follow.
- **The runbooks' home** (`docs/runbooks/`; Implementation Details 6–7): six `git mv` renames — `office-services-setup.md`, `backup-restore.md`, `observability.md`, `install-upgrade.md`, `pin-watch-app-setup.md`, `built-images.md` — their sibling links and bare-name references retargeted, the research link shortened, the two links to slice-0 ticket 22 made text, and the office-services document's "TNMD's are under `OU=…`" clause dropped (the observation struck at the claim). Retargeted outside them: the README's sentence and five table rows as inline links, `CONTRIBUTING.md`, the map's §17, the four Grafana alert templates' sixteen `runbook:` annotations (`docs/runbooks/observability.md §4`) with the three fixture trees regenerated and `tests/test_render_grafana.py`'s assertion, `tools/pinwatch/pr.py`'s three texts and `tests/test_pinwatch.py`'s assertion, and the open tickets 41, 57, 74 and slice-0 ticket 11's criterion. Kept as dated records: the two transcripts, the move ledger, the eight kept changelogs' references, and the resolved slice-0 tickets' notes.
- **The kept documents' links** (`docs/ARCHI.md` §1 and §5, `docs/4-unit-tests/TESTING.md`, `README.md`; Implementation Details 8): the seven inline links into the boundary — the spec, the greenfield issues, the TRIP-init handoff, slice-0 ticket 10 and the lock-coupled memo, the two excluded workflows — are backticked full paths, the sentences otherwise unchanged.
- **The fence** (`.github/workflows/ci.yml`; Implementation Details 9): `mirror-images`, `frontend-contract`, and `search-sentinel` run on `github.event_name == 'push'` and the event payload's repository `private` flag together, one comment clause saying why; the proof is this release's own run on this repository.
- **The office-values tripwire** (`tests/test_office_values.py`): the fence's expression ends in `private`, a private-use suffix, so a dotted token opening `github.event.` in a file under `.github/workflows/` is an Actions expression and never a hostname — a contextual rule, not the token allowlist the first batch wrote — seeded both ways (the token in a workflow silent, in a markdown file a finding), the token built by the seed helper because the tripwire reads the module's own literals.
- **The proof** (the ticket's first criterion, its 2026-09-10 comment): the staged tree archived under the working tree's attributes and extracted to an empty directory — 542 files, no excluded prefix — where `ruff check .`, `mypy gideon tests tools`, and `pytest` pass: 1,396 passed, 12 skipped by the helper (the ten vendored-skills tests, the tracker's real-tree test, the archive rule). On the checkout 1,408 passed, nothing skipped.
- **Documents** (Implementation Details 10): the map's §4 tree (`runbooks/`, `exportboundary.py`), §16's Evidence bullet (the public repository a filtered export, its list, its mirror, its test, the skip rule), §17's runbook paths; `docs/archi/tests.md` (the tripwire's line, the three skips, the Actions-expression clause on the office-values line), `docs/archi/tools.md` (the module in the read-this-when line, an inventory row, the box paragraph), `docs/archi/host.md` (the memory table's errors name the leaf); the map at ~11,557 tokens against its 12,000 warning.
- **This release**: the plan's Codex review took three rounds (rules b, c, and i also copy the skills; the models-lock assertion; the kept changelogs; the host leaf; the grep's pattern) and the code review one. Codex implemented in two batches, the reviewer's corrections between them: a link turned to text keeps the full path; the tripwire exemption is contextual; the move ledger and the transcripts are records. Five ticket-body corrections recorded on the claim's comment — three box jobs, the `tools/` criterion amended for the tracker's tools, the docstring's path, the transcripts, the changelogs — and four rulings: the helper's home under `tools/`, the runbooks' short names, the `OU=` name dropped, the tracker's tools exempt by name.

**Window**: no apply and nothing recreated in this cycle. The Grafana alert templates' annotations changed, so a box's next `apply` recreates Grafana by its changed rendered files — the observability tier, which no user's turn passes through; no rider. No release note and no human read: a pre-General 0.x tag. The box ledger: no shared thing touched; the runner's labels stay in `ci.yml`, the fence changes which repository runs the jobs; the §11 fetch found Transcribe's copy at rules version 2026-09-09.5 with no line ours lacks, so nothing merged and no entry added.
