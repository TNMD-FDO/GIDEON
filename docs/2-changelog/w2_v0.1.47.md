# Changelog - Week 2, 10-09-2026, V. 0.1.47

**Release Date**: Week 2, 10-09-2026 at 11:32
**Version**: 0.1.47 (previously 0.1.46)
**Object**: the release renumbers by one command when `main` took its number, and the cycles record reads the version a release took (§2.1, §19.4; workflow ticket 17): `python3 -m tools.renumber <old> <new>`, a committed standard-library tool run in the cycle's worktree before every rebase onto a moved `main` — at `TRIP-3-release`'s Step 2 from the plan's number, before the tag from the Step 2 number — renames the release files, substitutes the old version in the cycle's own lines alone, and moves the previous clause and the changelog table's bumped bullet to `main`'s version, so the renamed files meet no add/add conflict and the shared tables' rows stay a rebase's ordinary merge; `tools.cycles` reads a plan's released version from the code review whose `**Plan**:` line names it; the `version-bump-rules` block branches on `main`'s version, the `worktree-merge` block becomes a pointer to `docs/agents/worktrees.md` §8, which carries the merge procedure whole; proven over `v0.1.41`'s reconstructed shape and used first on this release itself, whose plan's number `main` took while the cycle ran
**Code review**: `docs/3-code-review/CR_w2_v0.1.47.md` (Codex loop, 4 rounds -> APPROVED)

## Changes

- **The tool** (`tools/renumber.py`, `python3 -m tools.renumber <old> <new> [--dry-run] [--root DIR]`; the plan's Solution Architecture item 1): the file set is git's — the branch's diff against the merge-base with `main` and the tree's changed and untracked paths, `docs/1-plans/` excluded whole; the changelog, the code review, the tutorial, and the release note named by the old version are renamed on disk; in every file the cycle's own lines — never a line the merge-base's copy carries — have the old version's bounded token replaced by the new, an occurrence inside a plan file name kept since the plan is never renamed; then `previously <base>` and `bumped <base>` move to `main`'s version, both read from `gideon/__init__.py` at the merge-base and at `main`. Two forms: the *renumber* (`new` above `old`) and the *refresh* (the same number twice — a patch landed under a minor candidate — moving the two clauses alone). Four refusals before any write: the new number below the old, not above `main`'s, a rename target present, the old number a tag the merge-base reaches (the branch then carries `main`'s release of it: by hand). The report is renames, `path:line: n replacement(s)`, skips, and totals, never a line's text; a second run changes nothing; the tool stages and commits nothing.
- **The cycles record** (`tools/cycles.py`): `PLAN_PATTERN` keeps the plan's file name beside its version (`Session.plan_files`); `review_versions` maps each plan a code review's `**Plan**:` line names to the version in the review's file name, two reviews naming one plan a refusal with its fix; at render a present row's plans column reads the mapped version and the file name's number when no review names the plan (the review-posture plan today), a kept row passing its stored versions through; the effort attribution and the bands keep the file name's number, so no band moves; the record's measure paragraph says so.
- **The skill and the documents**: `version-bump-rules` reads `main`'s version when the ancestry check is false — unchanged, rebase alone; at or above the plan's number, the tip's next and the tool from the plan's number, then the rebase; below it, the plan's number stands — and says the plan's number is the claim's candidate, two plans may share it, the file is never renamed (1,120 bytes). `worktree-merge` is a summary and a pointer (1,173 bytes): `docs/agents/worktrees.md` gains §8, the merge procedure whole with the renumber-then-rebase order at the commit question and before the tag, the shared tables' content merge, and the late fast-forward recovery re-entering the worktree — the evidence commit dropped first at a minor tag, the tag re-made under its new name, the acceptance re-run — the review's four rounds having tightened that case; §4 gains the order and the header sentence points at §8; `docs/agents/tooling.md` §4's two rows and its pointer-documents sentence; CLAUDE.md's "Two sessions, one checkout" one sentence; the README's table gains the tool's row and the cycles row its plans-column clause; the map's §2 leaf row, §4 tree line, and §8 tools row; the tools leaf's inventory row, tooling paragraph, Commands bullets (the renumber's, the cycles record's plans column), and Tests lines.
- **Tests** (`tests/test_renumber.py`, 10 new; `tests/test_cycles.py`, 4 new): over a real temporary repository — the four renames, the own lines rewritten and the inherited line untouched, the plan file and the plan path in the review untouched, the two clauses moved, the report's lines, idempotence, the refresh and its no-change case, `--dry-run` and the four refusals writing nothing, the argparse error, the undecodable skip, and the AST check holding the tool to the standard library; the review map over a synthetic reviews directory with its two-reviews refusal, the mapped row without a band move, the kept row's pass-through.
- **The proof** (`.scratch/workflow/assets/17-renumber-proof.txt`): a throwaway clone with `main` forced to `v0.1.40` and a branch from `v0.1.39` carrying `v0.1.41`'s release commit as it stood before its rebase (the seven shared files three-way merged, the release files left untracked as at `v0.1.37`); one run renamed the changelog, the code review, and the tutorial and rewrote 21 lines in 11 files — the same file set the real commit carries — leaving every inherited line and the plan file alone; the rebase met six content conflicts (the shared tables, the version file and its test) and no add/add; the tutorial came out identical to `v0.1.41`'s, the changelog and the code review differing in one line each where the committed text names `main`'s `v0.1.40` as `main`'s, written after the real rebase. The cycles writes over both transcript stores (`~/.claude/projects` holds the three sessions the ticket names, the harness's configuration directory having moved since) read `0.1.37`, `0.1.40`, and `0.1.41` for `95c98c03`, `5504523c`, and `ef9a36d5`, the committed record regenerated at the outstanding-items step.
- **This release**: the plan claimed `0.1.46`; workflow ticket 24's release took `v0.1.46` while the cycle ran, so Step 2 ran `python3 -m tools.renumber 0.1.46 0.1.47` from the plan's number — four lines in two files, `worktrees.md` §8's origin line and three fictitious review names in the new cycles test — then the rebase, which met no conflict; the plan file keeps its `F_0.1.46` name.

**Window**: none — nothing on the box changes; no apply. No release note and no human read: a pre-General 0.x tag. The render-apply leaf stands at ~6,316 tokens, past its 6k warning and under its cap, its compaction workflow ticket 18's as the tracker records; every other budgeted file is under its warning.
