# Changelog - Week 2, 10-09-2026, V. 0.1.44

**Release Date**: Week 2, 10-09-2026 at 06:13
**Version**: 0.1.44 (previously 0.1.43)
**Object**: the web sources tightened to the ones that answer the box (§3.3, §15, [06] items 10–13; slice-1 ticket 71): the default `web.engines` loses DuckDuckGo, whose bot detection answers a SearXNG instance with a CAPTCHA at every search, the six allowed names unchanged; the frontend's page loader identifies itself with the rendered descriptive agent `GIDEON (+https://github.com/TNMD-FDO/gideon)`, which turns Wikipedia's 403 into the page, and bounds each fetch at 30 s where the pinned loader had aiohttp's 300 s default — the bound the maintainer's ruling at the code review, since the one site that tarpits any address-carrying agent would otherwise hold a search for five minutes
**Code review**: `docs/3-code-review/CR_w2_v0.1.44.md` (Codex loop, 3 rounds -> APPROVED)

## Changes

- **The default engine list** (`gideon/host/site.py`, `config/site.schema.json`, `config/site.example.yaml` and its greenfield twin; the plan's Implementation Details §§1–3): the `web.engines` field's default is Brave, Bing, Startpage, Wikipedia; `WEB_ENGINE_VALUES` keeps its six names, so DuckDuckGo and Google are allowed and off by default, the comment above it and the example file's allowed line saying why (DuckDuckGo's bot detection answers a SearXNG instance's request shape with a CAPTCHA at every search — 29 of 29 in the box's journal at triage — whatever the box's address, so the old default shipped a dead engine to every office and sent each query to a provider that answered nothing); the schema regenerated from the registry; the SearXNG settings document unchanged in code (it keeps and re-enables the site's list as given), a site naming `duckduckgo` still rendering it (`tests/test_render_searxng.py`'s new case); the validation test's defaults table reading the four.
- **The loader's agent and bound** (`gideon/host/render/owui.py`; §§4–5): two release constants beside the confirmation text — `WEB_LOADER_USER_AGENT`, version-free so the frontend's Compose block digest is stable across releases, and `WEB_LOADER_TIMEOUT_SECONDS` at 30 — rendered as `USER_AGENT` and `WEB_LOADER_TIMEOUT` in the search block beside the loader's variables, present on every host with search on (GPU and no-GPU) and absent where search is off (an off site, the disconnected frontend, the drill); the render test asserting both values on both searching hosts, both absent on the disconnected frontend, both in the difference set; the two searching fixtures regenerated (the settings file losing the engine, the frontend's block gaining the two lines, both manifests' site digests moving since both render the example site file), the search-off fixture byte-identical; the search-sentinel contract's key list unchanged by discovery's ruling. On the pinned frontend (Open WebUI v0.11.3, read and run inside the running container) `USER_AGENT` is read in the environment module and set by the `safe_web` loader's constructor on every fetch's headers; unset, langchain's placeholder goes out, which Wikipedia answers with a 403 whose body is stored as the page because `raise_for_status` is off by default and never set. The bound was not in the plan: the code review's one Major finding was the replay's Post row — a `TimeoutError` under the rendered agent three of three, read as transient at triage — and six agent shapes over the Post, Wikipedia, and CourtListener showed the two policies complementary (Wikipedia answers only an address-carrying agent; the Post tarpits exactly those), so the maintainer ruled the agent kept and the bound rendered beside it, a starting value under ADR-0017 (`exempt: policy`) amending the ticket's out-of-scope line; the pinned loader class instantiated as the factory does skipped the Post after 30.6 s with the other pages intact.
- **The proof assets** (`.scratch/slice-1/assets/`; §8): ticket 65's driver `65-search-turn.py` extended in place — a start timestamp line for the journal script and one line per stored document naming its host and character count, every line it printed before kept; `71-loader-lines.sh "<since>" ["<until>"]`, the frontend container's journal captured before filtering and six counts printed, never a line — the retry warning, the blocked-URLs warning, the content-loading traceback, the sync loader's per-URL error, langchain's per-page skip warning (the shape the container read at the code review added), and langchain's agent warning; `71-searxng-readback.py`, run inside the frontend's container, printing SearXNG's enabled engines and up to three fixed searches' per-engine counts and unresponsive lists, no result text; `71-ua-probe.py` as triage committed it, its 23-address list kept outside every checkout (`~/gideon-proofs/71-urls.txt`, recovered from the triage session's scratchpad copy of the probe, a reconstruction rule in the plan as the fallback) and never committed.
- **The proof** (`71-on-box.txt`; §9): from the cycle's worktree inside the quiet window, 00:05–04:57 CDT, no users — `render --diff` naming SearXNG's settings file and the frontend's block with the row `open-webui, searxng`; the replay before and after the apply under the placeholder, the release text, and a browser-shaped control — Wikipedia 403 of 141 characters under the placeholder and 200 of 96,447 under the release text, Reuters, AP News, Politico, and Justia refusing all three, every other row a record (the Post the one row that was not a transient); the apply recreating exactly the two by their reasons and verifying, the recreated container carrying the agent and starting without langchain's warning, SearXNG's enabled engines read back as four with the fixed search's unresponsive list empty on its first run, `render --diff` then `none`; three searched browser turns through the extended driver as the test account (before, after, and after the bound's apply), each with the engine idle and its four requests attributed, each chat deleted, the loader's failure shapes at zero in every window and one skip line in the last, the after-turn storing Wikipedia's page at 37,677 characters where the loader stored a 141-character refusal before, five of nine documents in that turn carrying no page (challenge pages and shells from sites whose bot managers key below the agent — the count ruling (d) measures and does not fix); the bound's apply recreating the frontend alone by its block, the value read back, `render --diff` empty. Codex's plan review two rounds (the URL list's durable home and reconstruction rule; gates separated from records with bounded retries; both searching fixtures' site digests); its code review three (the Post's tarpit ruled into the bound; the plan's stale whole-load clauses corrected).
- **Documents**: spec §3.3's default line, §15's search sentence (the four, DuckDuckGo and Google off, the loader's agent and bound), and Appendix E's entry; the render-apply leaf's search-rig row (the agent and bound; the residual clause corrected — an HTTP refusal is silent, the loader's lines a connection retry, a blocked address, or langchain's per-page skip) and `render/owui.py`'s constants line; the map's §16 network bullet, one pointer clause; the two research notes corrected in place and dated — the SearXNG note's §7 table gaining `USER_AGENT` and `WEB_LOADER_TIMEOUT`, the logs note's §A3 losing its non-2xx and whole-load premises (the factory's `continue_on_failure`, the session's absent timeout); `docs/agents/on-box-proofs.md` §11 (a third party's behaviour is measured with the real client class from inside the container; a live-network proof gates on the asserted behaviours and records the rest); the README's landed clause; ticket 71 resolved with the two facts beyond the triage's on it, ticket 41's dated line for the frontend bump's read-back; no ADR (§15: every General decision is a toggle or a config line); no release note before `v0.2.0`; `[06]` item 10's comment, posted at triage, the supersession's record.

**Window**: SearXNG and the frontend, recreated by the proof's apply from the worktree, and the frontend once more by the bound's apply, both free with no users on the box and inside the quiet window; the release's apply from the primary moves the units' `WorkingDirectory` alone. **The architecture memory**: the map at ~11,160 tokens; the render-apply leaf at ~6,044, past its 6,000 warning by this release's row — the maintainer compacts it in a new session once this ticket finishes (discovery's ruling), no ticket opened for it, ticket 72 queued on the same leaf; the glossary at ~14,784, reported and not budgeted.
