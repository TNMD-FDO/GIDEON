# Changelog - Week 1, 06-09-2026, V. 0.1.16

**Release Date**: Week 1, 06-09-2026 at 17:35
**Version**: 0.1.16 (previously 0.1.15)
**Object**: docs: ARCHI-rules.md's Step 7 checklist names the landed-mechanism case — a release that lands the mechanism an unlinked CONTEXT.md entry names adds that entry's _Leaf_: line through /domain-modeling, one sentence between the slice-start and slice-close sentences (slice-1 ticket 49, from v0.1.15's outstanding-items step: the slice-start sentence covers a new leaf, and the five entries left unlinked for tickets 12–16 — Engine verification, Citation stamp, Search egress, Prompt logging, Chat retention — each land into a leaf that already exists); no clause on the budget test, since the checklist's own 'run the budget test' sentence and the tests leaf's tripwire line already carry the proof, and existing entries only, a new entry being covered by the plan template's CONTEXT.md candidate and the glossary's preamble; no skill, test, leaf, map, or CLAUDE.md change, the archi-map-update block already pointing at the rules doc and the link rule already scanning the glossary; ticket 49 resolved with criteria 1 and 2 ticked and criterion 3 deferred to the first of tickets 12–16 that lands, each of which already carries its entry's note
**Code review**: `docs/3-code-review/CR_w1_v0.1.16.md` (Codex loop, 1 round -> APPROVED)

## Changes

- **The Step 7 checklist** (`docs/ARCHI-rules.md`; slice-1 ticket 49): one sentence between the slice-start sentence and the slice-close sentence — a release that lands the mechanism an unlinked `CONTEXT.md` entry names adds that entry's `_Leaf_:` line through `/domain-modeling`. It closes the gap ticket 48's close-out found at `v0.1.15`: the slice-start sentence covers a new leaf, but the five entries left unlinked for tickets 12–16 (Engine verification, Citation stamp, Search egress, Prompt logging, Chat retention) each land into a leaf that already exists, and until now only `CLAUDE.md`'s standing sentence and the glossary's preamble stated the shape, neither a step in the release's checklist. Three rulings shaped the sentence (`docs/1-plans/F_0.1.16_step-7-landed-mechanism-link.plan.md`): it sits after the slice-start sentence so the lifecycle cases read start, landed mechanism, close; it carries no clause about the budget test, since the checklist's "run the budget test" sentence and `docs/archi/tests.md`'s tripwire line are the one home of that proof; and it covers existing entries only, a new entry for a landed mechanism being covered by the plan template's `CONTEXT.md` candidate and the preamble's own rule. The checklist paragraph is one physical line, so the proof is `git diff --word-diff=plain`: the added sentence and no removed words.
- **Deliberately untouched**: the `TRIP-3-release` skill (its `archi-map-update` block already says to read the rules doc first, so the sentence lands where the block points; `docs/agents/tooling.md`'s block table unchanged), the `domain-modeling` skill (pinned byte for byte, `tests/test_skills_vendored.py` green), `tests/test_archi_budget.py` (the link rule already scans the glossary, so each later `_Leaf_:` line is proven by the existing rule), `docs/archi/tests.md`, `CLAUDE.md`, `CONTEXT.md`, and `docs/ARCHI.md` (no fact of theirs moves; the map at ~11.1k and every leaf under 3k).
- **Tracker**: ticket 49's first two criteria ticked and the ticket resolved; its third criterion — the first release that lands one of tickets 12–16 adds its entry's line — is deferred to that cycle in the completion comment, the record being the note each of those five open tickets already carries from `v0.1.15`.
- **Version**: bumped 0.1.15 → 0.1.16 (with `tests/test_version.py`); a patch — one sentence in one rules document, no product code, no rendered artifact, no site-file or `/etc/gideon` contract change (§2.1); no acceptance run (patch tags never run it). Pre-General 0.x tag: no release note (§21's check begins at `v0.2.0`). No tutorial: a documentation-process rule with no teachable core beyond the plan.
