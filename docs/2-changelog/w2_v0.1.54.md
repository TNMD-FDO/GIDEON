# Changelog - Week 2, 10-09-2026, V. 0.1.54

**Release Date**: Week 2, 10-09-2026 at 22:41
**Version**: 0.1.54 (previously 0.1.53)
**Object**: the second CSA learns the method, not only the tools (§0, §2.1; ADR-0036; workflow ticket 14): `docs/agents/onboarding.md`, the method's home for a person joining — a reading order that ends at the next ticket, one cycle end to end by its artifacts' paths (slice-1 ticket 55, released as `v0.1.52`), the tracker's shapes each cited to its one home, the first-day constraints (a worktree per cycle with the primary on `main`, one box, one release at a time, the box ledger, ADR-0036 stated as a rule); `tooling.md` §7, the README's table, and `CONTRIBUTING.md` point at it; proven by two fresh Sonnet sessions that read nothing else and dry-ran to a claimed ticket with a plan — the first found the command card's ticket-selection rule disagreeing with the board's and took a ticket whose cycle was in flight, so the card's "Working the frontier" became the rule's one home (the critical path's first row, else the frontier's first row, a ticket whose worktree exists skipped, the board as of the last outstanding-items step) and the second reached an unclaimed ticket by it, every ask written back, the record `.scratch/workflow/assets/14-fresh-session-proof.md`; the plan's number `0.1.53` taken by `main` while the cycle ran, the renumber skipped because its six rewrites were all evidence lines quoting `main`'s tag
**Code review**: `docs/3-code-review/CR_w2_v0.1.54.md` (Codex loop, 2 rounds -> APPROVED)

## Changes

- **The document** (`docs/agents/onboarding.md`; the plan's Implementation Details 1): seven numbered sections, each opening with what the reader holds after it — what the page is and is not, with the three pointers for what it does not cover (the spec and its Answers, the slice plan and the fog list, the domain's glossary and ADRs); the reading order (`CLAUDE.md` whole, `tooling.md`, the map then the ticket's leaf by §8, the board and the effort's observations file, the command card and the skills under `.claude/skills/<name>/SKILL.md` with `TRIP-1-plan`'s Step 2 as the plan template); the shape of the work (an effort a directory, the product slices beside `workflow` and `standing`, the three-phase cycle chained in one fresh session per ticket, a triage that rules and commits once, the fold, the map before its leaves, a research note cited by section, a ruling that contradicts a rule as a plan Conflict); one cycle end to end — slice-1 ticket 55: its triage commit `2d7cede`, the plan `F_0.1.51_office-values-redacted-at-capture.plan.md` and its renumber, `CR_w2_v0.1.52.md`, `w2_v0.1.52.md`, the asset `55-on-box.txt`, the release commit `f4dd374`, the tracker commit `687a46a` and its bins, the compaction `82a74aa` its crossing caused — every fact read from the artifact and the round counts from the changelog; the tracker's shapes (the `Statuses:` line as one pointer and never a second list, the `Blocked by:` grammar, the fold, the four bins, the strike rule, standing and `.out-of-scope/`, the board and its critical path, the cycles record); the first-day constraints (the worktree and the guard, a session starting in the primary, one box and one eval identity with the recreate-set read before an apply, one version file and one tag with `main` moving under a running cycle, the ledger's §1, §8, §11, §12, ADR-0036 as the review posture, the session's cost); and what the proof asked for. Every rule a Markdown link to the sentence that holds it; ~4,775 tokens against the 6,000 warning.
- **The pointers** (`docs/agents/tooling.md` §7, `README.md`, `CONTRIBUTING.md`; Implementation Details 2–3): §7's lead sentence names the method's home beside the lifecycle's, adding nothing about method (5,924 → 5,935 tokens, under its warning); the README's Documentation table gains the row after the tracker's; `CONTRIBUTING.md`'s "How the work is organized" gains the bullet after the tracker's.
- **The selection rule's one home** (`docs/6-memo/workflow-commands.md`, "Working the frontier"; added at implement from the proof's central finding, the code review's first round asking for the one home): the next ticket is the board's — the critical path's first row when that section has one, else the frontier's first row in the board's order — the board as of the last outstanding-items step, a cycle claimed since found by `git worktree list` and skipped, a ticket's `Status:` line on `main` the truth; the pre-board directory scan (workflow ticket 01) kept as history. The onboarding page cites the section in one clause.
- **The proof** (`.scratch/workflow/assets/14-fresh-session-proof.md`; Implementation Details 4–5): two dry runs by a fresh `general-purpose` Sonnet subagent, never a fork, the same prompt (recorded verbatim), each writing a claimed ticket and a full plan under the scratchpad only, with the harness's `CLAUDE.md` in its prompt as a second seat's first-day condition. The first run reached slice-1 73 through the board's critical path while the command card said the lowest-numbered file (slice-1 38), and 73's cycle was in flight in another worktree; its seven asks written back (the route, the board's age, the critical path's definition, the plan template's and the `worktree-claim` block's homes, where a session starts, the ledger's §8). The second run, over the corrected page, skipped 73 by its worktree and reached slice-0 21, unclaimed; its four refinements written back (which effort's observations file, the board as of the outstanding-items step rather than the release — `main` then carried `v0.1.53` without its tracker commit — `main` moving under a cycle, a ruling that contradicts a rule). Each run's claim diff and plan are in the asset whole. The resolving script (a scratchpad tool over backticked paths, link targets and anchors, `§n` citations, ADR numbers, hashes, and `<effort> ticket NN`) reports one placeholder path per document and nothing else; it is not committed.
- **Not this page's**, for the outstanding-items step: `docs/archi/host.md` names no per-step owned files (the first run read `steps/docker.py` for it); the plan's version number is the plan skill's Step 2 (the second run derived it from `__version__` and ADR-0002).
- **This release**: the plan's Codex review took three rounds (the proof's evidence kept whole rather than a skeleton; one fresh session per ticket chaining the phases; the open slice read from the board; the round counts sourced from the changelog; Markdown link targets resolved; one baseline for the claim's diff). Codex drafted the document in one batch; the requester's delta review fixed four broken anchors and the one-box paragraph and made the write-backs, the pointers, and the card's rewrite directly, each a few lines. The code review took two rounds (the two Major findings above, both addressed). No tutorial: a documentation release with no code. `main` took `0.1.53` while the cycle ran (slice-1 ticket 56's export boundary, which moved the runbooks to `docs/runbooks/`); `python3 -m tools.renumber 0.1.53 0.1.54 --dry-run` reported six rewrites, every one in the proof asset's quoted plans and narrative naming `main`'s real tag, none this cycle's own version reference, so the tool was not run and the version's documents were written once at `0.1.54` after a clean rebase, which brought ticket 56's export-boundary test: the page's six inline links into `.scratch/` and `docs/6-memo/` — prefixes the export drops — became backticked paths, the sentences unchanged (the full suite: 1,408 passed on the rebased tree); the case joins the `v0.1.47` fixture observation as an evidence-asset rule for the outstanding-items step. Lint, types, and the affected tests green on the rebased tree.

**Window**: no apply and nothing rendered, recreated, restarted, or rebooted; no rider. No release note and no human read: a pre-General 0.x tag. The box ledger: no shared thing touched; the §11 fetch found Transcribe's copy at rules version 2026-09-09.5 against ours at 2026-09-09.6, its own §12 subsection identical to our copy's, so nothing merged and no entry added.
