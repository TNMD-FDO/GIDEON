# Changelog - Week 2, 09-09-2026, V. 0.1.35

**Release Date**: Week 2, 09-09-2026 at 07:47
**Version**: 0.1.35 (previously 0.1.34)
**Object**: no search-query text anywhere: the frontend's log, audit, and telemetry posture rendered as named facts on every host and the drill — `GLOBAL_LOG_LEVEL` INFO, `AUDIT_LOG_LEVEL` METADATA with the audit file's path and its excluded paths pinned and its stdout sink off, the four OpenTelemetry gates off (traces need a second gate, and the client instrumentors set `http.url` to the full query-bearing URL), the same-request query cache off (a hit logs the generated queries at INFO), loguru's diagnose off — and the loader's private-address switch never rendered; SearXNG's one query-bearing line above DEBUG (`searx.network`'s `HTTP Request failed` warning on an engine's HTTP error, the URL carrying the query for GET engines) dropped by a third rendered file, `searxng/logging.json`, granian's own dictConfig applied in every worker before the application imports (its two loggers restated, `searx.network` at ERROR covering every engine's child logger, the hostname-only `ErrorContext` lines still reporting every failure), mounted read-only and named by `GRANIAN_LOG_CONFIG` in the service block; the frontend's failure-path tracebacks (`routers/retrieval.py`'s and `utils/middleware.py`'s `log.exception` on a non-2xx from SearXNG, the URL in aiohttp's error text) the one accepted residual, since v0.11.3 has no per-area level and only an app-wide CRITICAL would silence them — ruled at discovery, the ticket's criterion amended, the shape pinned by the test; the managed turn's optional `features` mapping (ticket 39 inherits it); the search-sentinel contract on the self-hosted tier — `tests/contract/search_sentinel.py` over a throwaway stack of Postgres, the frontend, SearXNG, and a stdlib stub under the frontend's own image that is the engine (the query task answered with the sentinel, the answer a fixed sentence), SearXNG's engines (the generic JSON engine for results and the shipped `bing` module repointed by `base_url` for the network warning, since the JSON engine turns `raise_for_httperror` off; both with `enable_http`, the stub's pages under a dotted Compose alias because the loader's validator rejects a hostname without a dot before its address guard), and the frontend's SearXNG stand-in; the frontend's environment derived from the render so a raised rendered level is red; five ordered runs each with a fresh sentinel — the happy path, SearXNG's engine answering 500, the frontend's SearXNG call answering 500 (hits only in the frontend's journal and only in the residual's shape), SearXNG's control over a WARNING logging file, the frontend's control at DEBUG — every container's journal and the audit file enumerated; the `search-sentinel` CI job after the frontend contract on its port and concurrency group; 12 new hosted tests (the stub by path, the classifier, the posture on every host, the logging document, the mapping); proven by hand on the box's runner (five runs green, the residual's shape and both controls seen) and on the box after the rebase onto `v0.1.34` (the recreate row the frontend and SearXNG alone, the apply, the granian variable and the file inside the container, one searched turn as the eval identity, the enumeration clean over every journal and the audit file; four pre-release SearXNG lines from real searches counted and left to retention); the research note `docs/research/search-path-logs-audit-telemetry.md`; the leaves, the map, the glossary's Search sentinel entry, `CLAUDE.md`'s researcher lesson, the runbook, the ledger (slice-1 ticket 16, resolved)
**Code review**: `docs/3-code-review/CR_w2_v0.1.35.md` (Codex loop, 3 rounds -> APPROVED with observations)

## Changes

- **The frontend's posture** (`gideon/host/render/owui.py`): `GLOBAL_LOG_LEVEL=INFO`, `AUDIT_LOG_LEVEL=METADATA`, `AUDIT_LOGS_FILE_PATH` (the container's data-directory path, a release constant the enumeration reads), `ENABLE_AUDIT_STDOUT=false`, `AUDIT_EXCLUDED_PATHS` (the default `/chats,/chat,/folders`, a release constant), `ENABLE_OTEL`, `ENABLE_OTEL_TRACES`, `ENABLE_OTEL_METRICS`, and `ENABLE_OTEL_LOGS` false, `ENABLE_QUERIES_CACHE=false`, `LOGURU_DIAGNOSE=false` — each the pinned image's default made a rendered fact (ADR-0017), on every host and the drill; `ENABLE_LOCAL_WEB_FETCH` never rendered, a test holding its absence. The three render fixtures moved on these keys.
- **SearXNG's logging file** (`gideon/host/render/searxng.py`, `render/__init__.py`, `render/compose.py`): `searxng_logging_document()` builds granian's `dictConfig` document (version 1, existing loggers kept, granian's `_granian` and `granian.access` restated as shipped because granian replaces the `loggers` key wholesale and keeps its handlers when the file names none, `searx.network` at the release's level ERROR); `SearxngLoggingArtifact` renders it as `searxng/logging.json` (owner `searxng`, 0644, present iff `web.search` is on) with sorted keys; the service block gains `GRANIAN_LOG_CONFIG` and the read-only mount beside the settings file. The engines' `ErrorContext` warnings are hostname-only by construction and stay.
- **The residual, recorded**: the pinned frontend has one log knob (`SRC_LOG_LEVELS` an empty legacy dict), so `routers/retrieval.py`'s and `utils/middleware.py`'s `log.exception` on a non-2xx from SearXNG print the query-bearing URL at ERROR and nothing short of CRITICAL app-wide silences them; failure-path only, the query inside a URL, ephemeral under journald's retention and outside every backup set (ADR-0027). Ruled at discovery; the ticket's second criterion amended; the test's third run pins the shape. The loader's fetch-failure lines carry a fetched page's URL, a fact recorded on the ticket for a person.
- **The managed turn** (`gideon/host/owuiturn.py`): `managed_turn(features=...)`, an optional mapping carried under the body's `features` key, the body byte-identical when absent.
- **The sentinel contract** (`tests/contract/search_sentinel.py`, `tests/contract/sentinel/{compose.yaml,stub.py}`): the stack, the stub's routes (`/v1/models`, `/v1/chat/completions` streamed and not, `/json`, `/search`, `/searxng/search`, `/page/<n>`, `/health`; a content-free request line per request), the frontend's environment from `owui_environment` over the example site for a named tuple of keys, SearXNG's settings from the render's document with two engines (`stub-json` over `json_engine`, `bing` over the shipped module with `base_url` overridden, both `enable_http`, explicit shortcuts), the logging file from the artifact and a control variant at WARNING, `LOG_SOURCES` (the four services' Compose logs and the audit file at the rendered path), `is_frontend_residual`, `mint_sentinel`, and the five runs in order; the stack starts the stub and SearXNG before the frontend so its base model resolves and `bootstrap`'s live attachment check runs over a real listing, and each turn refreshes the model listing first.
- **CI** (`.github/workflows/ci.yml`): the `search-sentinel` job after `frontend-contract`, the box's runner, `concurrency: frontend-contract`, the system Python.
- **Tests** (12 new): the posture on the GPU host, the no-GPU host, and the drill and the local-fetch switch's absence; the logging document and its artifact; the service block's variable and mount; the artifact order; the managed turn's body with the mapping; the stub by path in both modes (routes, the streamed chunks and `[DONE]`, the content-free request line, the 404, the standard-library rule); the classifier and the sentinel's shape. `tests/test_office_values.py`'s curated allowlist gained the four documentation hosts the research note cites.
- **The proof** (`.scratch/slice-1/assets/16-on-box.txt`, `16-sentinel-turn.py`, `16-enumerate-logs.sh`): as the Object says. The contract's first two by-hand runs taught two facts now in the module: SearXNG's engines refuse a plain-HTTP URL unless `enable_http` is set (an `InvalidSchema` error context before any request), and the frontend's loader validates a result URL with a library that rejects a hostname without a dot before its address guard runs.
- **Documents**: the research note (branch `research/search-path-logs-audit-telemetry`, merged); the render-apply leaf (the search rig's row and a row for the posture, the rendered tree, the constants, the tests), the tests leaf (the contract tier), the map (§4's tree, §6's CI table, §12's slice-1 row, §16's network bullet); `CONTEXT.md`'s Search sentinel entry through `/domain-modeling`; `CLAUDE.md`'s Subagents lesson (a researcher spawned inside a worktree); `docs/agents/on-box-proofs.md` §7 (two cycles in flight share one box); the observability runbook's retention section; the box ledger's entry; README's status and contract row; the notes on ticket 39 and greenfield [22] item 15; the ticket's close-out.
