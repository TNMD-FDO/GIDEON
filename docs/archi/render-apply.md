# Render and apply — architecture leaf

**Read this when** implementing `render`, `apply`, `secrets rotate`, `models pull`, the rendered tree, or secrets; the owning modules are `gideon/host/render/`, `gideon/host/apply.py`, `gideon/host/rotate.py`, `gideon/host/secrets.py`, `gideon/host/weights.py`, and their tests. The engine and the frontend's runtime are in [`engine-frontend.md`](engine-frontend.md); the map is [`../ARCHI.md`](../ARCHI.md); its §8 table names every command's leaf.

## Inventory

| Surface | Code | Spec / ADR |
|---|---|---|
| `render`, `apply` | `host/render/`, `host/apply.py` | §3.5 |
| the release-note template and its test; `workspace.knowledge` staged off until Ingestion GA | `docs/release-notes/TEMPLATE.md`, `tests/test_release_notes.py`, `compose/open-webui/permissions.yaml` | §21, §4.2, slice-1 ticket 17 |
| `models pull` and apply's `models` stage: the weights tree under `/data/models` and the pull record | `host/weights.py`, `config/egress.yaml` | §5.4, §2.3–2.4, slice-1 ticket 03 |
| the search rig while `web.search` is on, on either host: SearXNG as shipped on the Compose network alone, its three rendered files, the frontend's non-tool search connection, its page loader's rendered agent and 30 s bound, the reminder, the kill switch through the recreate rule; no search-query text in any log — the frontend's log, audit, and telemetry posture and SearXNG's logging file as rendered facts, the recorded residuals (the frontend's traceback on a non-2xx from SearXNG; the loader's lines naming a failed fetch's address — a connection retry, a blocked address, langchain's per-page skip — while an HTTP refusal is silent, its body entering the context as a source), the search-sentinel contract that holds it ([`tests.md`](tests.md)) | `host/render/{searxng,compose,owui,proxy}.py`, `host/secrets.py`, `host/owuiturn.py`, `images.lock` | §15, §19.4, §3.3, §3.5, §2.3, §1.7, §1.8, §20.1, [06] items 9–15, [22] items 14–15, ADR-0001, ADR-0010, ADR-0017, ADR-0027, ADR-0028, slice-1 tickets 15, 16, 65, 71, 72 |
| every rendered service's memory limit from the profile's memory table, the drill's two from their namesakes' rows; the overview board's limit series | `host/render/{compose,drill}.py`, `compose/grafana/dashboards/overview.json` | §7.6, §3.5, ADR-0017, ADR-0028, slice-1 ticket 19 |
| `secrets rotate <name>`: one rotatable secret regenerated and exactly its consumers recreated, the rest converged as apply does; the registry's rotation classes and the supplied registry; the consumer map over the render structure | `host/rotate.py`, `host/secrets.py`, `host/render/consumers.py`, `host/apply.py` | §1.7, §4.4, §3.5, §1.5, slice-1 ticket 24 |

## Commands

- **`render [--diff]`** (§3.5, `render/`): `render_all(inputs)` is pure and touches no `Host`; `RenderInputs` carries the site, the locks, the selected profile, the host facts, the no-GPU marker, the build-box marker, the templates, the release string, the declared `secrets` (read for the `env_file` only), and the checkout, and the manifest's `inputs` block records their digests. **The marker governs the engine**: with it absent, the profile's generator becomes the engine service, and the frontend's connection, the two model records, and the engine's scrape job, rule, and board panels render with it — each constructed only under the marker's absence, never built then deleted; the drill's frontend is always disconnected. **The build-box marker governs the host-unit rule and the node exporter's unit pattern**: both are constructed only under it, while an undeclared host collects its GIDEON units alone. **The site's `web.search` governs the search rig**: on, the `searxng` service, its three files, the frontend's search block, and the probe and its rule render ([`stack.md`](stack.md)); off, `ENABLE_WEB_SEARCH=false` renders alone, the three files are stale, and the departed service is left to orphan removal — never in the drill. **Every rendered service carries `mem_limit`** from its row in the profile's memory table ([`host.md`](host.md)), the table held to the stack's services both ways before the marker and the site settle the host's, so one lock is complete on every host or refused on every host; the drill's two take their namesakes' rows. An `Artifact` carries its path, mode, owners (the services that mount it), templates, `secret` flag, `applies(inputs)`, and a pure `emit(inputs)`; `compose.yaml` is a Python structure serialized by the deterministic `yamlout.dump`. `render_to_disk` — the one path `render` and `apply` share — classifies each file `new/changed/unchanged/stale/foreign`, refuses on a foreign file, writes atomically, removes stale files, and writes the manifest last. **The recreate rule** (`recreate_judgment`) is judged against `applied.yaml` alone, with three owners per service — the rendered files that mount it, its own `compose.yaml` block, and the top-level sections jointly (`compose_digests`) — and names every service when the record predates the map, the top-level digest moved, or the whole file moved with every digest equal; each reason is kept for apply's rows, the union intersected with the running set, every service before the first verified apply; a malformed record refuses.
- **`apply`** (§3.5, `apply.py`), in order: preconditions → **secrets** (`ensure_generated`; a break-glass password printed exactly once, on the run that created it) → render → registry → pull → **models** (`weights.pull_profile`, before anything restarts) → **recreate** (the judgment's members within `STORE_SERVICES` force-recreated, the row grouping them by reason — `first apply`, `changed rendered files`, `changed compose block`, `changed compose top-level`) → **stores** (`stores.converge`: roles and databases absent-only, existing roles never altered; the forward-only migration runner, one transaction per `migrations/NNNN_*.sql`; the pgBackRest stanza and `check`) → **start** (the judgment's other members the same way, then `up -d --remove-orphans`; the frontend never starts against an uninitialized database) → **apply-manifest** (`owui.wait_ready`, then `owui.bootstrap`: keys minted absent-only, groups converged by name with LDAP-owned memberships untouched, Functions and models synced desired-state and **read back** row by row, then the live model listing read, each listed manifest model held to its attachment key) → verify (every service but the engine within the default bound, then the engine alone within what remains of `ENGINE_READY_SECONDS`; `compose ps` equal to `service_names(inputs)`, every healthcheck healthy, the ingress probe, the frontend's `/ready`, Grafana's health) → **timers** (the units converged against `applied.yaml`) → record (`applied.yaml`). Every Compose invocation goes through `stack.compose_argv`/`exec_argv`; `stack.logs_fix` is the one operator fix for a misbehaving service. The stages from `secrets` through `record` are `converge`, the callable `run_apply` and `secrets rotate` share.
- **`secrets rotate <name>`** (§1.7, §4.4; `rotate.py`): pre-run refusals on stderr before any row, write, or Compose call — an unknown name, a supplied name (its replacement path), a `role`, `account`, or `seeded` entry (its second home and the by-hand route) — then `preconditions` (apply's Compose check; the rendered tree and `applied.yaml` present; the recreate judgment against the record strictly empty, since a pending change cannot be attributed to one secret) → `plan` (the class, each mount consumer with its unavailability, each carrier the converge recreates; a `rewrite` secret nothing on this host mounts or carries is one row and exit 0 with nothing written) → `rotate` (`secrets.rotate_generated`, or a minted key's file removed) → `recreate` (`apply.force_recreate_services` over the mounts, each once) → `converge`. Invariants: a service recreated at most once; exactly the consumers; the value in no row, refusal, argv, environment, or written file but the secret's own and the secret-flagged artifacts that declare it; a failure past the write finished by `apply`, then a fresh rotation where a mount consumer was not reached.
- **`models pull`** (§5.4, §2.4; `weights.py`): the selected profile's models converged into the hub-cache tree — every pinned path classified against disk, a mismatch re-fetched and reported, a `foreign` snapshot entry the lock does not name refused and never deleted, each blob checked before it is renamed into place; rows `fetched`/`present`/`failed` per model, a fetch the lock rejects naming repin-or-republish, every host a fetch traverses held to the `install-upgrade` group ([`host.md`](host.md)). **The pull record** (`gideon/pulls.yaml`: claim, current, previous, retiring) is walked in atomic writes under a claim, `retiring` holding exactly the references leaving the named slots, so the pinned set and the newest previous complete set stay for rollback. Runs on a no-GPU host too (the marker governs serving, ADR-0035).

## Configuration and the rendered tree

- **Secrets are files** (§1.7): `gideon/host/secrets.py` is the one module that names `/etc/gideon/secrets/` (every other module calls `secret_path()`) and holds `SECRET_REGISTRY`: the password-kind secrets apply generates absent-only (the two break-glass passwords as `print_once`), the two API keys the apply-manifest stage mints absent-only, and SearXNG's signing key; supplied secrets (the LDAP bind password, the TLS key, the SMTP password, the proxy credentials) are never generated, `SUPPLIED_REGISTRY` naming each one's replacement path, and `read_secret` returns a `missing` flag so a caller can name the command that creates a generated one. Every secret file is `root:gideon` 0440; unprivileged containers read through `group_add`; the engine's key is read inside its container from the Compose secret mount by a fail-closed entrypoint, its one other home the frontend's rendered env file on a GPU host (§1.7's exception); the audit role's password reaches the frontend's container as a Compose secret mount too, for the guardrail's trip writer. Every entry carries a **rotation class** — `rewrite` (the file the value's only home; rewritten in place), `remint` (the two minted keys; removed and re-minted), `role`, `account`, `seeded` (a second home the file does not own; refused) — `ROTATABLE_NAMES` the first two. The **consumer map** (`render/consumers.py`) derives, for every secret, the services that mount it (the Compose blocks' `secrets:` lists) and those that carry it (each secret-flagged artifact's `secret_names(inputs)`, held equal to what it reads), so `secrets rotate` and render cannot disagree. Beside them: `/etc/gideon/backup_age_recipient` (public; the decrypting identity lives only in the office password manager) and the keyed **secrets fingerprint** (a hash over the secret files keyed by the session key's digest, carried in every backup manifest, so a restore can say whether the secrets on disk are the set's without giving a human-chosen password anything to test against).
- **`/etc/gideon/rendered/`** (§3.2) holds the artifacts `ARTIFACTS` registers — `compose.yaml`, the Caddyfile, the Prometheus and blackbox configs, Grafana's LDAP, provisioning, and boards (the GPU-host jobs, rules, and panels marker-governed blocks), `pgbackrest.conf`, the systemd units (`gideon-backup` runs `backup run` then `backup push`, the drill and the quarterly `gideon-backup-verify` `After=` it; every `OnCalendar` zoned; every Compose service carries `TZ`), **`open-webui/env`** (the §1.7 `env_file` exception — the LDAP bind password, the database URL, the break-glass password, and on a GPU host the engine's key; 0600, flagged `secret`, never printed by `render --diff`), the three SearXNG files present only while `web.search` is on — **`searxng/settings.yml`** (the pinned image's defaults made rendered facts, the site's engines kept and re-enabled by name), **`searxng/env`** (the same exception: the signing key and, under a proxy, its URL with the credentials), and `searxng/logging.json` (granian's loggers as shipped and `searx.network` at ERROR) — and `open-webui/manifest.yaml` (the apply manifest: the groups, the two identities, the two Filters' Function rows on every host, each with its whole file as its `content`, and on a GPU host the two model records, General's carrying the stamp's attachment). Beside them: `manifest.yaml` (every render: file sha256/mode/owners, the per-block and top-level Compose digests, the inputs' digests) and `applied.yaml` (the same shape, written by apply only after verify — the record the recreate rule is judged against). The TLS key, the frontend's session key, and the Postgres superuser password reach containers as paths (`*_FILE`), never through `env`.

## Release constants by module

**Release artifacts that are not site keys** (ADR-0028) are constants in the module named; a reader opens the module for the name and value.

- `compose/open-webui/permissions.yaml` — the §4.2 permission set, rendered as the frontend's env defaults and each group's manifest permissions (binding every `user`-role account, bypassed by the frontend's admins by design).
- `render/owui.py` — the API-key endpoint allowlist; the two machine identities, their group, and the eval identity's password name; the readiness bounds; the frontend's connection facts and frontend-wide switches, the image policy among them (`Content-Security-Policy: img-src 'self' data: blob:`, set by the pinned frontend's own security-headers middleware on every response, so the citation UI's favicons never leave the workstation — slice-1 ticket 72; no other directive); the two model records, General's texts from `compose/open-webui/general.yaml` with `$office_name` the one placeholder; the search connection's reminder, result count, and domain filter, the page loader's agent and its bound; the two Filters' identities and General's attachment list.
- `render/compose.py` — the store tier; the engine's healthcheck, entrypoint, and access-log exclusions (slice-1 ticket 51); the frontend's command, its access line off (slice-1 ticket 69; the drill's frontend shares it); the memory-limit helper.
- `render/engine.py` — the engine's identity (service name, port, secret name, scrape job name, metrics target), shared by the service, the connection, the Prometheus configuration, and the engine rule, so `owui.py` never imports `compose.py`.
- `render/searxng.py` — the search service's identity, the settings document's pinned defaults, and the logging document's logger and level; `render/proxy.py` — the one proxy helper both env files use; `host/site.py` — the six engine names and the bare-domain grammar ([`host.md`](host.md)).
- `render/systemd.py` — the four timer calendars, each zoned to the office; `render/grafana.py` — the alert thresholds.
- `apply.py`, `users.py`, `ldap.py`, `weights.py` — the timeout bounds.

## Tests

- `test_render.py`, `test_render_owui.py`, `test_render_grafana.py`, `test_yamlout.py` — the three host-kind fixtures render byte-for-byte to `tests/fixtures/render/` (the failure names `tests/regenerate_render_fixtures.py`); render-twice equality; the classification matrix; the recreate rule with its reasons and the record's refusals; every timer zoned, every service carrying `TZ` and a memory limit derived from the loaded profile, the table held to the rendered union both ways; the render command's secret rows.
- `test_render_searxng.py` — the three SearXNG artifacts present iff search is on, the service block as shipped with no port, the flip both ways under the recreate rule.
- `test_rotate.py` — the command over `test_apply.py`'s fake stack after one verified apply: each class rotated or refused before any write or Compose call, the consumers recreated once each with the value nowhere else, every precondition and failure variant with its recovery, the no-GPU case; `test_render_consumers.py` — the map over the three sites, each env artifact's declaration equal to its emitter's reads; `test_secrets.py` — the registry's classes and rotatable names, the supplied names held to render's, `rotate_generated`'s contract.
- `tests/contract/owui_apply_manifest.py` proves the apply manifest is desired state and `tests/contract/search_sentinel.py` that the search path leaves no query text in any log; both contracts are in [`tests.md`](tests.md).

## Cross-references

- Cites: [`host.md`](host.md), [`engine-frontend.md`](engine-frontend.md), [`backup-restore.md`](backup-restore.md), [`stack.md`](stack.md), [`tests.md`](tests.md)
- Cited by: the map's §§2, 4, 7, 8, 12, 13, and 16
